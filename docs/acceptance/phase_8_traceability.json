{
  "phase_id": "8",
  "note": "Derived from REQUIREMENT_REGISTER.md by reading the Phase column, not from any earlier summary. NO REGISTER ROW CARRIES PHASE 8: the canonical denominator for this phase is exactly zero, which makes it the first zero-denominator phase this repository has submitted. `claims` is therefore empty, and so is `ark_req_ids_closed` in phase_8_report.json. This is legal because and only because the denominator is empty - check C6 resolves the entitlement from the register, never from this record, and `discharge_shape.check_claim_shape` refuses BOTH directions: a record that claims nothing while the register assigns requirements, and a record that claims anything while the denominator is empty. The second direction is what protects this phase, and it is strictly stronger than the pre-F-0040 rule: no synthetic claim, no contract-id-as-requirement claim and no foreign-phase claim can be manufactured to make this file look busy. Phase 8's deliverable is the C-21 contract from CONTRACT_INVENTORY.md - kind INT, ADDITIVE, document docs/contracts/worker.md, verified by admission tests - and a contract is evidenced through the report's public_contracts, tests_executed and evidence_created, never by pretending it is an ARK-REQ. execution.scheduler's only registered requirement is ARK-REQ-0354 (failure domains isolated), which the register assigns to PHASE 31; it is NOT CLAIMED here, and a control derives that fact from the register rather than asserting it in prose. Cumulative discharged therefore remains 80 and MANDATORY verified remains 79, both unchanged by this phase. No applicability waiver was requested or granted, because there is no requirement whose applicability could be waived.",
  "claims": []
}
