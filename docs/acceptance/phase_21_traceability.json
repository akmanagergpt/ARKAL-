{
  "phase_id": "21",
  "note": "Derived from RequirementRegister.for_phase(\"21\"): exactly ARK-REQ-0117, ARK-REQ-0163, ARK-REQ-0164, ARK-REQ-0165, ARK-REQ-0166 (all MANDATORY) and ARK-REQ-0167 (OPTIONAL). ARK-REQ-0117, 0165, 0166 are owned by control.policy; ARK-REQ-0163, 0164 by engineering.plugin. Matrix Gate column maps Phase 21 to HUMAN_GATE_4 (\"GATE 4 if boundary changes\") in GovernanceState.phase_gates via the same mechanical matrix-cell derivation Phase 19's HUMAN_GATE_4 used, and the checker's _human_gate_for reads the gate token unconditionally regardless of the canonical qualifier's prose. HGR-002 (Phase 19's HUMAN_GATE_4 grant) is mechanically proven not to satisfy Phase 21: human_gate_authorization._find_phase_gate_grant requires an exact (gate_id, phase_id, evidence_digest) match, and HGR-002-SCOPED's own row is bound to phase=\"19\" only. No HUMAN_GATE_4 grant exists for Phase 21 as of this submission - the verdict is therefore expected to be AWAITING_HUMAN_GATE, not PHASE_ACCEPTED_BY_MACHINE, regardless of every other check's result. This is a genuine, mechanically-derived stop condition, not a defect in this candidate. ARK-REQ-0167 is OPTIONAL and not attempted; REQUIREMENT_REGISTER.md states plainly that optional entries do not contribute to mandatory coverage and never block release.",
  "claims": [
    {
      "req_id": "ARK-REQ-0164",
      "state": "SATISFIED",
      "implementation": "backend/arkali/engineering/plugin/manifest.py (PluginManifest: frozen, content-addressed, semver-2.0.0-validated version per CONTRACT_INVENTORY.md row C-30) and lifecycle_pipeline.py (admit_plugin, driving a real instance of Phase 3's PluginLifecycle state machine - STATE_MACHINES.md sec 6 - unmodified, through DISCOVERED -> MANIFEST_VALIDATED -> PERMISSIONS_DECLARED -> {APPROVED | stops}). An unmappable declared permission is a named, recorded outcome (PluginLifecycleOutcome.unmappable_permissions), never a raised exception - the 'cannot crash core' half of this requirement begins at admission time. No thirteenth state machine is declared; STATE_MACHINES.md stays at 12.",
      "evidence": "unit: backend/tests/engineering/test_plugin_manifest.py (27 tests: semver validation positive/negative, identity, content-addressing). integ: backend/tests/engineering/test_plugin_lifecycle_pipeline.py (12 tests: every real canonical class individually admissible, unmappable permission stops at PERMISSIONS_DECLARED without raising, the pipeline's own pre-filter proven to agree with the guard it defers to, no second operation-class literal declared, STATE_MACHINES.md stays 12). journey: backend/tests/engineering/test_phase_21_journey.py::TestCondition0164And0165ManifestAdmission (3 conditions, real vocabulary and real machine). Executed by phase_21_report.json.",
      "note": "docs/contracts/plugin_manifest.md section 2."
    },
    {
      "req_id": "ARK-REQ-0165",
      "state": "SATISFIED",
      "implementation": "No second operation-class vocabulary is declared by engineering.plugin - control.policy.operation_class.OperationClassVocabulary remains the sole store of the fourteen canonical classes (its own docstring already states plugin manifests share it). lifecycle_pipeline.admit_plugin checks every declared permission against the live vocabulary at admission time and reuses plugin_lifecycle_state_machine.py's pre-existing permission_mapping_guard on the PERMISSIONS_DECLARED -> APPROVED transition rather than re-implementing the check.",
      "evidence": "integ: backend/tests/engineering/test_plugin_lifecycle_pipeline.py::TestAllPermissionsMappableReachesApproved::test_every_real_canonical_class_individually_is_admissible (every one of the live vocabulary's classes, read at call time, not a hand-picked subset) and TestNoSecondAuthorityIsIntroduced (no canonical class literal appears in the module source; STATE_MACHINES.md machine count stays 12). journey: test_phase_21_journey.py::TestCondition0164And0165ManifestAdmission::test_a_manifest_with_every_permission_mappable_reaches_approved. Executed by phase_21_report.json.",
      "note": "Owner control.policy per the register; satisfied by composing the already-accepted Phase 4 vocabulary and the already-accepted Phase 3 guard, not by new control.policy code. docs/contracts/plugin_manifest.md section 3."
    },
    {
      "req_id": "ARK-REQ-0166",
      "state": "SATISFIED",
      "implementation": "backend/arkali/control/policy/pdp.py::PolicyDecisionPoint.decide_or_deny_unmapped - a second, additive entry point alongside the existing decide(). SECURITY_ARCHITECTURE.md sec 2: 'Any action not mappable to a class is DENY.' decide() already enforces this by raising UnknownOperationClass, correct for a caller that can treat the exception as the refusal; a plugin action invocation cannot afford an uncaught exception (that would itself be the ARK-REQ-0164 'crashes core' failure), so the new method resolves an unmappable class to a real, audited PolicyDecisionRecord instead, reading the resolution from AUTHORITY_MAP.yaml's unmapped_action_resolution rather than hard-coding DENY. decide()'s own raising behaviour, and every existing caller depending on it, is unchanged.",
      "evidence": "unit+neg: backend/tests/security/test_pdp_decisions.py::TestDecideOrDenyUnmappedNeverRaisesForAnUnmappableClass (6 tests: resolves rather than raises, resolution read from the authority map not hard-coded, real audited record with a real reason, agrees with decide() for every mappable class over every tier, a genuinely unknown trust tier still raises, decide() itself is unmodified and still raises - TestMalformedInputFailsClosed's pre-existing 42 tests are unchanged and green). journey: test_phase_21_journey.py::TestCondition0166UnmappableActionIsDeny. Executed by phase_21_report.json.",
      "note": "docs/contracts/plugin_manifest.md section 4."
    },
    {
      "req_id": "ARK-REQ-0117",
      "state": "SATISFIED",
      "implementation": "backend/arkali/engineering/plugin/trust4_execution_gate.py composes control.isolation.IsolationAuthority.resolve and control.policy.WorkflowApprovalGate - both Protected Core, both reused unmodified - the identical composition Phase 19's engineering.import.execution_gate established for TRUST-3 (ARK-REQ-0116), rebound to TRUST-4 and to a stricter binding: MS Trust-Tiered Isolation requires TRUST-3 approval 'before first execution' (once) but TRUST-4 approval 'per execution' (every time). WorkflowApprovalGate.is_enforced_approval's existing staleness check is reused against execution_binding_ref - a fresh, content-addressed identity per attempt - rather than a static tier-assignment ref, which is what turns 'once' into 'every time'. Isolation is checked first so it cannot be skipped by an approval-only path.",
      "evidence": "integ: backend/tests/engineering/test_plugin_trust4_execution_gate.py (12 tests: unique-per-attempt binding, isolation-must-be-satisfiable including this real host's honest TRUST-4 DENY via an un-doubled backend probe, approval-must-be-genuinely-human, and TestApprovalIsScopedToExactlyOneExecution proving a previous execution's approval never authorises the next one). journey: test_phase_21_journey.py::TestCondition0117Trust4ExecutionGate (this real host's honest DENY, and an approval genuinely proven not to carry over to a second execution). Executed by phase_21_report.json.",
      "note": "Owner control.policy per the register; satisfied by composing the already-accepted Phase 4 isolation authority and Phase 17/19 approval gate, not by new control.policy code beyond the reused, unmodified methods. docs/contracts/plugin_manifest.md section 5."
    },
    {
      "req_id": "ARK-REQ-0163",
      "state": "SATISFIED",
      "implementation": "backend/arkali/engineering/plugin/research.py: resolve_sources stably partitions any declared source set into every OFFICIAL source (in given order) before any COMMUNITY source - the module owns the resolution order, not a domain allowlist (which concrete domains are official is governed data a future provider/domain-pack registry would own, out of this phase's denominator). ResearchResult.mutation_applied is Literal[False] - the StaticInspectionReport.executed idiom Phase 19 established for a structural, type-level no-execution proof. attempt_fetch performs no real network call; it classifies the attempt as NETWORK_EXTERNAL and asks the real PDP via the new primitive-typed PolicyDecisionPoint.decide_network_egress (added because policy_contract.PolicyRequest sits at its fan-in ceiling of 15) whether it may proceed.",
      "evidence": "prop: backend/tests/engineering/test_plugin_research.py::TestOfficialSourcesAreAlwaysTriedFirst (exhaustive permutation sweep over 4 sources - 24 orderings - proving official-first for every possible input order; hypothesis is NOT_CONFIGURED on this host per BUILD_STATE.md). sec: TestRealPdpGovernsEveryFetch (the real PDP, not a fixture: Local-Only genuinely denies research's own NETWORK_EXTERNAL egress - the VDC names 'research' explicitly among the paths whose Local-Only DENY evidence is required - and Local-Only off does not force DENY). journey: test_phase_21_journey.py::TestCondition0163ResearchOfficialSourcesAndNoMutation. Executed by phase_21_report.json.",
      "note": "docs/contracts/plugin_manifest.md section 6."
    }
  ],
  "deferred": [
    {
      "req_id": "ARK-REQ-0167",
      "reason": "OPTIONAL classification (REQUIREMENT_REGISTER.md Appendix: 'Optional entries do not contribute to mandatory coverage and never block release'). Not attempted this submission; MCP-compatible adapter shape remains a future session's work, not a canonical gap."
    }
  ]
}
