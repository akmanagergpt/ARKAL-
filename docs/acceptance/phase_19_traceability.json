{
  "phase_id": "19",
  "note": "Derived from RequirementRegister.for_phase(\"19\"): exactly ARK-REQ-0115, ARK-REQ-0116, ARK-REQ-0161, ARK-REQ-0162 and ARK-REQ-0348, all MANDATORY. Four are owned by engineering.import; ARK-REQ-0116 is owned by control.policy and is satisfied by composing the already-accepted WorkflowApprovalGate unmodified, so no file under backend/arkali/control/ is touched by this phase and the normal (not stronger) verification profile applies. C-29 is INT with STRICT compatibility. engineering.import was already declared in AUTHORITY_MAP.yaml since Phase 1 bootstrap (module_root project_import per ERR-001; only an empty __init__.py and the pre-existing ImportProject state machine existed), so no gap-closing governance ruling was required. Matrix Gate column HUMAN_GATE_4 (\"weakening or change of a security boundary, sandbox tier or protected-core policy\") applies to this phase and is NOT recorded ACCEPTED in docs/acceptance/HUMAN_GATE_RECORDS.md as of this submission (only HUMAN_GATE_1 is recorded) - the verdict is therefore expected to be AWAITING_HUMAN_GATE, not PHASE_ACCEPTED_BY_MACHINE, regardless of every other check's result. This is a genuine, mechanically-derived stop condition, not a defect in this candidate.",
  "claims": [
    {
      "req_id": "ARK-REQ-0115",
      "state": "SATISFIED",
      "implementation": "backend/arkali/engineering/project_import/static_inspection.py: StaticInspector.inspect(root) reuses engineering.codeintel.PythonGraphBuilder unmodified (declared sibling edge engineering.import -> engineering.codeintel, AUTHORITY_MAP.yaml) - AST-only via ast.parse, never importlib/exec/eval/subprocess against the target. source_address is a content address over the sorted (relative_path, sha256(bytes)) file listing. StaticInspectionReport.executed is Literal[False] - a type-level guarantee, not a runtime flag.",
      "evidence": "sec: backend/tests/engineering/test_import_static_inspection.py::TestNoExecutionOccurs::test_inspecting_a_landmine_never_triggers_its_side_effect - a fixture whose only defect is a side effect at import time is never triggered. prop: TestDeterministicAddressing (three tests: identical content addresses identically, one changed byte changes the address, filesystem enumeration order does not affect the address) and test_import_contracts.py::TestStaticInspectionReportIsStructurallyHonest::test_executed_cannot_be_constructed_true (ValidationError). integ: backend/tests/engineering/test_phase_19_journey.py TestAllSevenAcceptanceConditions::test_condition_1_static_inspection_before_execution and test_step_0_no_execution_capability_exists_in_the_shipping_source (text-scan proof over the whole package). Executed by phase_19_report.json.",
      "note": "Both register evidence keys, sec and prop, are supplied, plus integration evidence beyond the minimum. docs/contracts/import.md section 2."
    },
    {
      "req_id": "ARK-REQ-0116",
      "state": "SATISFIED",
      "implementation": "backend/arkali/engineering/project_import/execution_gate.py: assert_execution_approved composes control.policy.workflow_approval.WorkflowApprovalGate unmodified, binding the 'revision' it compares to TierAssignment.assignment_ref instead of a workflow-graph revision. is_enforced_approval refuses an automated actor (the governed stable_mutation.prohibited_actors list), a non-APPROVED decision, and an approval bound to a stale hash. The isolation check (ARK-REQ-0348 condition 7) is evaluated first and can never be bypassed by an approval-only path. No file under backend/arkali/control/ is modified.",
      "evidence": "human: backend/tests/engineering/test_import_execution_gate.py::TestApprovalMustBeGenuinelyHuman (an automated actor 'ai_agent' cannot approve; a REJECTED decision never permits; an approval bound to a stale tier-assignment hash never permits) and TestIsolationMustBeSatisfiable::test_satisfied_isolation_and_approval_together_permit (a genuine 'human_reviewer' actor with a correctly-bound APPROVED decision is honoured). Executed by phase_19_report.json.",
      "note": "The register's human evidence key is supplied. Owner is control.policy per the register; satisfied by composition of the already-accepted Phase 17 authority, not by new control.policy code, recorded in docs/contracts/import.md section 5."
    },
    {
      "req_id": "ARK-REQ-0161",
      "state": "SATISFIED",
      "implementation": "backend/arkali/engineering/project_import/tier_authority.py: assign_tier(inspection) takes no tier parameter - MS section Trust-Tiered Isolation names TRUST-3 for imported/untrusted projects unconditionally, and no code path in this package can produce any other value. backend/arkali/engineering/project_import/contracts.py: TierAssignment.tier and .assigned_by are each a single-member Literal type (Literal[\"TRUST-3\"], Literal[\"engineering.import.tier_authority\"]) - reassignment by an implementing actor is a pydantic.ValidationError, not a runtime check. inspection_ref binds the assignment to the exact StaticInspectionReport.source_address it was made against; ImportedProjectDescriptor refuses construction on a mismatch.",
      "evidence": "sec: backend/tests/engineering/test_import_contracts.py::TestTierAssignmentIsFixedByType (a different tier cannot be constructed; implementing_actor cannot become the assigner) and TestImportedProjectDescriptorBindsTierToItsInspection::test_a_tier_assigned_against_a_different_inspection_is_refused. prop: test_default_tier_is_trust_3, test_assignment_ref_is_a_canonical_address. integ: test_phase_19_journey.py TestAllSevenAcceptanceConditions::test_condition_2_tier_assigned_and_not_reassignable. Executed by phase_19_report.json.",
      "note": "Both register evidence keys, sec and prop, are supplied. docs/contracts/import.md section 3."
    },
    {
      "req_id": "ARK-REQ-0162",
      "state": "SATISFIED",
      "implementation": "backend/arkali/engineering/project_import/rescue_vocabulary.py: RescueModeVocabulary.load parses 'Rescue modes: Repair in Place, Controlled Modernization, Clean Rebuild with Migration.' from the live Master Specification section Import / Rescue / Research / Plugins at call time - the same specification-referenced-set idiom engineering.codeintel.GraphVocabulary established. The number three is asserted nowhere in shipping source. pipeline.py: ImportPipeline.run(rescue_mode=...) resolves the caller's spelling via require_mode and records it on the resulting descriptor; the pipeline does not execute a rescue strategy, only selects and records one (docs/contracts/import.md section 4 and 9 deliberate boundary).",
      "evidence": "integ: backend/tests/engineering/test_import_pipeline.py::TestAllThreeRescueModesAreSupported::test_each_canonical_mode_reaches_working_copy_created, parametrised over all three canonical spellings, each genuinely reaching WORKING_COPY_CREATED with a materialised isolated working copy. backend/tests/engineering/test_import_contracts.py::TestRescueModeVocabularyIsParsedNotHardcoded proves the set and count are parsed, not hard-coded (behavioural control feeding a four-mode declaration). Executed by phase_19_report.json.",
      "note": "The register's integ evidence key is supplied. docs/contracts/import.md section 4."
    },
    {
      "req_id": "ARK-REQ-0348",
      "state": "SATISFIED",
      "implementation": "All seven VDC section Import/Reverse Engineering/Rescue conditions, proven against the real composed ImportPipeline (backend/arkali/engineering/project_import/pipeline.py) and the real ImportProject state machine (import_project_state_machine.py, which gained exactly one additive guard, execution_approval_guard, on APPROVED_FOR_EXECUTION -> WORKING_COPY_CREATED; STATE_MACHINES.md's reconciled count stays 12). Condition 1 (static inspection before execution): structural machine topology plus static_inspection_guard. Condition 2 (correct, non-reassignable tier): see ARK-REQ-0161. Condition 3 (source preserved, independently restorable): WorkspaceAuthority.allocate only ever reads source_root; workspace.snapshot is byte-identical. Condition 4 (modifications confined to the working copy): further writes are confined to workspace.root, disjoint from source_root. Condition 5 (provenance traceable): TierAssignment.inspection_ref == StaticInspectionReport.source_address; ImportedProjectDescriptor.descriptor_ref content-addresses the bound record. Condition 6 (rescue cannot overwrite the source): proven directly by hashing source_root before/after a run. Condition 7 (execution DENY where isolation is unsatisfiable): execution_gate.assert_execution_approved checks isolation before approval and raises RescueBoundaryViolationError; proven both with a real, un-doubled host probe (honestly DENY on this unconfigured host) and with composition-root test doubles proving the ALLOW path exists.",
      "evidence": "sec + integ: backend/tests/engineering/test_phase_19_journey.py::TestAllSevenAcceptanceConditions, one test per condition (test_condition_1 .. test_condition_7), plus backend/tests/engineering/test_import_pipeline.py::TestSourcePreservationAndProvenance and TestUnsatisfiableIsolationRefusesTheWorkingCopy, and backend/tests/engineering/test_import_execution_gate.py for the isolation/approval composition proof independent of the pipeline. Executed by phase_19_report.json.",
      "note": "Both register evidence keys, sec and integ, are supplied. docs/contracts/import.md is the derived C-29 contract document naming all seven conditions and their proofs explicitly."
    }
  ]
}
