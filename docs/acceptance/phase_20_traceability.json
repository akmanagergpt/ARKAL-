{
  "phase_id": "20",
  "note": "Derived from RequirementRegister.for_phase(\"20\"): exactly ARK-REQ-0151, ARK-REQ-0152, ARK-REQ-0336 and ARK-REQ-0337, all MANDATORY. ARK-REQ-0151 and ARK-REQ-0336 are owned by lifecycle.recovery; ARK-REQ-0152 by control.policy, satisfied by exercising the already-accepted Phase 4 PDP mechanism plus Phase 19's WorkflowApprovalGate, both reused unmodified; ARK-REQ-0337 by acceptance.engine. This completes C-03's deferred runtime half (docs/contracts/persistence.md explicitly deferred HUMAN_GATE_6 and the nine-step sequence to this phase) rather than minting a new contract id. Matrix Gate column HUMAN_GATE_6 (\"APPLY of a migration to real or stable data\") maps to the whole phase in GovernanceState.phase_gates (matrix-cell derivation, the same mechanism Phase 19's HUMAN_GATE_4 used) and is NOT recorded ACCEPTED in docs/acceptance/HUMAN_GATE_RECORDS.md as of this submission - the verdict is therefore expected to be AWAITING_HUMAN_GATE, not PHASE_ACCEPTED_BY_MACHINE, regardless of every other check's result. This is a genuine, mechanically-derived stop condition, not a defect in this candidate.",
  "claims": [
    {
      "req_id": "ARK-REQ-0151",
      "state": "SATISFIED",
      "implementation": "backend/arkali/lifecycle/recovery/migration_safety.py (MigrationSafetySequence), migration_safety_steps.py (the nine step implementations) and migration_safety_types.py (shapes, split under the module logical-line budget). The nine VDC \"Migration safety\" steps - Impact, Backup, Dry Run, Integrity, Candidate Migration, Application Tests, Apply, Verify, Rollback Point - run in fixed order, composing Phase 5's RecoveryService/BackupRestore machine, kernel.persistence.migrations (chain reading + the new run_upgrade mechanic) and kernel.persistence.migration_impact (the new AST-based destructive-operation analyser) unmodified. No thirteenth canonical state machine is declared; STATE_MACHINES.md still counts 12.",
      "evidence": "integ: backend/tests/persistence/test_migration_safety.py (11 tests: full happy path, data-loss-risk stop, apply gating, application-tests stop) and backend/tests/persistence/test_phase_20_journey.py::TestTheComposedMigrationSafetyJourney (5 conditions against real SQLite with real seeded ORM data). persist: test_condition_2_real_data_survives_the_forward_migration proves a real project row survives a real forward migration from 0002_project_registry to head. Executed by phase_20_report.json.",
      "note": "Both register evidence keys, integ and persist, are supplied. docs/contracts/migration_safety.md section 1."
    },
    {
      "req_id": "ARK-REQ-0152",
      "state": "SATISFIED",
      "implementation": "The PDP mechanism for HUMAN_GATE_6 was already complete at Phase 4 (pdp.py::_required_gate, generic over any HUMAN_GATE_n fixed rule) and is exercised, not rebuilt, by migration_safety_steps.py::step_apply: APPLY_MIGRATION is requested through the real PolicyEnforcementPoint with targets_real_or_stable_data and recorded_human_gates as facts, and a genuine, non-stale human decision is additionally required through control.policy.WorkflowApprovalGate.is_enforced_approval (Phase 19's authority, reused unmodified, its 'revision' binding repurposed to the resolved target migration revision). No file under backend/arkali/control/ is touched by this phase.",
      "evidence": "human: backend/tests/persistence/test_migration_safety.py::TestApplyIsGatedByPolicyAndApproval (automated actor refused, rejected decision refused, stale revision-bound approval refused) and TestHumanGate6OnRealOrStableData (real-or-stable-data apply blocked without a recorded HUMAN_GATE_6, permitted once one is recorded through the HumanGateSource Protocol; dev-data apply needs no recorded gate at all). backend/tests/persistence/test_phase_20_journey.py::test_condition_3 and test_condition_4 repeat both outcomes against the full composed sequence. Executed by phase_20_report.json.",
      "note": "The register's human evidence key is supplied. Owner is control.policy per the register; satisfied by exercising the already-accepted Phase 4/19 authorities, not by new control.policy code, recorded in docs/contracts/migration_safety.md section 2."
    },
    {
      "req_id": "ARK-REQ-0336",
      "state": "SATISFIED",
      "implementation": "The same MigrationSafetySequence as ARK-REQ-0151: every step produces a MigrationStepResult (step name, PASS/FAIL/BLOCKED, summary, detail), and MigrationSafetyResult.render() gives a complete, ordered record of one run including the steps an earlier stop left BLOCKED - the record is never truncated. The Rollback Point step proves the pre-migration backup restorable via a real RecoveryService.prove_by_restore call and records the proven backup id.",
      "evidence": "integ: backend/tests/persistence/test_migration_safety.py::TestTheFullSequenceOnDevData (every one of the nine steps recorded PASS in canonical order) and TestKnownDataLossRiskBlocksTheWholeSequence (a synthetic destructive chain stops at Impact with every remaining step recorded BLOCKED, not silently omitted). persist: backend/tests/persistence/test_phase_20_journey.py::test_condition_5_the_rollback_point_is_proven_by_a_real_restore. Executed by phase_20_report.json.",
      "note": "Both register evidence keys, integ and persist, are supplied. docs/contracts/migration_safety.md section 1."
    },
    {
      "req_id": "ARK-REQ-0337",
      "state": "SATISFIED",
      "implementation": "backend/arkali/kernel/persistence/migration_impact.py: analyze_file/analyze_chain, an AST-based static analyser over a migration's upgrade() body flagging op.drop_table, op.drop_column and a raw destructive op.execute. Two independent enforcement points reuse it: migration_safety_steps.py::step_impact stops one candidate run at Impact when a pending revision is flagged, and acceptance.engine.checker.PhaseGateChecker.check_migration_data_loss_risk (delegating to the new private migration_release_check.py, split out under the touched-context budget) re-derives the same analysis over the live shipping backend/alembic/versions/ chain on every phase-gate evaluation, blocking acceptance if any revision is ever flagged - never read from a phase report's own claim.",
      "evidence": "prop: backend/tests/persistence/test_migration_impact.py (14 tests: additive operations never flagged, drop_table/drop_column/raw DROP and TRUNCATE all flagged, multi-reason accumulation, downgrade() not analysed, file/chain-level analysis, and the real shipping chain proven to carry zero flagged revisions). backend/tests/persistence/test_phase_20_journey.py::test_step_3_ark_req_0337_over_the_real_shipping_chain and test_step_4_the_phase_gate_checker_carries_the_data_loss_risk_check exercise the acceptance.engine side directly, asserting the analysed-revision count appears in the summary (not only `passed`). backend/tests/acceptance/test_migration_release_check.py (3 tests) pins the BACKEND_RELPATH path-resolution fix: `PhaseGateChecker` is constructed with the outer repository root, not the backend root `VERSIONS_DIR` is relative to, and the check's first draft silently resolved to a nonexistent directory and returned a vacuous PASS - caught by running the real checker end to end against `run_phase_gate.py 20 21` before submission, not by a unit test that made the identical backend-root assumption. Executed by phase_20_report.json.",
      "note": "The register's prop evidence key is supplied. docs/contracts/migration_safety.md section 3."
    }
  ]
}
