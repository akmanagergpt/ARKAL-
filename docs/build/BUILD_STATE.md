# BUILD STATE — ARKALI GENESIS v2

**Current state:** **PHASE 26 (Release / Supply Chain / Deployment) MACHINE-ACCEPTED, no human gate required. PHASE 27 (Command Center Consolidation + Skill Modes) unlocked, not started.**

**Phase 26 is accepted.** `python scripts/run_phase_gate.py 26 27` returned **`PHASE_ACCEPTED_BY_MACHINE`, progression PERMITTED**, on the **first** submission (`docs/acceptance/phase_26_report.json`, `phase_26_traceability.json`). C1–C6 PASS; EXTERNAL_RESULT PASS over all 10 recorded runs; **PROTECTED_CORE COMPLETE** (`lifecycle.release` touched — `control.policy` itself untouched this phase; security review 284, adversarial review 56, full regression 3332, all exit 0); RESCORING NOT_APPLICABLE (first submission); DATA_LOSS_RISK PASS; FINDINGS PASS; PREREQ 3/3 (Phases 6, 13, 22B, all MACHINE-ACCEPTED); HUMAN_GATE NOT_APPLICABLE (Matrix Gate column is `—`); all 8 architecture gates PASS over **222** edges (up from 219) with all 9 numeric budgets. Six atomic packages deliver C-31 under `lifecycle.release`: `ReleaseAuthority`, the first real production caller of the already-declared `Release` machine, composing the unmodified `StableRevisionPointer` read-only and refusing to declare a candidate before any Stable Core revision exists (Package 1, `ARK-REQ-0007` partial); real C-14 provenance registration through an `ArtifactRegistrar` structural Protocol, every VDC §Provenance field populated and re-verified after write (Package 2, `ARK-REQ-0015`/`0350` partial); a real, deterministic SBOM parsed from this repository's own `backend/pyproject.toml`/`frontend/package.json`, no network call (Package 3, `ARK-REQ-0124` partial, `ARK-REQ-0369`); a real, local, deterministic suspicious-package review — unpinned dependencies, documented typosquat names, name-confusable pairs (Package 4, `ARK-REQ-0125`); a dedicated supply-chain negative/adversarial suite naming artifact tampering, digest mismatch, stale/forged candidates, wrong provenance, dependency substitution, release replay, secret leakage, direct Stable mutation, wrong target/environment, unsigned/unverified package, rollback to an unverified revision, and package/version identity collision (Package 5); the final manifest composition (real parent edges to provenance/SBOM/review) plus the real `HUMAN_GATE_7` singleton wiring closing "bypassed acceptance"/"bypassed release policy" (Package 6, completing `ARK-REQ-0007`); and the composed VDC journey — a real Stable Core promotion, a real release candidate reaching `SIGNED_READY`, `RELEASED` genuinely refused without a real Gate 7 grant and genuinely reachable once one exists in a temporary copy — `docs/contracts/release_manifest.md`, the C-17 report and traceability record (Package 7, candidate frozen). A genuine mid-Package-6 canonical-discovery correction is recorded in full in that package's own commit and the contract document §5: an early `release_gate.py` draft built a scoped `(gate, operation_class, target, revision)` grant lookup mirroring `HUMAN_GATE_2`/`3`'s own mechanism, discarded before shipping once `CLAUDE_ARKALI_GENESIS_V2_BUILD_PROTOCOL.md`'s own Canonical HUMAN GATES list and `acceptance.human_gate_authorization.SINGLETON_GATES` were read closely enough to show Gate 7 ("Final Production Release") is textually the same one-time, unscoped shape as Gate 1, not a repeatable category — the real mechanism composes `GovernanceState.accepted_human_gates` unmodified. Two real `max_fan_in_per_module` violations, one real `max_orchestration_depth` violation and one real `max_parameters_per_public_function` violation were found by running the gate and repaired by decomposition/Protocol-decoupling/parameter-grouping, never exception. **6/6 requirements discharged**, 5 MANDATORY, 1 CONDITIONAL (genuinely applicable and PASS on this real repository) — `ARK-REQ-0007`, `0015`, `0124`, `0125`, `0350`, `0369` — taking cumulative verified 209 → **215**. No real deployment (proven structurally: no release function accepts a deployment-target parameter); no real code signing (VDC's own wording is "signing-ready", not "signed"); no live external SBOM/vulnerability-database query; no live goal-to-product pipeline exists (DEF-009, permanently open, out of this phase's canonical scope); `HUMAN_GATE_7` has never been recorded in this real repository, so `RELEASED` remains genuinely unreachable in production today — the honest, expected state for a canonical singleton gate describing "Final Production Release". **Phase 27 unlocked.**

**Phase 24 is accepted.** `python scripts/run_phase_gate.py 24 25` returned **`PHASE_ACCEPTED_BY_MACHINE`, progression PERMITTED**, following `HGR-006` (`docs/acceptance/HUMAN_GATE_RECORDS.md`) — the human acceptance authority granted `HUMAN_GATE_3` for this exact candidate (candidate freeze commit `42390ee`, evidence-package digest `sha256:ecf43065...286c06dc1`) after independent mechanical re-verification of HEAD/branch/clean-tree, `check_handoff.py` PASS, `current_work_phase()` == `24`, the candidate's evidence byte-identical to `42390ee`, the recomputed digest, all 8 architecture gates (0 violations), and zero open BLOCKER/HIGH findings — all confirmed immediately before the grant was recorded. C1–C6 PASS; EXTERNAL_RESULT PASS over all 10 recorded runs; **PROTECTED_CORE normal profile** (no protected-core member touched — `lifecycle.evolution` is not Protected Core); RESCORING NOT_APPLICABLE (first submission); DATA_LOSS_RISK PASS; FINDINGS PASS; PREREQ 2/2 (Phases 13, 16, both MACHINE-ACCEPTED); **HUMAN_GATE: `HUMAN_GATE_3` has a recorded acceptance scoped to this exact evidence package** (`HGR-006`/`HGR-006-SCOPED`, bound to phase 24 and the digest above only — the record's scope section states plainly this does not authorize any other phase, any modified candidate, or any real future `PROMOTE_CHILD_PRODUCT` runtime operation, which requires its own separate `RUNTIME_OPERATION`-scope grant); all 8 architecture gates PASS over **202** edges with all 9 numeric budgets (unchanged since the candidate froze). Seven atomic packages deliver C-36 under `lifecycle.evolution`: child-product identity and the three canonical modes, `uses_evolution_sdk` the single eligibility gate (Package 1, `ARK-REQ-0131`); per-product version lineage (Package 2, `ARK-REQ-0132`/`0358`); working-copy isolation reusing Phase 12's `WorkspaceAuthority` (Package 3, `ARK-REQ-0132`/`0358`); scoped `HUMAN_GATE_3` promotion reusing the `HUMAN_GATE_2`/`CORE_PROMOTION` grant mechanism unmodified, plus ADR-0010 (Package 4, `ARK-REQ-0132`/`0358`); a separate rollback authority requiring no fresh grant (Package 5, `ARK-REQ-0132`/`0358`); structural negative/adversarial proofs of no direct mutation and no self-approval (Package 6); and the composed real-authority journey — real filesystem, real SQLite, real scoped grants, two full evolution cycles, a real `campaign_id` collision defect found and fixed — `docs/contracts/product_sdk.md`, the C-17 report and traceability record (Package 7, candidate frozen). Two real architecture-budget violations were found by running the gate and repaired by decomposition/privatization, not exception, across the seven packages. **4/4 requirements discharged**, all MANDATORY — `ARK-REQ-0131`, `0132`, `0133`, `0358` — taking cumulative verified 197 → **201**. No live goal-to-child-product generation pipeline exists (tracked separately and permanently as DEF-009); no AI-provider-driven multi-file product generation (Phase 30's own obligation); no production surface exposes any Phase 24 module; no change to ARKALI's own `StableRevisionPointer` or `RecoverySupervisor`. **Phase 25 unlocked.**

**Phase 23 is accepted.** `python scripts/run_phase_gate.py 24 25` returned **`AWAITING_HUMAN_GATE`**, progression STOPPED, failing condition "HUMAN_GATE_3 required and not recorded for this exact evidence package" (digest `sha256:ecf43065fcdedb13fceece933145c58085363cda775b67e41873f8e286c06dc1`) — the **only** failing condition; C1–C6 PASS; EXTERNAL_RESULT PASS over all 10 recorded runs; PROTECTED_CORE normal profile (no protected-core member touched — `lifecycle.evolution` is not Protected Core, confirmed directly against `AUTHORITY_MAP.yaml`); RESCORING NOT_APPLICABLE (first submission); DATA_LOSS_RISK PASS; FINDINGS PASS; PREREQ 2/2 (Phases 13, 16, both MACHINE-ACCEPTED); all 8 architecture gates PASS over **202** edges with all 9 numeric budgets (unchanged from Phase 23's own baseline — this candidate added zero new cross-context edges). Seven atomic packages deliver C-36 under `lifecycle.evolution`, reusing every Protected Core authority they touch unmodified: child-product identity and the three canonical modes (Standard, AI-Assisted, AI-Native Self-Evolving), with `uses_evolution_sdk` the single eligibility gate (Package 1, `ARK-REQ-0131`); per-product version lineage, content-addressed and append-only (Package 2, `ARK-REQ-0132`/`0358`); working-copy isolation reusing Phase 12's `WorkspaceAuthority` (Package 3, `ARK-REQ-0132`/`0358`); scoped `HUMAN_GATE_3` promotion, reusing the `HUMAN_GATE_2`/`CORE_PROMOTION` grant mechanism unmodified, plus ADR-0010 (the default-approval-gated policy this phase's own acceptance now requires) (Package 4, `ARK-REQ-0132`/`0358`); a separate rollback authority requiring no fresh grant (Package 5, `ARK-REQ-0132`/`0358`); structural AST proof of no direct Stable/live mutation and no self-approval (Package 6, restating `ARK-REQ-0134`'s adversarial shape for `HUMAN_GATE_3`); and the composed real-authority journey — real filesystem, real SQLite, real scoped grants, two full evolution cycles, a real `campaign_id` collision defect found and fixed before the candidate froze — `docs/contracts/product_sdk.md`, the C-17 report and traceability record (Package 7, candidate frozen). Two real architecture-budget violations (fan-in, public surface) were found by running the gate mid-candidate and repaired by decomposition/privatization, not exception — see the live architecture summary for detail. **4/4 requirements claimed SATISFIED with named evidence, none discharged** — discharge happens only at phase acceptance, which this gate run did not reach. Cumulative verified stays **197**. `HUMAN_GATE_RECORDS.md`'s own "Outstanding gates" table lists `HUMAN_GATE_3` "not reached" — verified mechanically before any Phase 24 code was written, not discovered afterward. **This is a genuine stop condition; no implementing actor may self-grant a human gate.** No live goal-to-child-product generation pipeline exists or is claimed (tracked separately and permanently as DEF-009, out of this phase's canonical scope); no AI-provider-driven multi-file product generation (Phase 30's own obligation); no production surface exposes this SDK anywhere; no change to ARKALI's own `StableRevisionPointer` or `RecoverySupervisor`. **Phase 25 was not started.**

**Phase 23 is accepted.** `python scripts/run_phase_gate.py 23 24` returned **`PHASE_ACCEPTED_BY_MACHINE`, progression PERMITTED**, following `HGR-005` (`docs/acceptance/HUMAN_GATE_RECORDS.md`) — the human acceptance authority granted `HUMAN_GATE_2` for this exact candidate (candidate freeze commit `54f9e8b`, evidence-package digest `sha256:3ad6f5fe...1eb5c00e`) after independent mechanical re-verification of HEAD/branch/clean-tree, `check_handoff.py` PASS, `current_work_phase()` == `23`, the candidate's evidence byte-identical to `54f9e8b`, the recomputed digest, all 8 architecture gates (0 violations), and zero open BLOCKER/HIGH findings — all confirmed immediately before the grant was recorded. C1–C6 PASS; EXTERNAL_RESULT PASS over all 9 recorded runs; **PROTECTED_CORE COMPLETE** (`acceptance.engine` touched via `campaign_record_shape.py`; security review 254, adversarial review 807, full regression 3078, all exit 0); RESCORING NOT_APPLICABLE (first submission); DATA_LOSS_RISK PASS; FINDINGS PASS; PREREQ 3/3 (Phases 13, 16, 22B, all MACHINE-ACCEPTED); **HUMAN_GATE: `HUMAN_GATE_2` has a recorded acceptance scoped to this exact evidence package** (`HGR-005`/`HGR-005-SCOPED`, bound to phase 23 and the digest above only — the record's scope section states plainly this does not authorize any other phase, any modified candidate, or any real future `CORE_PROMOTION` runtime operation, which requires its own separate `RUNTIME_OPERATION`-scope grant); all 8 architecture gates PASS over **202** edges with all 9 numeric budgets. Eight atomic packages deliver C-33 under `lifecycle.evolution`/`lifecycle.recovery`/`control.policy`/`acceptance.engine`: `CampaignBudgets`/`CampaignDeclaration` wired to the pre-existing `evolution_campaign_state_machine.declaration_guard` (Package 1, `ARK-REQ-0139`); `CampaignLedger`'s successor-eligibility and honest terminal-state derivation, mirroring `RepairBudgetLedger`'s structural shape (Package 2, `ARK-REQ-0140`/`0141`/`0142`); `lifecycle.recovery/core_snapshot.py::take_core_snapshot`, a real restorable-snapshot proof distinct from `RecoveryService` and `RecoverySupervisor`, feeding the pre-existing `core_upgrade_state_machine`'s entry (Package 3, `ARK-REQ-0137`); a real scoped `HUMAN_GATE_2` grant lookup (`HumanGate2Source` Protocol over `acceptance.engine.GovernanceState.operation_grant`) feeding the pre-existing `gate_2_guard` (Package 4, `ARK-REQ-0138`); composed promotion sequencing that guard with the real, unmodified `StableRevisionPointer.promote` — a real ordering defect (a stale receipt could leave the state machine claiming `PROMOTED` while Stable was never written) found by testing and repaired (Package 5, `ARK-REQ-0134`'s pipeline half); structural AST proof that no `lifecycle.evolution` module but `core_promotion.py` can reach a Stable mutation, plus the self-approval boundary composed end-to-end against a real temporary `HUMAN_GATE_RECORDS.md` (Package 6, `ARK-REQ-0134`'s live-core half, `ARK-REQ-0359`); `_check_campaign_record_shape`, a private acceptance-engine check (kept private: the context's public-surface budget was already at its ceiling and no live caller exists yet) refusing an unbounded campaign record (Package 7, `ARK-REQ-0360`); and the composed VDC "ARKALI Self-Evolution" journey (real prior rollback verifies the Recovery Supervisor, a real snapshot precedes a real candidate build, promotion refused without a grant and succeeds with one, a second campaign reaches `COMPLETE_WITH_NO_FURTHER_MEASURED_GAIN` having never touched Stable), `docs/contracts/campaign.md`, the C-17 report and traceability record (Package 8, candidate frozen). Three real architecture-budget violations were found by running the gate and repaired by decomposition (ADR-0008), not exception, across the eight packages. **9/9 requirements discharged**, all MANDATORY — `ARK-REQ-0134`, `0137`–`0142`, `0359`, `0360` — taking cumulative verified 188 → **197**. No live self-evolution campaign is run against ARKALI's own source; no Product Evolution SDK, Golden Factory or Golden Repair capability is built; no production surface exposes any Phase 23 module. **Phase 24 unlocked.**

**Phase 22B is accepted.** `python scripts/run_phase_gate.py 22B 23` returned **`PHASE_ACCEPTED_BY_MACHINE`, progression PERMITTED**, on the first and only submission (`docs/acceptance/phase_22B_report.json`, `phase_22B_traceability.json`). C1–C6 PASS; EXTERNAL_RESULT PASS over all 10 recorded runs; **PROTECTED_CORE COMPLETE** (three Protected Core contexts touched — `control.policy`, `lifecycle.release`, `lifecycle.recovery`; security review 253, adversarial review 801, full regression 3000, all exit 0); RESCORING NOT_APPLICABLE (first submission); DATA_LOSS_RISK PASS (9 declared revisions, zero flagged); FINDINGS PASS; PREREQ 4/4 (Phases 5, 6, 13, 20, all MACHINE-ACCEPTED); HUMAN_GATE NOT_APPLICABLE (Matrix Gate column empty for Phase 22B); all 8 architecture gates PASS over **197** edges with all 9 numeric budgets. Five atomic packages deliver: the minimal Stable-revision pointer primitive (D-017, `lifecycle.release`) whose durable history — built only from genuine `StableCandidatePath.promotion_receipt` proofs — is the sole record of a "previously verified immutable revision" (`ARK-REQ-0157`); the PDP's real conditional `ROLLBACK_STABLE` grant, `AUTO` to exactly the canonical invoker with a truthful verified-immutable fact, `DENY` otherwise (`control.policy`, `ARK-REQ-0155`/`0156`); the independent, deterministic Recovery Supervisor — the sole production caller of that grant, importing no AI provider, no candidate-generation authority and no repair pipeline, every rollback an identity-only pointer switch always evidenced through the real C-14/C-15 plane (`lifecycle.recovery`, `ARK-REQ-0154`/`0159`/`0160`); a real, measured `max_orchestration_depth` (5 of 4) and `max_fan_in_per_module`/`max_contexts_touched_by_module` violation found by running the architecture gate and repaired via two ADR-0008 structural Protocols (`RollbackAuthorization`, `EvidenceSink`) rather than requested as an exception; and the composed VDC "Recovery Supervisor" journey (bad candidate → launch → health failure → known-good rollback → failure record → stable available) proving the end-to-end evidenced rollback (`ARK-REQ-0338`/`0339`) and Self-Evolution's pre-existing `core_upgrade_state_machine.recovery_supervisor_guard` now backed by a real, PDP-derived, non-fabricable `RecoverySupervisor.is_verified()` fact (`ARK-REQ-0135`/`0136`) — structurally proven the sole production module able to produce it. **11/11 requirements discharged**, all MANDATORY — `ARK-REQ-0135`, `0136`, `0154`–`0160`, `0338`, `0339` — taking cumulative verified 177 → **188** and MANDATORY verified 175 → **186** (Phase 22B's own denominator was already all-MANDATORY). No Self-Evolution capability is built (Phase 23's own obligation); no full release/supply-chain/deployment system is built (Phase 26, D-017 explicit). **Phase 23 unlocked.**

**Phase 20 is accepted.** `python scripts/run_phase_gate.py 20 21` returned **`PHASE_ACCEPTED_BY_MACHINE`, progression PERMITTED**, following `HGR-003` (`docs/acceptance/HUMAN_GATE_RECORDS.md`) — the human acceptance authority granted `HUMAN_GATE_6` for this exact candidate (evidence commit `73a968f`, evidence-package digest `sha256:b94d28bb...202c2c`) after independent mechanical re-verification of HEAD/branch/clean-tree, `check_handoff.py` PASS, `current_work_phase()` == `20`, the candidate's evidence byte-identical to `73a968f`, the recomputed digest, the complete governance suite (399 passed, 0 failed), all 8 architecture gates (0 violations), and zero open BLOCKER/HIGH findings — all confirmed immediately before the grant was recorded. C1–C6 PASS; EXTERNAL_RESULT PASS over all 11 recorded runs; **PROTECTED_CORE COMPLETE** (`acceptance.engine` is touched by the new ARK-REQ-0337 check; security review 245, adversarial review 802, full regression 2762, all exit 0); RESCORING NOT_APPLICABLE (first submission); DATA_LOSS_RISK PASS (9 declared revisions, zero flagged); FINDINGS PASS; PREREQ 2/2 (Phases 5, 6 both MACHINE-ACCEPTED); **HUMAN_GATE: `HUMAN_GATE_6` has a recorded acceptance scoped to this exact evidence package** (`HGR-003`/`HGR-003-SCOPED`, bound to phase 20 and the digest above only — the record's scope section states plainly this does not authorize any other phase, any modified candidate, or any real `APPLY_MIGRATION` runtime operation, which requires its own separate `RUNTIME_OPERATION`-scope grant); all 8 architecture gates PASS over **183** edges with all 9 numeric budgets. Three atomic packages deliver the nine-step migration safety sequence under `lifecycle.recovery`, completing C-03's deferred runtime half (details below). **4/4 requirements discharged**, all MANDATORY — `ARK-REQ-0151`, `0152`, `0336`, `0337` — taking cumulative verified 166 → **170** and MANDATORY verified 165 → **169**.

**Three atomic packages deliver the nine-step VDC "Migration safety" sequence, completing C-03's deferred runtime half.** Package 1 (`kernel.persistence`): `run_upgrade` (the real `command.upgrade` mechanic, promoted out of a test-local duplicate) and `migration_impact.py` (an AST-based analyser flagging `op.drop_table`/`op.drop_column`/a raw destructive `op.execute` in a revision's `upgrade()` body, never executing the file it inspects) — the real declared chain (9 revisions) proven to carry zero known data-loss risk. Package 2 (`lifecycle.recovery`): `MigrationSafetySequence` (split across `migration_safety.py`/`_steps.py`/`_types.py` under the module logical-line budget) composes Phase 5's `RecoveryService`/`BackupRestore` machine, Package 1's mechanics, Phase 4's PDP/PEP and Phase 19's `WorkflowApprovalGate` — all reused unmodified — into Impact → Backup → Dry Run → Integrity → Candidate Migration → Application Tests → Apply → Verify → Rollback Point; a data-loss risk detected at Impact stops the whole sequence, every later step recorded `BLOCKED` rather than silently skipped; Apply requires both a real, non-stale human decision (`WorkflowApprovalGate`, its "revision" binding repurposed to the target migration revision) and, for real-or-stable-data targets, a recorded `HUMAN_GATE_6` read through a `HumanGateSource` structural `Protocol` — never a direct `acceptance.engine` import, since that edge would extend the already-4-of-4 orchestration-depth chain to 5. No thirteenth canonical state machine is declared; `STATE_MACHINES.md` stays at 12. Package 3 (`acceptance.engine`): `check_migration_data_loss_risk`, re-derived over the live shipping chain on every gate evaluation via `migration_release_check.py` — split out under the touched-context budget, alongside `governance_gates.py` absorbing `check_prerequisites`/`check_human_gate`'s bodies after `checker.py` hit its 400-logical-line ceiling; both new functions are named with a leading underscore, so `acceptance.engine`'s public surface (already 40 of 40) gains nothing. A real defect was caught and fixed pre-submission: the check's first draft resolved the migrations path against the repo root `PhaseGateChecker` holds, not the backend root it is actually relative to, silently finding nothing and reporting a vacuous PASS — caught by running the real gate end to end, fixed with `BACKEND_RELPATH`, pinned by three new tests including a negative control reproducing the original mistake. The composed journey (`test_phase_20_journey.py`, one test per VDC condition, real seeded ORM data surviving a real forward migration to head), `docs/contracts/migration_safety.md`, the traceability record and the C-17 report complete the candidate. Architecture is re-derived at **183** cross-context edges (14 new, from six new modules); `lifecycle.recovery` public surface rises 11 → 28 of 40, `kernel.persistence` 32 → 38 of 40, `acceptance.engine` stays exactly 40 of 40, `control.policy.pep` fan-in reaches 15 of 15 (its ceiling, not exceeded). No PostgreSQL migration evidence is claimed (no driver installed on this host, verified directly); no Stable-revision rollback is claimed (that stays Phase 22B). No AI provider was contacted anywhere in this phase. **Phase 21 unlocked.**

**Phase 19 is accepted.** `python scripts/run_phase_gate.py 19 20` returned **`PHASE_ACCEPTED_BY_MACHINE`, progression PERMITTED**, following `HGR-002` (`docs/acceptance/HUMAN_GATE_RECORDS.md`) — the human acceptance authority granted `HUMAN_GATE_4` for this exact candidate (HEAD `ef5cb3b`, evidence-package digest `sha256:6e41dd0d...`) after independent mechanical re-verification of all twelve stated conditions, none altered from the original submission. C1–C6 PASS; EXTERNAL_RESULT PASS over all 11 recorded runs; **PROTECTED_CORE PASS** — no Protected Core member was touched (confirmed by `git diff --stat` over `backend/arkali/control/` and `backend/arkali/acceptance/`, both empty across the whole candidate), so the normal profile applies; RESCORING NOT_APPLICABLE (first submission); FINDINGS PASS; PREREQ 3/3 (Phases 3, 4, 12); **HUMAN_GATE: `HUMAN_GATE_4` has a recorded acceptance** (`HGR-002`, scoped to this candidate only — see the record's own explicit known-gap note before assuming this covers Phase 21's own future `HUMAN_GATE_4` obligation); all 8 architecture gates PASS over **169** edges with all 9 budgets. **5/5 requirements discharged**, all MANDATORY — `ARK-REQ-0115`, `0116`, `0161`, `0162`, `0348` — taking cumulative verified 161 → **166** and MANDATORY verified 160 → **165**.

**Five atomic packages deliver the C-29 contract under `engineering.import`.** Package 1: `ImportedProjectDescriptor`/`TierAssignment`/`StaticInspectionReport` (`contracts.py`), each frozen and content-addressed, `INT` category matching the C-11/C-13/C-23/C-28 precedent; `TierAssignment.tier`/`.assigned_by` and `StaticInspectionReport.executed` are single-member `Literal` types — a type-level guarantee, not a runtime check — and `RescueModeVocabulary` (`rescue_vocabulary.py`) parses "Repair in Place, Controlled Modernization, Clean Rebuild with Migration" from the live Master Specification at call time (`ARK-REQ-0162`). Package 2: `StaticInspector` (`static_inspection.py`) reuses `engineering.codeintel.PythonGraphBuilder` unmodified — AST-only, never importing or executing the target project; proven with a landmine fixture whose side effect is never triggered (`ARK-REQ-0115`, `ARK-REQ-0161`). Package 3: `tier_authority.assign_tier` (no tier parameter — the whole proof a tier cannot be reassigned) and `execution_gate.assert_execution_approved`, which composes `control.isolation.IsolationAuthority.resolve` and `control.policy.workflow_approval.WorkflowApprovalGate` **unmodified** — no file under `backend/arkali/control/` is touched, so `ARK-REQ-0116` (owned by `control.policy`) is satisfied without the Protected Core stronger profile; `import_project_state_machine.py` gains its third additive guard, `execution_approval_guard` (`STATE_MACHINES.md`'s count stays 12). Package 4: `ImportPipeline` (`pipeline.py`) composes Packages 1–3 into the real `ImportProject` state-machine instance and materialises a genuinely isolated working copy via `engineering.candidate.workspace.WorkspaceAuthority`, reused through a structural `Protocol` rather than a direct import — `engineering.candidate`'s own chain into `kernel.contracts` was already measured at 4 of 4 (the orchestration-depth ceiling) as of Phase 16, and a direct import would have extended it to 5, the identical shape Phase 16's `product_generation.py` answered the same way. No `engineering.import -> engineering.candidate` sibling edge is declared. Package 5: the composed journey (`test_phase_19_journey.py`, one test per VDC acceptance condition), the structural authority proof (`test_import_authority.py`), `docs/contracts/import.md`, the traceability record and the C-17 report. **On this real, unconfigured host TRUST-3 is honestly `DENY`** (`KERNEL_ISOLATION` UNSUPPORTED, matching the existing TRUST-2/3/4 UNSUPPORTED record); the ALLOW path is proven only with composition-root test doubles, the identical shape Phase 9B used for its own activation mechanism. Architecture is re-derived at **169** cross-context edges (unchanged since the candidate's own last production commit; the acceptance/handoff commits added no production code); `engineering.import` public surface is approximately 18 of 40. No repair/modernisation/rebuild execution engine, HTTP surface, Research capability or TRUST-4 support is delivered — each is a recorded deliberate boundary in `docs/contracts/import.md` §9. No AI provider was contacted anywhere in this phase. **Phase 20 unlocked** (Database Migration Safety + Full Backup/Recovery, 4 requirements, prerequisites Phases 5 and 6 both already MACHINE-ACCEPTED, matrix Gate column `GATE 6` on APPLY — not started, not implemented, read-only discovery only if any).

**Five atomic packages deliver the C-29 contract under `engineering.import`.** Package 1: `ImportedProjectDescriptor`/`TierAssignment`/`StaticInspectionReport` (`contracts.py`), each frozen and content-addressed, `INT` category matching the C-11/C-13/C-23/C-28 precedent; `TierAssignment.tier`/`.assigned_by` and `StaticInspectionReport.executed` are single-member `Literal` types — a type-level guarantee, not a runtime check — and `RescueModeVocabulary` (`rescue_vocabulary.py`) parses "Repair in Place, Controlled Modernization, Clean Rebuild with Migration" from the live Master Specification at call time (`ARK-REQ-0162`). Package 2: `StaticInspector` (`static_inspection.py`) reuses `engineering.codeintel.PythonGraphBuilder` unmodified (one declared sibling edge) — AST-only, never importing or executing the target project; proven with a landmine fixture whose side effect is never triggered (`ARK-REQ-0115`, `ARK-REQ-0161`). Package 3: `tier_authority.assign_tier` (no tier parameter — the whole proof a tier cannot be reassigned) and `execution_gate.assert_execution_approved`, which composes `control.isolation.IsolationAuthority.resolve` and `control.policy.workflow_approval.WorkflowApprovalGate` **unmodified** — no file under `backend/arkali/control/` is touched, so `ARK-REQ-0116` (owned by `control.policy`) is satisfied without the Protected Core stronger profile; `import_project_state_machine.py` gains its third additive guard, `execution_approval_guard` (`STATE_MACHINES.md`'s count stays 12). Package 4: `ImportPipeline` (`pipeline.py`) composes Packages 1–3 into the real `ImportProject` state-machine instance and materialises a genuinely isolated working copy via `engineering.candidate.workspace.WorkspaceAuthority`, reused through a structural `Protocol` rather than a direct import — `engineering.candidate`'s own chain into `kernel.contracts` was already measured at 4 of 4 (the orchestration-depth ceiling) as of Phase 16, and a direct import would have extended it to 5, the identical shape Phase 16's `product_generation.py` answered the same way. No `engineering.import -> engineering.candidate` sibling edge is declared. Package 5: the composed journey (`test_phase_19_journey.py`, one test per VDC acceptance condition), the structural authority proof (`test_import_authority.py`), `docs/contracts/import.md`, the traceability record and the C-17 report. **On this real, unconfigured host TRUST-3 is honestly `DENY`** (`KERNEL_ISOLATION` UNSUPPORTED, matching the existing TRUST-2/3/4 UNSUPPORTED record); the ALLOW path is proven only with composition-root test doubles, the identical shape Phase 9B used for its own activation mechanism. Architecture is re-derived at **169** cross-context edges (ten new: `content_address` ×2, `contract_violation_base` ×1, `error_base` ×1, `engineering.codeintel` ×2, `control.isolation` ×2, `control.policy` ×2); `engineering.import` public surface is approximately 18 of 40. No repair/modernisation/rebuild execution engine, HTTP surface, Research capability or TRUST-4 support is delivered — each is a recorded deliberate boundary in `docs/contracts/import.md` §9. No AI provider was contacted anywhere in this phase.

**Phase 18 is accepted.** `python scripts/run_phase_gate.py 18 19` returned **`PHASE_ACCEPTED_BY_MACHINE`, progression PERMITTED, exit 0, on the first submission.** C1–C6 PASS; EXTERNAL_RESULT PASS over all 10 recorded runs; **PROTECTED_CORE PASS** — no Protected Core member was touched (`engineering.knowledge` is not Protected Core), so the normal profile applies; RESCORING NOT_APPLICABLE (no prior acceptance record); FINDINGS PASS; PREREQ 1/1 (Phase 13); HUMAN_GATE NOT_APPLICABLE; all 8 architecture gates PASS over 159 edges with all 9 budgets. **4/4 requirements discharged**, all MANDATORY — `ARK-REQ-0126`, `0127`, `0128`, `0394` — taking cumulative verified 157 → **161** and MANDATORY verified 156 → **160**.

**Four atomic packages deliver the C-28 contract under `engineering.knowledge`.** Package 1: `EvidenceReference`/`SelfReportedClaim` (the structural, type-level exclusion of a model's self-reported claim from anywhere evidence is required) and `KnowledgeRecord` with its derived five-state validity lifecycle (`fresh`/`aging`/`revalidation_required`/`deprecated`/`invalid`) — `ARK-REQ-0126`, `ARK-REQ-0127`. Package 2: `ReusableComponentDescriptor` requiring mandatory test/security/compatibility metadata, with `is_verified_reusable` a narrower honest derivation over that carried metadata — `ARK-REQ-0128`. Package 3: `VerifiedOutcome`/`aggregate` for per-model, per-task-class empirical statistics, reusing the identical `EvidenceReference` exclusion so a self-reported claim cannot enter the aggregate — `ARK-REQ-0394` (D-026). Package 4: the composed Phase 18 journey (`test_phase_18_journey.py`), `docs/contracts/knowledge.md`, the structural authority proof that `STATE_MACHINES.md`'s reconciled count stays 12 and no forbidden authority (persistence, PDP, `engineering.repair`/`.factory`) is imported, plus the traceability record, C-17 report and the gate run. C-28 is `INT`: no table, no migration, no ORM record, matching the C-11/C-13 precedent. No new `allowed_sibling_edges` entry was required — evidence composes generically through `EvidenceReference` rather than importing `engineering.repair`'s concrete types. Architecture is re-derived at **159** cross-context edges (two new kernel-contract imports: `content_address` ×2, `contract_violation_base` ×1); `engineering.knowledge` public surface is **16 of 40**. `execution_routing.py`'s `VERIFIED_KNOWLEDGE` tier is deliberately left wired to `NOT_CONFIGURED` — rewiring it is a future caller's composition, per that module's own documented deferral, and no requirement in this phase's denominator requires it. **No AI provider was contacted anywhere in this phase. Phase 19 unlocked.**

**Phase 17 is accepted.** `python scripts/run_phase_gate.py 17 18` returned **`PHASE_ACCEPTED_BY_MACHINE`, progression PERMITTED, exit 0, on the first fully-corrected submission.** C1–C6 PASS; EXTERNAL_RESULT PASS over all 12 recorded runs; **PROTECTED_CORE COMPLETE** (member `control.policy`, touched by `workflow_approval.py`/`policy_errors.py` — security review 245, adversarial review 788, full regression 2610, all exit 0); RESCORING NOT_APPLICABLE; FINDINGS PASS; PREREQ 3/3 (Phases 7, 8, 13); HUMAN_GATE NOT_APPLICABLE; all 8 architecture gates PASS over 156 edges with all 9 budgets. Eight atomic packages delivered C-20 (the canonical workflow graph document): the graph domain model, the persistence/revision store, the ADR-0004 derived execution plan, `control.policy`'s HUMAN APPROVAL enforced policy stop, the executor (real evidence for all 10 node kinds and 10 control constructs), the `surfaces.command` HTTP API, the frontend Visual Workflow Studio (a hand-rolled canvas — real pointer drag, real click-to-connect, no graph-drawing library dependency — proven by a real T10 Playwright journey through compose/publish/reload persistence/fresh-context persistence/execution pause/the ARK-REQ-0330 negative path (`ARK-ERR-0133`)/a real approval to `SUCCEEDED`), and the ARK-REQ-0331 coverage check (a structural cross-check rather than a new production module, since `execution.workflow`, `surfaces.command` and `acceptance.engine` were all at their 40-of-40 public-surface ceiling). **9/9 requirements discharged**, all MANDATORY — `ARK-REQ-0062`…`0065`, `0328`…`0332` — taking cumulative verified 148 → **157**.

**Phase 16 is accepted.** `/home/lenovo/.venvs/arkali/bin/python scripts/run_phase_gate.py 16 17` returned **`PHASE_ACCEPTED_BY_MACHINE`, progression PERMITTED, exit 0, on the first and only submission.** C1–C6 PASS; EXTERNAL_RESULT PASS over all 11 recorded runs; **PROTECTED_CORE PASS** — no Protected Core member was touched; RESCORING NOT_APPLICABLE; FINDINGS PASS; PREREQ 3/3 (Phases 12, 14, 15); HUMAN_GATE NOT_APPLICABLE; all 8 architecture gates PASS over 143 edges with all 9 budgets. **3/3 requirements discharged**, all MANDATORY — `ARK-REQ-0233`, `0392`, `0393` — taking cumulative verified 145 → **148** and MANDATORY verified 144 → **147**.

**Two atomic packages, composing existing authorities rather than building a new one.** `execution_routing.py` (`ARK-REQ-0392`/`0393`) evaluates D-026's exact eight-tier preference order — deterministic tool, verified knowledge, local model, stronger local reviewer, cloud provider, stronger cloud specialist, multi-reviewer, human governance, read fresh from `DECISION_LOG.md` — composing the real `CapabilityGraph.can_perform` answer and the real C-26 `RepairBudgetLedger` for anti-loop bounding (a new declared sibling edge, `engineering.factory → engineering.repair`, was required and added with written rationale). Tiers whose owning authority is unbuilt (Phase 18's `engineering.knowledge`, Phase 22's `engineering.localai`) or has no live runtime instance (no configured provider exists anywhere in this repository) honestly resolve `NOT_CONFIGURED` by construction — the same shape `CapabilityGraph.can_perform` already uses pre-activation, never a stub. `TierSelection` carries an `ExecutionTier`, never a boolean, so an exhausted or unconfigured tier cannot be misread as success. `product_generation.py` (`ARK-REQ-0233`) deterministically scaffolds a real, importable, executable Python module from a fully-resolved C-37 blueprint — every function either a real derived constraint check (a captured numeric acceptance criterion translated into an executable comparison, proven by importing the generated module and calling it) or an honest acknowledgement, never an unimplemented-marker standing in for undelivered logic — and refuses any blueprint carrying an unresolved question. BP's "generate progressively harder real products through the normal pipeline" is read as textually distinct from "Golden Factory Acceptance requires at least one real provider or real working local model" in the same sentence; only the second names a provider requirement, and it remains Phase 30's unclaimed obligation (`ARK-REQ-0234`/`0235`). A real orchestration-depth violation was found and fixed by decomposition: `engineering.candidate`'s own chain into `kernel.contracts` was already 4 of 4, so `product_generation.py` composes a structural `Protocol` matching `CandidateWorkspace.write` instead of importing the concrete class, never extending the measured chain. No AI provider is contacted anywhere in this phase, no execution engine exists (matching the Phase 10/14 precedent), and D-026's remaining phase-owned requirements (`ARK-REQ-0394` at Phase 18, `ARK-REQ-0395` at Phase 25) remain correctly unimplemented here. **Phase 17 unlocked.**

**Phase 15 is accepted.** `/home/lenovo/.venvs/arkali/bin/python scripts/run_phase_gate.py 15 16` returned **`PHASE_ACCEPTED_BY_MACHINE`, progression PERMITTED, exit 0, on the first and only submission.** C1–C6 PASS; EXTERNAL_RESULT PASS over all 11 recorded runs; **PROTECTED_CORE PASS** — no Protected Core member was touched; RESCORING NOT_APPLICABLE; FINDINGS PASS; PREREQ 1/1 (Phase 13); HUMAN_GATE NOT_APPLICABLE; all 8 architecture gates PASS over 138 edges with all 9 budgets. **11/11 requirements discharged**, all MANDATORY — `ARK-REQ-0381`…`0391` — taking cumulative verified 134 → **145** and MANDATORY verified 133 → **144**.

**One atomic package, plus two human governance rulings.** Read-only research found Phase 15 to be a genuine canonical gap — zero requirements, zero contract, zero bounded context, zero MS elaboration — reported rather than guessed at, the same discipline Phase 14 Package 3 used for `CANONICAL_AMBIGUITY`. **D-025** (`docs/build/DECISION_LOG.md`) then ruled: Phase 15 SHALL provide a deterministic, evidence-backed capability converting a human product goal into a provenance-tracked, ambiguity-honest requirement/architecture blueprint, extending `control.specification` (never a second architecture authority) and introducing exactly one new contract, `C-37`, since no existing one could carry a product blueprint without conflating it with `C-04`'s canonical governance register. Eleven new canonical requirements (`ARK-REQ-0381`–`0391`, Register Block 8) gave Phase 15 a real denominator. **D-026**, recorded alongside it, establishes ARKALI's permanent adaptive execution-routing policy and maps each of its responsibilities to its correctly-owning future phase (16, 18, 25) rather than building any of it here — four further requirements (`ARK-REQ-0392`–`0395`) were added at those phases, with no runtime routing behaviour implemented under this ruling. The one Phase 15 package delivers `blueprint_engine.py`: deterministic goal decomposition, content-addressed provenance, ambiguity/contradiction/underspecification/missing-acceptance-criteria detection recorded as explicit unresolved questions rather than invented semantics, closed-vocabulary classification, and architecture/capability mapping resolved live from `AuthorityMap.concerns` — composing `control.architecture` and `control.capability`, never duplicating them. A real same-layer architecture violation (both contexts are layer `control`) was measured and answered with a declared sibling edge, not an exception; a real cyclomatic-complexity budget violation was measured and answered by decomposition. No product code is generated and no acceptance verdict is rendered — both are structurally proven, not merely asserted. **Phase 16 unlocked.**

**Phase 14 is accepted.** `/home/lenovo/.venvs/arkali/bin/python scripts/run_phase_gate.py 14 15` returned **`PHASE_ACCEPTED_BY_MACHINE`, progression PERMITTED, exit 0, on the first and only submission.** C1–C6 PASS; EXTERNAL_RESULT PASS over all 11 recorded runs, each declaring `NO_EXTERNAL_RESULT` explicitly; **PROTECTED_CORE PASS** — no Protected Core member was touched by any of the five Phase 14 packages, so the normal profile applies; RESCORING NOT_APPLICABLE (no prior acceptance record); FINDINGS PASS; PREREQ 1/1 (Phase 13); HUMAN_GATE NOT_APPLICABLE (matrix Gate column `—`); all 8 architecture gates PASS over 135 edges with all 9 budgets. **7/7 requirements discharged**, all MANDATORY — `ARK-REQ-0029`, `0086`, `0087`, `0088`, `0238`, `0239`, `0240` — taking cumulative verified 127 → **134** and MANDATORY verified 126 → **133**.

**Five atomic packages, plus one human governance ruling.** Package 1 delivered the C-26 evidence contract (`RepairFingerprint`, `RepairBudgetLedger`); Package 2 the anti-loop repeated-strategy refusal; Package 3 the mechanical proof that MS's 9-stage Root-Cause sequence and BP's 11-stage Failure Protocol could not be reconciled without governance authority (`CANONICAL_AMBIGUITY`); Package 4 `ARK-REQ-0240`'s deterministic-transformer candidate-lifecycle confinement, delegated entirely to the existing Phase 10 `control.policy.agent_authority.AgentAuthority`; **D-024** (`docs/build/DECISION_LOG.md`) then resolved the Package 3 ambiguity prospectively — BP's 11-stage sequence governs the executable Phase 14 pipeline, MS's 9-stage sequence remains the conceptual description and was never implemented as a competing state machine (`STATE_MACHINES.md` still declares exactly 12) — and Package 5 delivered the executable `RepairPipelinePath`, composed with the unchanged Packages 1–4 mechanisms in a real-authority journey. Package 3's original mechanical proof of the ambiguity is preserved unamended; the ruling resolves it prospectively rather than retroactively inventing a derivation neither document ever stated. No deterministic-transformer execution engine exists — Phase 14 delivers the evidence contract, the anti-loop refusal, the governed pipeline and its confinement authority, not an engine that reads a real defect and writes a real fix. Golden Repair remains Phase 30. **Phase 15 unlocked.**

**Phase 14 Package 4 (`ARK-REQ-0240` — deterministic-transformer confinement, owned by `control.policy`).** BP §Deterministic repair: "Transformers execute inside the candidate lifecycle only; a transformer may never write to a Stable Core or Stable Product revision." `AUTHORITY_MAP.yaml` `stable_mutation.prohibited_actors` already named `deterministic_transformer` — for MS §Constitution 6, before Phase 14 existed — and `direct_mutation_permitted_by` is empty by design, so confinement to candidate and refusal of a direct Stable write are the same rule. `control.policy.agent_authority.AgentAuthority` (Phase 10, Protected Core, accepted) already exposes `assert_may_directly_mutate_stable(actor)`, generic over the actor label. `engineering.repair`'s new `DeterministicTransformer` (identity only — name, version) delegates its confinement check to that same authority via `assert_confined_to_candidate_lifecycle`, exactly as `AgentRole.assert_may_accept` delegates for `ARK-REQ-0051`. No second copy of `prohibited_actors`/`direct_mutation_permitted_by`/`required_path` exists in `engineering.repair`; no lifecycle, release, Stable or acceptance import or operation is introduced there. **Not blocked by `CANONICAL_AMBIGUITY`**: `ARK-REQ-0240` has one canonical source and does not depend on the MS/BP pipeline reconciling. No transformer execution engine exists — this is the confinement authority a future one must consult. `docs/contracts/repair.md` records the addition. 11 new structural/property tests prove: the actor label is genuinely canonical, the delegation actually refuses (negative control), and `engineering.repair` acquired no further authority.

**Phase 14 Package 3 (C-26 failure-protocol stage vocabulary — proves, does not resolve, CANONICAL_AMBIGUITY).** `ARK-REQ-0086` cites `MS §Root-Cause and Convergence Engine` (9 stages); `ARK-REQ-0238` cites `BP §Failure protocol` (11 stages). `failure_protocol.py` parses both at call time and attempts the identical reconciliation algorithm `harness_elements.py` established for this exact shape of problem (Phase 10): stage counts must agree, and each position must be a word-wise abbreviation match. **They do not reconcile** — proven by `test_failure_protocol.py::TestTheRealDocumentsGenuinelyDoNotReconcile`, which asserts the live documents raise `AuthoritativeSourceError` naming both counts (9 against 11). `Reproduce`, `Hypotheses`, `Experiment`, `Root Cause` and `Regression` are identical in both; `Observe`/`Evidence` (MS) against `Evidence`/`Classify` (BP) share no word root at either position, and BP names two explicit stages (`Candidate`, `Accept/Reject`) MS's text never names at all. `docs/contracts/repair.md` records the exact conflicting clauses and the minimal governance decision required: either rule which document's stage vocabulary governs the operational pipeline, or amend one canonical document so the texts reconcile. **No mapping was invented** — doing so would be the alias-table defect (F-0013 wearing a different hat) `harness_elements.py`'s own docstring warns against. This is `CANONICAL_AMBIGUITY`, the same terminal state `scripts/automation/arkali_automation.py`'s `STOP_PATTERN` already names. **`ARK-REQ-0086` and `ARK-REQ-0238` cannot be discharged, and no pipeline orchestrator can be built against a single derived stage list, until a human ruling resolves this** — matching the D-023 precedent (Phase 9 Package 3 stopped at Stage 1 and reported an ambiguity rather than guessing).

**Phase 14 Package 2 (C-26 anti-loop refusal).** `RepairBudgetLedger.record` now refuses an attempt whose (failure signature, root-cause class, strategy) repeats one already recorded in the same ledger, raising `RepeatedFailedStrategyError` before the budget math runs. The identity is structural, not a classification of the free-text `outcome` field: a ledger is scoped to one candidate's convergence attempt on one defect, so a second fingerprint sharing that triple is, by construction, a repeat of a strategy that did not resolve the defect the first time — there is no canonical pass/fail vocabulary for a single attempt's `outcome` to classify (unlike the campaign-level `ESCALATED`/`BLOCKED` machines `STATE_MACHINES.md` defines elsewhere, which stay Phase 30's Golden Repair, not Phase 14's). `files`, `provider/model` and `outcome` do not participate in the repeat identity. This is `ARK-REQ-0087` and `ARK-REQ-0239`'s "repeated failed strategy escalates instead of looping", and is exactly the verification responsibility `CONTRACT_INVENTORY.md` row C-26 names: "anti-loop property tests". `docs/contracts/repair.md` records the addition.

**Phase 14 Package 1 (C-26 repair fingerprint + budget ledger contract).** `engineering.repair` now holds the executable C-26 contract: `RepairFingerprint`, the immutable six-field identity of one repair attempt (failure signature, root-cause class, files, strategy, provider/model, outcome), content-addressed and with canonicalised, duplicate-refusing file sets; and `RepairBudgetLedger`, which accounts for all six canonically required dimensions — attempts, AI calls, elapsed seconds, cost, touched files, regression delta — against declared ceilings, returning a new immutable ledger per recorded attempt and refusing any candidate that would cross a ceiling. Attempt accounting is reconciled against the recorded fingerprint count, so an unaccounted repair cannot pass as evidence.

**A real architecture budget was hit and answered by decomposition, not exception — twice, because the first repair's own claim was wrong.** Adding `engineering/repair/errors.py` made `engineering.repair` the sixteenth direct importer of `kernel.contracts.error_base`, taking its fan-in to 16 against the budget of 15. `ContractViolation` and the root `ArkaliError` were split into `contract_violation_base.py` and `error_root.py` respectively. **Correction:** the PRE-PHASE-14 GOVERNANCE REPAIR commit's own message claimed this left `error_base.py` at fan-in 11, but only `engineering/repair/errors.py` was actually redirected to the new module — the four other `ContractViolation`-only importers (`control/registry/provider/errors.py`, `engineering/agent/errors.py`, `engineering/candidate/errors.py`, `engineering/codeintel/errors.py`) were left pointed at `error_base.py`, so the true fan-in after that commit was 15, not 11 — still exactly at the ceiling. This surfaced during Phase 14 Package 3 pre-work, when adding a fifth new importer (`failure_protocol.py`, needing `AuthoritativeSourceError`) pushed it back to 16. The four omitted redirects were completed at that point: `error_base.py` now genuinely sits at fan-in **12 of 15**, `contract_violation_base.py` at **6**, `error_root.py` at **2**. No GATE 8 exception requested at either step.

**Packages 1–4 together provide the C-26 evidence contract, its anti-loop refusal, the proof that the root-cause pipeline vocabulary is currently unreconcilable, and `ARK-REQ-0240`'s deterministic-transformer confinement authority.** They do not run the root-cause pipeline (`ARK-REQ-0086`, `ARK-REQ-0238` — blocked on CANONICAL_AMBIGUITY above), alter a candidate, choose which strategy to attempt next, execute a deterministic transformer against real content, discharge any of Phase 14's 7 requirements, or run the Phase 14 gate. Cumulative verified stays **127**; Phase 14 is IN PROGRESS, NOT ACCEPTED.

**Phase 13 is accepted.** `/home/lenovo/.venvs/arkali/bin/python scripts/run_phase_gate.py 13 14` returned **`PHASE_ACCEPTED_BY_MACHINE`, progression PERMITTED, exit 0, on the first and only submission.** C1–C6 PASS; EXTERNAL_RESULT PASS over all 13 recorded runs; **PROTECTED_CORE COMPLETE** — the whole six-package candidate changed `acceptance.engine` and `control.policy`, and security review, adversarial review and full regression are all satisfied; RESCORING NOT_APPLICABLE; FINDINGS PASS; PREREQ 2/2 (Phases 6 and 12); HUMAN_GATE NOT_APPLICABLE; all 8 architecture gates PASS over 131 edges with all 9 budgets. **27/27 requirements discharged**, all MANDATORY, taking cumulative verified 100 → **127** and MANDATORY verified 99 → **126**. Phase 14 (Repair / Root Cause / Convergence, C-26) is unlocked.

**Phase 13 Package 6 (composition and acceptance evidence).** The composed journey proves the intact C-15 → C-16 graph → register-derived coverage → Passport → capability-verdict path against real repository authorities. The traceability record contains the exact 27-row Phase 13 denominator, and the frozen C-17 report records every required Protected Core category. No release, Stable-promotion or human-gate authority is added. Do not re-run the Phase 13 gate.

**Phase 12 is accepted.** `/home/lenovo/.venvs/arkali/bin/python scripts/run_phase_gate.py 12 13` returned **`PHASE_ACCEPTED_BY_MACHINE`, progression PERMITTED, exit 0, on the first submission.** C1–C6 PASS; **EXTERNAL_RESULT PASS** over all 13 recorded runs; **PROTECTED_CORE COMPLETE** — the whole six-package candidate changed `control.policy` and `lifecycle.release`, and the security review, adversarial review and full regression categories are all satisfied; RESCORING NOT_APPLICABLE; FINDINGS PASS; PREREQ 1/1 (Phase 11); HUMAN_GATE NOT_APPLICABLE; all 8 architecture gates PASS over 125 edges with all 9 budgets. **7/7 requirements discharged** — `ARK-REQ-0005`, `0023`, `0024`, `0025`, `0056`, `0058`, `0212`, all MANDATORY — taking cumulative verified 93 → **100** and MANDATORY verified 92 → **99**. The dependency machine now unlocks **Phase 13** (Acceptance Infrastructure + Evidence Graph, C-16 and the Acceptance Engine), whose prerequisites are Phases 6 and 12.

**Phase 12 Package 6 (acceptance evidence and the single gate submission).** The frozen C-17 report records 13 exact-candidate runs, including all three Protected Core evidence categories, and the acceptance guard proves that empty claims against a nonzero denominator and false discharge records are refused. The one permitted Phase 12 gate submission accepted all seven register-derived claims. No provider was contacted and no external-provider result exists. Do not re-run the Phase 12 gate.

**Phase 13 Package 2 (register-derived applicability and coverage).** The
read-only coverage projection loads the complete denominator, classifications,
required evidence keys and conditional rules from `RequirementRegister` on
every call. MANDATORY entries remain applicable; objectively false CONDITIONAL
entries leave the denominator; missing state or an unevaluable rule remains
APPLICABLE; and an unregistered applicability claim fails. Coverage is derived
per immutable C-16 path, so shared Contract or Result nodes cannot leak a PASS
between requirements. Only a reachable PASS counts, and an evidence key counts
only when that exact register key is recorded as the Test node of a passing
path. Package 2 records no Passport and issues no acceptance or release verdict.

**Phase 13 Package 3 (Proof-of-Engineering Passport accounting).** The Passport
parses its applicable nineteen-kind vocabulary from the frozen Verification and
Delivery Contract and takes every concrete evidence obligation from the
Canonical Requirement Register. It groups applicable requirements by the
register's owning component, preserves the exact requirement, contract,
artifact, test, content-addressed evidence-record and result identities from
each passing C-16 path, and reports missing obligations explicitly. Completion
is evidence-complete requirement coverage against the applicable register
denominator, never a count of files. This package records nothing and issues no
Acceptance Engine, phase, release or human-gate verdict.

**Phase 13 Package 4 (`control.policy` acceptance boundaries).** The policy
authority derives every automated actor from
`stable_mutation.prohibited_actors`, every human gate from `human_gates`, and
the exceptional applicability-waiver gate from that gate's canonical meaning.
Automated actors may evaluate register-owned applicability but cannot author,
amend, reinterpret, classify or originate it; they cannot use a recorded human
gate as a waiver token, waive a MANDATORY requirement or weaken acceptance to
obtain PASS. A machine result cannot substitute for any required HUMAN GATE.
The requirement classification remains an input from its owning authority and
is not copied into policy. This package issues no Acceptance Engine verdict.

**Phase 13 Package 5 (Acceptance Engine capability verdict).** The verdict is
derived solely from the immutable Passport accounting. At least one passing,
fully bound C-16 path can establish IMPLEMENTED, but only complete evidence for
every applicable register-owned obligation establishes VERIFIED. Missing
evidence remains explicit by requirement and evidence key. Every declared
required HUMAN GATE is checked by calling Package 4's policy authority and
cannot be replaced by the machine result. The verdict creates no evidence and
grants no release, Stable-mutation, promotion, phase-acceptance or human-gate
authority.

**Phase 11 is accepted.** `python scripts/run_phase_gate.py 11 12` returned **`PHASE_ACCEPTED_BY_MACHINE`, progression PERMITTED, exit 0, on the first submission.** C1–C6 PASS; **EXTERNAL_RESULT PASS** over all 12 recorded runs; **PROTECTED_CORE PASS** — no Protected Core member was touched, so the normal profile applies; RESCORING NOT_APPLICABLE; FINDINGS PASS; PREREQ 1/1 (Phase 10); HUMAN_GATE NOT_APPLICABLE; all 8 architecture gates PASS over 118 edges with all 9 budgets. **2/2 requirements discharged** — `ARK-REQ-0066` and `ARK-REQ-0067`, both MANDATORY — taking cumulative verified 91 → **93**. The dependency machine now unlocks **Phase 12** (Candidate Workspace + Semantic Assembly, C-25), whose sole prerequisite is Phase 11.

**Phase 11 Package 4 (composition and acceptance candidate).** The composed journey loads the graph and view vocabularies from repository authority, builds nonempty symbol/import/dependency graphs from the real `engineering.codeintel` Python source, rebuilds all three with a fresh builder and requires identical canonical addresses, and supplies those addresses to the Digital Twin's code view. Every view without a Phase 11 adapter is explicitly absent rather than fabricated as an empty graph. The two-claim traceability record is derived from `RequirementRegister.for_phase("11")`; C-24's derived contract document and the C-17 report are present. The phase gate has not yet been run.

**Phase 11 Package 3 (the Digital Twin composing every specified view).** `DigitalTwin.compose` obtains the complete view set from `GraphVocabulary.view_ids()` and returns contributions in that canonical order; neither the set nor its count is copied into shipping source. A behavioural control composes in-memory vocabularies of three different sizes, so the count follows repository authority rather than an implementation constant.

**Composition is complete without pretending every source is available.** Every specified view must appear exactly once. A contribution is either backed by one or more canonical content addresses or explicitly declares why its owning source is absent; neither an empty contribution nor one claiming both material and absence can be constructed. Missing, duplicate and unknown views are refused with their distinct C-24 taxonomy, and a derived view cannot exist without naming the source a reader must consult. The twin itself always refuses an authoritative question.

**No requirement is discharged.** Phase 11 owns two and each is discharged only at phase acceptance; cumulative verified stays **91**. Package 4 still owes the composed journey, traceability, report and the single permitted gate run. All 8 gates PASS over **118** cross-context edges with all 9 budgets and no violation.

**Phase 11 Package 2 (the graph set, and C-24's rebuild determinism).** `CONTRACT_INVENTORY.md` row 24 names **rebuild determinism** as C-24's evidence responsibility, so it is not a quality hoped for but the contract's declared obligation. `CodeGraph.address` is computed over a fully normalised rendering with `kernel.contracts.content_address` — the same canonical addressing C-14 and C-23 use — and `assert_rebuild_matches` turns the check into a **refusal** rather than a comparison a caller might skip.

**Determinism is achieved by normalisation, not by asking callers to be tidy.** Nodes and edges live in sets and render sorted, so insertion order, duplicate insertion and dictionary iteration order cannot reach the address; controls prove each of those independently. The kind is part of what a graph *is*, so two kinds carrying identical facts address differently, and edge direction is part of the identity. Determinism is **not** achieved by ignoring the input: a control changes a real file and requires the address to move.

**The store is derived and never an authority.** `ARCHITECTURE.md` §11 says so directly, and `resolve_authority` **always** refuses — there is no branch in which it answers, for any concern. Every node and edge records the source it was observed in, so a reader can go back to the owning authority rather than believe the derived copy; a node without a source cannot be constructed.

**`PythonGraphBuilder` builds three of the six kinds, and says so.** Symbol, import and dependency are derivable from Python source alone. `route` and `model` need framework semantics rather than syntax, and `frontend-contract` needs TypeScript, which §11 assigns to a Tree-sitter adapter this build does not have. Those three are **not stubbed, not defaulted and not returned empty**: `builds()` reports exactly what it produces and asking for an unbuilt kind is a **refusal**, because an empty graph is indistinguishable from "nothing found" and would report coverage the builder does not have. Files are walked in sorted order, so determinism cannot depend on filesystem enumeration. Nothing is imported or executed — an unimportable module still contributes the symbols it syntactically declares.

**No requirement is discharged.** Phase 11 owns two and each is discharged only at phase acceptance; cumulative verified stays **91**. **No Digital Twin exists** — composing the nine views is Package 3. All 8 gates PASS over **117** cross-context edges with all 9 budgets and no violation.

**Phase 11 Package 1 (the canonical graph set and Digital Twin view set).** `ARK-REQ-0066` requires Code Intelligence to maintain "the specified graph set" and `ARK-REQ-0067` requires the Digital Twin to compose "the nine specified views". **Both requirements are phrased as references to a specification rather than as lists**, so the lists belong in the specification and nowhere else. `ARCHITECTURE.md` §11 declares both in one paragraph, and `GraphVocabulary` parses them at call time: **6** graph kinds (symbol, import, dependency, route, model, frontend-contract) and **9** twin views (specification, architecture, code, data, runtime, deployment, test, version, failure).

**The number nine is nowhere in the shipping source.** `ARK-REQ-0067` states it, but the count lives in the register's requirement text and the views live in the architecture — so whatever §11 declares is what the twin must compose. A control proves the counts follow the document behaviourally, by parsing declarations of three different sizes.

**The one cross-document check this phase actually owes.** The view *count* lives in the register and the view *list* lives in the architecture, so the two could drift apart with neither document looking wrong on its own. A control reads both and requires them to agree — and takes the number **from the register** rather than writing it into the control, so a canonical set that grows to ten moves the test instead of expiring it.

**The presentation suffix is not part of the identity.** §11 writes `test view` and `code graph` in the same list, so treating those trailing nouns as meaningful would make the vocabulary depend on prose style rather than on what is declared; `slug` strips them. The two vocabularies stay distinct even where they read alike — `deployment` is a view and is refused as a graph kind.

**Fails closed, and anti-vacuity is explicit.** A missing document, an absent section, a paragraph with no graph clause, a paragraph with no view clause, or either list carrying fewer than two entries are each refused, because a specified set with nothing to exclude would pass vacuously. The section is bounded by the next heading, proven by a later section whose sentence must not be read as this declaration.

**No requirement is discharged.** Phase 11 owns two — `ARK-REQ-0066` and `ARK-REQ-0067` — and each is discharged only at phase acceptance; cumulative verified stays **91**. **No graph is built yet**: this package describes the vocabulary and nothing here reads source code, walks an AST or touches a repository other than the canonical document it parses. C-24's declared verification is **rebuild determinism**, which a later package owes. All 8 gates PASS over **116** cross-context edges with all 9 budgets and no violation.

**Phase 10 is accepted.** `python scripts/run_phase_gate.py 10` returned **`PHASE_ACCEPTED_BY_MACHINE`, progression PERMITTED, exit 0, on the first submission.** C1–C6 PASS; **EXTERNAL_RESULT PASS** over all 14 recorded runs; **PROTECTED_CORE COMPLETE** — the profile was derived as `PROTECTED_CORE` over the whole four-package change set (Package 3 changed Protected Core `control.policy`), with all three canonical categories satisfied by real zero-exit executions: security review 205, adversarial review 695, full regression 1764 + 348 + 48; RESCORING NOT_APPLICABLE; FINDINGS PASS; PREREQ 2/2 (Phases 9 and 9B); HUMAN_GATE NOT_APPLICABLE; all 8 architecture gates PASS over 114 edges with all 9 budgets. **5/5 requirements discharged** — `ARK-REQ-0050`, `0051`, `0054`, `0055`, `0231`, all MANDATORY — taking cumulative verified 86 → **91**. The canonical dependency machine now unlocks **Phase 11** (Code Intelligence + Digital Twin, C-24), whose sole prerequisite is Phase 10.

**Phase 10 Package 4 (composition, traceability, the C-17 report and the gate).** Package 4 adds the phase's composed integration evidence and its acceptance record, and **changes no module under `backend/arkali/`** — the contracts it submits are the ones Packages 1–3 built. `backend/tests/engineering/test_phase_10_journey.py` is the composed journey in seven steps: the harness elements come from **both** canonical documents and reconcile positionally; a task omitting **any** element cannot be constructed, parametrised over the model's own field list rather than a written one; the context package admits only canonical kinds and records provenance per item; no raw secret can reach it, through the real C-09 guard; the task and its context bind by canonical content address; a role holds references and never backend state, with the live `shadow_registry` gate run over the real repository; and the role's prohibitions are decided by `control.policy`, not by the role.

**The record is derived, not carried over.** `RequirementRegister.for_phase("10")` returns exactly five entries, and each claim names an implementation and an evidence source using the register's own evidence keys — `contract` for `ARK-REQ-0231`, `contract`/`integ` for `0054`, `prov`/`integ` for `0055`, `arch` for `0050`, `sec`/`prop` for `0051`. `reconcile_discharge` returns **no violations**. Phase 9B's, Phase 9's and Phase 3's requirements are not restated.

**The candidate carries the STRONGER VERIFICATION PROFILE, and it is satisfied.** Package 3 changed `control.policy`, which is Protected Core, so `select_profile` derives `PROTECTED_CORE` over the **whole four-package change set** rather than over Package 4's files alone. All three canonical categories were executed against this candidate at exit 0: **security review 205**, **adversarial review 695**, **full regression 1764 + 348 + 48**. The profile verdict renders COMPLETE.

**The full suite was partitioned, and the partition is proven rather than asserted.** `pytest backend --collect-only` collects **2174** and the three parts collect 1778 + 348 + 48 = **2174** exactly. Nothing is excluded, deselected or reordered; the partition exists only because two drift modules alone take 340s and the suite exceeds this environment's per-invocation execution ceiling.

**Phase 10 Package 3 (`ARK-REQ-0050` and `ARK-REQ-0051`).** `MS §Provider and Agent separation` carries both: "Agents are engineering roles. Providers are execution backends. A Backend Engineer role may use Claude, GPT, Gemini or a local model. Agents operate under bounded task/context/permissions and cannot mutate canonical requirements or declare their own output accepted."

**The separation is structural (`ARK-REQ-0050`, evidence `arch`).** `AgentRole` has no field that could hold provider identity, model identity, configuration, health, availability, cost metadata or fallback, and `extra="forbid"` means one cannot be attached; it carries `provider_refs` and nothing else about a backend. The control that proves this reads the **owned-concern list from `AUTHORITY_MAP.yaml`** rather than naming concerns locally, so a concern the canonical map adds later is checked the day it appears. `engineering.agent` was **already** a declared reference-only consumer with `copying_permitted` and `caching_permitted` both false, so the live `shadow_registry` gate already read this context's source — Package 3 satisfies a gate that exists rather than introducing a second rule about the same thing. One role may declare many backends and one backend may serve many roles, both proven, which is the one-to-many relationship the canonical sentence states.

**The prohibitions live in `control.policy`, not in the agent (`ARK-REQ-0051`, evidence `sec, prop`).** The register assigns this requirement to `control.policy`, and the reason is structural rather than clerical: a rule about what an agent may not do, enforced inside the agent context, is enforced by the actor it constrains. `AgentRole` therefore **delegates** to `AgentAuthority` and a control asserts the agent context holds neither `prohibited_actors` nor `required_path` in its code. `engineering.agent` is rank 4 and `control.policy` is rank 1, so it can call this and can never replace or shadow it.

**Both prohibitions are derived, never written down.** Which actors are barred comes from `AUTHORITY_MAP.yaml` `stable_mutation.prohibited_actors`; a control asserts no actor label appears as a literal in the module, and a canonical set that adds an actor is proven to bar it immediately. Why self-acceptance is refused comes from `stable_mutation.required_path`, which lists candidate production and acceptance as **separate stages** — and that separation is *checked*, not assumed: a canonical set that merged those stages makes the module fail loudly instead of enforcing a rule the documents no longer state. **Self-acceptance is refused for every actor, not only barred ones**, because `direct_mutation_permitted_by` is empty by design and no actor is exempt from a stage boundary; narrowing it would have let an unlisted label accept its own output.

**No operation class was added, and that is deliberate.** `unmapped_action_resolution: DENY` already makes an unmapped action fail closed, and adding a class would change a Protected-Core-owned contract that `CONTRACT_INVENTORY.md` puts behind **HUMAN GATE 2** — a gate this phase has not reached and may not assume. Two controls assert the governed vocabulary is unchanged and that the new module declares no class of its own.

**The `prop` evidence is exhaustive, not sampled.** `hypothesis` is NOT_CONFIGURED on this host and no package was installed to satisfy an evidence key. The governed vocabularies are finite and canonical, so the properties are proven by enumerating them completely — every barred actor, every casing and padding variant, and every barred actor crossed with every operation class. Exhaustion over a closed vocabulary is a stronger result than sampling it.

**No requirement is discharged.** Phase 10 owns five and each is discharged only at phase acceptance; cumulative verified stays **86**. `control.policy` is Protected Core, so this candidate carries the **stronger verification profile**, which `select_profile` derives from the changed paths at acceptance and which Package 4 must satisfy with real security-review, adversarial-review and full-regression execution records. No agent runtime, no provider call, no dispatch and no execution exists. All 8 gates PASS over **114** cross-context edges with all 9 budgets and no violation.

**Phase 10 Package 2 (C-23, the context package and its provenance).** `MS §Context Compiler` is one sentence carrying two clauses, and both are now enforced: only the kinds it enumerates may be sent, and provenance is recorded per item.

**"Only relevant" is a closed vocabulary, not a judgement.** The sentence *enumerates* the admissible kinds — files, symbols, contracts, tests, ADRs, failures and verified knowledge — so relevance is not something this contract decides. `ContextKindAuthority` parses the enumeration at call time, anchored to the canonical verb phrase so ordinary prose in the section cannot be mistaken for it, and the kind list and its count appear nowhere in the shipping source — proven **behaviourally** by feeding the parser vocabularies of three different sizes. An absent section, a section with no enumeration, and a single-kind enumeration are each refused, because a relevance rule with nothing to exclude would pass vacuously.

**Provenance is per item and required.** `origin` has no default and blank is refused, so an item without provenance cannot be constructed; a package-level note would record nothing useful, since the question a reader has is *which* file or failure a claim came from. Items are frozen, because provenance that can be edited afterwards is not provenance.

**The no-secret assertion is C-09's, reused and not reimplemented.** `CONTRACT_INVENTORY.md` row 23 names it as this contract's verification, and it is implemented by calling `control.policy.secret_reference.assert_no_raw_secret` — whose own docstring already names the context package as a sink raw values must never reach. Copying its shape pattern here would create a second authority for what a secret looks like and the two would drift; a control reads this context's source and fails if such a pattern appears. Every string is scanned, derived from the model rather than field by field, and the sink is named in the refusal so a control can assert **which** boundary refused (F-0017).

**`context_hash` is an interlock with accepted work, not a local invention.** The Phase 6 `ArtifactProvenanceRecord` already carries a `context_hash` field, so an artifact's provenance is meant to bind to the exact context its producer saw. It is computed with `kernel.contracts.content_address`, the same canonical addressing C-14 uses. Identical content addresses identically, any change to any item changes it, and **order is part of the context** because a different order is a different context to a model.

**No requirement is discharged.** Phase 10 owns five and each is discharged only at phase acceptance; cumulative verified stays **86**. **No Context Compiler exists** — this is the compiler's output *contract*, and nothing here selects context, reads a repository or ranks relevance. `ARK-REQ-0050` and `ARK-REQ-0051` are not addressed, and `ARK-REQ-0051` is owned by **`control.policy`** and will not be discharged from this context. C-23 is `INT`: no table, no migration, no state machine (count stays **12**). All 8 gates PASS over **113** cross-context edges with all 9 budgets and no violation.

**Phase 10 Package 1 (C-22, the bounded harness task).** `engineering.agent` now holds the primitive `MS §Harness Engineering` names: "The primitive is not 'send a prompt'; the primitive is 'execute a bounded engineering task'." `harness_task.py` is that task and `harness_elements.py` is the authority for what bounds it.

**The eight elements are parsed, never written down.** Both canonical documents declare them — the register sources `ARK-REQ-0054` to `MS §Harness` and `ARK-REQ-0231` to `BP §Harness`, so **neither may be preferred over the other**. They are parsed independently and reconciled **positionally**; a disagreement in count or order fails closed. Where the spellings differ (`Task Spec` against `Task Specification`, `Context` against `Context Package`) each must be a word-wise **abbreviation** of the other — a property that is CHECKED, not assumed — and the unabbreviated form is taken because an abbreviation carries strictly less information. **There is no alias table**, which would be the F-0013 second-copy defect in a different shape. Neither the element list nor the element count appears in the shipping source, and that is proven **behaviourally** — the parser is fed declarations of four different sizes and the reported count must follow the document each time — rather than by a text scan, which would be brittle in both directions.

**Boundedness is structural, not policed.** Every element is a required field with **no default** and `extra="forbid"`, so an unbounded `HarnessTask` cannot be constructed at all; there is no window in which a half-specified task exists. `reconcile_with_authority` closes the rest in both directions: a canonical element with no field would let a task be unbounded in that dimension, and a field with no canonical element would mean the contract invented a boundary the documents do not declare. **Empty is a bound; absent is not** — `tools=()` says the task may use no tools, and omitting `tools` says nothing; that distinction is the whole reason no field carries a default. Zero repairs is a budget. The task is frozen, because bounds that can be widened after admission are not bounds.

**A hard budget was hit and respected, not excepted.** The error taxonomy was first written into `kernel.contracts` and the architecture gate **refused it**: public surface 42 against a `max_public_surface_per_context` of 40. Per ADR-0008 it was decomposed rather than excepted — an exception would need HUMAN GATE 8 plus an ADR and may not be authored by the implementing actor — and `ARK-ERR-0092`–`0094` now live in `engineering.agent/errors.py`, importing the abstract base from `kernel.contracts.error_base` so no edge is added to `errors.py` at its own 15/15 fan-in ceiling. This follows the C-11, C-12, C-14, C-15, C-19 and C-21 taxonomies before it. All 8 gates now PASS over **110** cross-context edges with all 9 budgets and no violation.

**No requirement is discharged.** Phase 10 owns five — `ARK-REQ-0050`, `ARK-REQ-0051`, `ARK-REQ-0054`, `ARK-REQ-0055`, `ARK-REQ-0231` — and each is discharged only at phase acceptance; cumulative verified stays **86**. C-22 is `INT`, so no table and no migration; no state machine was added and the count stays **12**. **C-23 does not exist yet**: C-22 carries a *reference* to a context package, and provenance plus the no-secret assertion are a later package's. **`ARK-REQ-0051` is owned by `control.policy`, not by this context, and is not enforced here** — `acceptance_target` is a field a task carries, and nothing in this package lets an agent decide its own. No agent runtime, no provider call, no dispatch and no execution of any kind exists.

**Phase 9B is accepted.** `python scripts/run_phase_gate.py 9B 10` returned **`PHASE_ACCEPTED_BY_MACHINE`, progression PERMITTED, exit 0, on the first submission.** C1–C6 PASS; **EXTERNAL_RESULT PASS** over all 14 recorded runs; PROTECTED_CORE PASS (no member touched, normal profile); RESCORING NOT_APPLICABLE (no prior acceptance record); FINDINGS PASS; PREREQ 4/4 (Phases 3, 4, 6 and 9); HUMAN_GATE NOT_APPLICABLE; all 8 architecture gates PASS over 108 edges with all 9 budgets evaluated. **3/3 requirements discharged** — `ARK-REQ-0046`, `ARK-REQ-0047`, `ARK-REQ-0048`, all MANDATORY — taking cumulative verified 83 → **86**. The canonical dependency machine now unlocks **Phase 10** (Agent Runtime + Harness Engineering, C-22/C-23), whose prerequisites are exactly Phases 9 and 9B.

**Phase 9B Package 3 (composition, traceability, the C-17 report and the gate).** Package 3 adds the phase's composed integration evidence and its acceptance record, and **changes no module under `backend/arkali/`** — the contract it submits is the one Packages 1 and 2 built. `backend/tests/capability/test_phase_9b_journey.py` is the composed journey: the activation phase read from the register rather than a constant; a pre-activation graph that is handed fully resolving authorities and must still consult none of them; the reference→authority binding parsed from the canonical block with every authority checked against `AUTHORITY_MAP.yaml`; `PASS` reached only after every declared authority was actually asked; determinism plus the no-cache rule proven together; the resolver question shown to be incapable of carrying a value with the live `shadow_registry` gate run over the real repository; transitive prerequisites and a cycle that fails closed; and finally a real `AdmissionService` decision — `ADMITTED` when everything resolves, `CAPABILITY_NOT_CONFIGURED` for every refusal shape.

**The traceability record is derived, not carried over.** `RequirementRegister.for_phase("9B")` returns exactly `ARK-REQ-0046`, `ARK-REQ-0047` and `ARK-REQ-0048`, all MANDATORY, all owned by `control.capability`; Phase 9's three are discharged and are not restated, and Phase 3's `ARK-REQ-0045`/`0049` are likewise absent. Each claim names an implementation and an evidence source, and `reconcile_discharge` returns **no violations** against the register.

**The change set is NORMAL** by `select_profile` over the phase's real created and modified paths, taken from git across all three packages — `control.capability` is not a Protected Core member and no path was unresolvable — so no security-review or adversarial-review category is owed. The battery was run in full regardless: **8 mutations injected, 8 caught**, none missed, each against the real shipping file through the control that is supposed to catch it, with the original bytes restored and the sha256 re-verified between every case. The set covers the pre-activation answer leaking an affirmative, prerequisites left unwalked, a real memoisation cache on resolution, a node's own `configured_state` ignored, a missing authority resolving anyway, a refusal reported as `FAIL` instead of `NOT_CONFIGURED`, the canonical binding going vacuous, and `activate()` becoming a settable operation.

**The honest capability position, stated plainly.** The activation *mechanism* is proven end to end and production admission can now reach `ADMITTED` in principle. Every affirmative proof composes composition-root doubles standing in for `control.registry.provider`, `control.policy` and `control.isolation` — the evidence-requirement authority is the real `RequirementRegister` — because no provider runtime exists and none may be fabricated (D-023). **No production capability has been declared configured with real identities anywhere in this repository**, no provider was contacted, no socket opened, nothing simulated and no external-provider result exists. C-13 is `INT`, so there is no table and no migration; no state machine was added and the canonical count stays **12**.

**Phase 9B Package 2 (the activated deterministic query).** `CapabilityGraph.can_perform` now answers `MS §Capability Graph`'s "Can I perform this?" for real, once activated: it composes Package 1's per-reference resolutions, the node's own `configured_state`, and its graph-owned `prerequisites` (transitively, cycle-checked) into one determinate `CapabilityQueryResult`. `activated_query.py` derives — never asserts — the mapping: an affirmative answer requires every declared reference to have resolved and the node to declare itself `CONFIGURED`; any other outcome is `NOT_CONFIGURED`.

**The negative state is derived from §4's own wording, not chosen.** §4 admits a job "when its capability resolves other than `NOT_CONFIGURED`" — pairing *resolving* with *not being* `NOT_CONFIGURED` — and `docs/contracts/worker.md` §7 glosses the accepted Phase 8 outcome identically. `FAIL` is assigned to no capability query anywhere in the canonical set, and choosing it would have been **unsafe**: the accepted Phase 8 predicate is `state is not NOT_CONFIGURED`, unchanged, so any state besides `NOT_CONFIGURED` reads as condition (a) satisfied. `UNSUPPORTED` is likewise refused — it is canonically another context's verdict (isolation unsatisfiability, or a secret-dependent capability with no key protection), both already produced by their own owners. **No authority conflict was found and no accepted semantics were changed**: the mapping was derived to be correct under the unchanged Phase 8 predicate, proven through the real `AdmissionService` rather than asserted in prose.

**Fields with no canonical evaluation rule are not evaluated.** `runtime_requirements` and `platform_support` carry no declared authority and nothing is invented for either. `fallback_refs` are validated as references and never consulted for the verdict — fallback *selection* has no canonical semantics here, proven by a broken capability that a healthy fallback does not rescue. `isolation_tier` satisfiability stays with `control.isolation` and admission condition (b); a control asserts this context declares no isolation or policy vocabulary.

**Two designed pre-activation tripwires were replaced, not deleted.** `test_capability_preactivation.py::test_resolution_is_not_implemented_in_the_schema_phase` and `test_admission_decision.py::test_the_scheduler_cannot_force_a_configured_verdict` both asserted `PrematureActivation` at the activation phase, because Phase 3/Package 1 had nothing to compose. Package 2 composes a verdict, so each was replaced with a strictly **stronger** obligation — reaching the activation phase with no authority composed must still answer `NOT_CONFIGURED`, never `PASS` — and a new permanent control (`TestRetiredTripwiresStillHaveTheirObligation`) reads both replacements' source and fails if either drops the obligation or is silently narrowed.

**Proven through the real `AdmissionService`, not just the graph.** `test_activated_admission.py` composes the shipping `WorkerContract`, `CapabilityAdmission`, `IsolationAdmission`, `ResourceAdmission`, a real PEP over a real PDP, and the activated graph: a fully resolved configured capability is `ADMITTED`; a missing resolver, a refusing authority, an unresolved reference, an unknown reference, an unconfigured node and an unresolved prerequisite are each `CAPABILITY_NOT_CONFIGURED`; a changed authority answer is observed on the next evaluation; condition (b) and (c) remain distinct and still refuse independently of capability resolution.

**Nothing is cached, at any layer.** Package 1's per-query resolution, the graph's composed verdict, and the admission evaluator all re-ask their authorities on every call; a control changes an authority's answer between two calls through the real service and requires the second answer to move. `shadow_registry` now scans **18** consumer modules, up from 17.

**Activation stays a derived phase fact.** `activate()` still always raises `PrematureActivation`, now at every phase rather than only before the activation phase, because there is no flag anywhere to set — `is_activated` remains derived from `current_phase == activation_phase`, both supplied by the caller.

**No requirement is discharged.** Phase 9B owns three — `ARK-REQ-0046`, `ARK-REQ-0047`, `ARK-REQ-0048` — and each is discharged only at phase acceptance; cumulative verified stays **83**. No table, no migration, no state machine (count stays **12**), no provider contacted, nothing simulated, no external-provider result.

**The Package 2 change set is NORMAL** by `select_profile` from its own paths — `control.capability` and `execution.scheduler` are both `protected_core: false`. The battery ran in full regardless. **18 mutations injected, 18 caught** by their intended control, every file restored from recorded bytes with sha256 re-verified. Three were initially **missed**, and each was chased down rather than accepted: two (a cached admission verdict, a frozen graph verdict) were defective mutations that added a cache check but never populated it, rewritten to actually cache and then caught; one (a tripwire silently removed from the permanent-replacement list) was a **real control gap** — the list living in the same file as the control that reads it meant shrinking it evaded detection — closed by asserting the list's own length before reading any entry.

**PHASE 9B PACKAGE 1 (query-time reference resolution for activated C-13).** `control.capability` now performs the rule `EXECUTION_AND_CAPABILITY.md` §1 states as the definition of activation: "`can_perform(capability_id)` resolves every `*_ref` at query time against its owning authority." `reference_authority.py` parses the reference→authority binding out of the canonical `capability_node:` block itself — each reference field carries a `# -> <context>` annotation — and `reference_resolution.py` performs the resolution. **Neither the fields, the target kinds nor the authorities appear in code**: a schema that gains a reference moves these mechanisms instead of expiring them, and controls reconcile the parsed binding against `CapabilityNode` in both directions.

**Resolution is by injected interface, because the import is forbidden.** `control.capability` is layer rank 1 and so is every authority it references — `control.policy`, `control.specification`, `control.registry.provider`, `control.isolation`. `allow_same_layer: false` and no declared sibling edge means the import may not exist; `ARCHITECTURE.md` §4 rule 3 makes interface inversion the answer. `ReferenceResolver` is therefore a `Protocol` declared in this context, mirroring the shape `execution.scheduler` already uses to ask this context its own question. **A derived control proves the design is forced rather than chosen**: it re-reads the map and fails if any referenced authority stops being the same layer rank. The `policy_callable_from_any_layer` exemption is **not** used — the same call Phase 6 Package 2 and Phase 9 Package 1 both made.

**A reference resolves; a value never crosses.** The whole question a resolver may be asked is `resolves(reference) -> bool`, and a control asserts the Protocol declares exactly one method returning exactly `bool`. There is no field in this context that could hold provider identity, model identity, configuration, health, availability, cost metadata or fallback, so `ARK-REQ-0047` and `ARK-REQ-0053` stay structural rather than policed. The `shadow_registry` source gate now scans **17** consumer modules, up from 15, and passes.

**PASS is reachable only through a live authority's affirmative answer.** A missing resolver yields `NOT_CONFIGURED`, a refusing resolver yields `FAIL`; both are determinate and neither is a stub. `resolve_references` **refuses** when no authorities are composed at all rather than returning an empty tuple, because an empty answer is indistinguishable from "everything resolved" — exactly the assumption ADR-0003 forbids. A resolver supplied for an authority the canonical schema does not name is refused at construction.

**Nothing is cached, and that is structural.** `ReferenceResolvers` holds the binding authority and the resolvers and has exactly two attributes; the graph stores no outcome either. A control changes an authority's answer between two calls and requires the second answer to be observed — a test that merely called twice would prove nothing.

**The Phase 3 schema is reused, not replaced, and `can_perform` is unchanged.** One `CapabilityNode`, one `CapabilityGraph`, asserted across the whole shipping package. **Production stays honest:** every capability query still returns `NOT_CONFIGURED` before the activation phase and still refuses to invent a verdict at it, so Phase 8 admission still refuses with `CAPABILITY_NOT_CONFIGURED`. Composing resolutions into one determinate answer to "Can I perform this?" is Package 2, and **the canonical set states the resolution rule but not the verdict for an unresolved external reference** — that mapping must be derived there, or the ambiguity reported, rather than guessed at here.

**No requirement is discharged.** Phase 9B owns three — `ARK-REQ-0046`, `ARK-REQ-0047`, `ARK-REQ-0048` — and each is discharged only at phase acceptance; cumulative verified stays **83**. C-13 is `INT`, so Package 1 adds no table and no migration. No state machine was declared and the canonical count stays **12**. No provider was contacted, nothing was simulated, and no external-provider result exists.

**The Package 1 change set is NORMAL** by `select_profile` from its own paths, with no unresolved path — `control.capability` is not a Protected Core member. The battery was run in full regardless. **15 mutations injected, 15 caught** by their intended control, every file restored from recorded bytes and digest-verified. One was initially **missed** and was a defective mutation rather than a control gap: it read a memo it never wrote, so it was a semantic no-op; rewritten as a real cache it was caught by the call-counting control.

**Phase 9 Package 1 (the C-11 provider/model record).** `control.registry.provider` now holds the record that makes `ARK-REQ-0052` concrete: the seven concerns `AUTHORITY_MAP.yaml` `provider_authority.fields_owned` assigns to this registry — identity, model identity, configuration, health, availability, cost metadata and fallback. **Neither the concern list nor the reference-only consumer list appears in code**: both are parsed at call time, and controls assert that no module in the context hard-codes either, reconciling the map, the model and `docs/contracts/provider_record.md` in all directions.

**The Phase 3 ProviderHealth machine is reused, not duplicated.** The record reads `DEFINITION.states` and refuses anything outside it; it declares no state, no transition and no second machine, and a control asserts that no module except the machine itself contains a health-state literal. **The canonical machine count stays at 12.**

**C-11 is `INT`, and that is governed data rather than a preference.** The inventory gives C-11 `INT` where C-12 — the Project/Revision Registry — is `DB+INT`, so this package adds no table, no migration and no ORM record. A control re-reads the inventory row and fails if the kind ever changes, so the reasoning cannot silently expire.

**Configuration cannot carry a secret, and holds no policy state either.** `configuration` holds opaque C-09 handles and every string the record carries — including inside tuples — is scanned for raw-secret shapes and refused. **A control forced a better design here:** the first draft imported `control.policy.SecretReference`, which would have been the repository's **first live edge leaning on the `policy_callable_from_any_layer` exemption**, and `test_live_repository_uses_no_exempt_edge` refused it. The exemption was left alone and the dependency dropped — the same call Phase 6 Package 2 made. The deeper reason is better still: `SecretReference` carries `revoked`, which is *policy* state, so storing it here would have mirrored another authority's values inside this registry, which is precisely the disease `ARK-REQ-0053` names.

**No requirement is discharged.** Phase 9 owns three and each is discharged only at phase acceptance; cumulative verified stays **80**. Package 1 contacts no provider, measures no health, simulates nothing, and does not activate the Capability Graph — that is **Phase 9B**, so `can_perform` still returns `NOT_CONFIGURED` and Phase 8 admission still refuses with `CAPABILITY_NOT_CONFIGURED`.

**The Package 1 change set is NORMAL** by `select_profile` from its own paths, with no unresolved path. Full regression **1812 passed / 13 skipped**, mypy strict clean over **141** modules, 8 gates PASS, 9 budgets no violation, depth 4 of 4. **15 mutations injected, 15 caught** by their intended control, every file digest-verified after restore.

**PHASE 8 IS MACHINE-ACCEPTED** on its **first and only** gate submission: verdict `PHASE_ACCEPTED_BY_MACHINE`, progression PERMITTED, recorded in `docs/acceptance/phase_8_report.json` and `phase_8_traceability.json`. C1–C6 PASS; PROTECTED_CORE PASS (no member touched, so the normal profile applies); RESCORING NOT_APPLICABLE; FINDINGS and PREREQ (1/1) PASS; HUMAN_GATE NOT_APPLICABLE — the matrix Gate column for Phase 8 is empty; all 8 architecture gates PASS over **100** cross-context edges with 9 budgets and no violation.

**This is the repository's first ZERO-DENOMINATOR acceptance, and it is the whole point of the F-0040 repair.** No register row carries Phase 8, so `claims: []` and `ark_req_ids_closed: []` are not omissions but the only truthful shapes, and C6 resolves that entitlement from the register rather than from the record under test. **Cumulative verified stays 80** and MANDATORY verified stays 79 — Phase 8 adds nothing to either, because it owns nothing to add. C-21 is evidenced as a *contract* through `public_contracts`, `tests_executed` and `evidence_created`, never as an `ARK-REQ`, and a control proves no requirement anywhere names it.

**Production admission returns `CAPABILITY_NOT_CONFIGURED` for every request, and that is the accepted outcome.** The Capability Graph is schema-only until **Phase 9B**; `EXECUTION_AND_CAPABILITY.md` §1 calls the pre-activation answer "a determinate answer, never a stub, default or assumption". The mechanism is real and complete — seven canonical worker classes declared under a real PEP, each submitted for admission against the real `CapabilityGraph`, each refused with the graph's own reason. The `ADMITTED` branch is verified only through a test-only resolver, and a control makes that resolver unshippable. **`ARK-REQ-0354` is Phase 31 and is NOT CLAIMED.**

**The acceptance guards were proven able to refuse before the gate was ever run.** Thirteen defective Package 3 artifacts — a synthetic claim, a foreign-phase discharge, the contract id smuggled in as a requirement, a discharge with no claim, a failing run under a PASS status, an unnamed run, emptied required fields, a deleted traceability record, a malformed claims list, an unknown requirement id, an open HIGH finding and an unaccepted prerequisite — were each injected and each **refused**, with the real artifacts restored and digest-verified after every case. One further case (a self-declared `FAIL` while the evidence is green) was **accepted** and that is correct: canonical C5 forbids converting a non-PASS state *into* PASS, not pessimism, so it was classified as a semantically inert mutation and replaced rather than used to invent a rule the canonical set does not state. The gate was then run **once**, on the final clean candidate.

**Phase 9 — Provider + Model Runtime (C-11) — is unlocked** by that verdict; its matrix prerequisites 4, 6 and 8 are all accepted.

**PHASE 8 WAS RE-SCORED UNDER A SCOPE-LIMITED GOV-001 AUTHORIZATION, AND THE FIRST ACCEPTANCE IS RECORDED AS DEFECTIVE.** Post-commit verification found that the first `phase_8_report.json` reported `check_repository_structure.py` and its negative control at exit 0 when, at the submitted commit `bb845d8`, the former returned **11 pass, 1 fail** — structure check 10 tripped by a docstring token in `test_phase_8_journey.py`. With the true exit codes the first submission would have been **PHASE_BLOCKED** on C3 and C5. That is **F-0045**, severity **HIGH**.

**The acceptance authority refused to downgrade it.** Lowering F-0045 to MEDIUM, or leaving the first acceptance standing under an erratum, were both explicitly rejected; a superseding re-score was required instead. `RSA-002` in `HUMAN_GATE_RECORDS.md` authorizes exactly one supersession of Phase 8, bound to the digest of this exact evidence package, and states in the record that it is **not a general re-scoring power and not a precedent**.

**The defective first revision is preserved, not deleted.** `docs/acceptance/phase_8_report_rev1_defective.json` retains it byte-for-byte, and the first verdict is not altered or hidden. It is permanent evidence that the first submission was accepted on an inaccurate record.

**The superseding submission is genuinely re-executed.** Every one of the twelve records in the new report was re-run against this candidate; no value is transcribed from the defective revision. The two validators F-0045 is about are now genuinely exit 0 — 12 pass, 0 fail. **Superseding verdict: `PHASE_ACCEPTED_BY_MACHINE`, progression PERMITTED**, with `RESCORING: superseding re-acceptance authorized by RSA-002` and `FINDINGS: no open BLOCKER or HIGH finding`.

**F-0045 is CLOSED with its HIGH severity intact**, only after all five conditions held together: the false records corrected in a re-executed report, the validators genuinely green, the acceptance guard re-proven with the true exit codes, the candidate fully re-verified, and the superseding gate run executed by the real checker. **Guard case G14 makes the finding executable**: it records the real failing validator exit code and the checker refuses it on C3 and C5 — precisely what should have happened at the first submission. 14 of 14 defective candidates were refused, each restored and digest-verified.

**The three governance tests that failed while F-0045 was open now pass again.** `test_every_check_passes_for_a_complete_report`, `test_an_accepted_phase_is_not_re_issued_without_authorization` and `test_13_gov_001_ratification_of_phase_4_remains_valid` assert that the **live** repository passes every gate check; they failed correctly while a HIGH was open and returned to green on the finding's legitimate closure. **None was weakened, skipped, xfailed or edited to accommodate the finding.**

**Pre-Package-3 remediation: three latent defects that only a canonical environment could expose.** Until now the repository had never been installed from its own manifest onto a clean interpreter — every phase ran on a machine whose environment had accumulated extras. Standing up the official Python **3.13.15** environment and installing `backend/pyproject.toml` editable+dev found three real defects in one pass. **All three are MEDIUM, all three fail in the refusing or detecting direction, and none reopens an accepted phase**: no PASS was ever obtained from a broken path, and every historical run really did execute.

**F-0042 — the real runtime entrypoint imported a dependency no manifest declared.** `scripts/run_command_center.py` serves the Command Center with `uvicorn` and is the T10 browser-evidence harness, but uvicorn appeared in no dependency group and this repository has no Python lockfile. A canonical install produced fastapi, pydantic, sqlalchemy, alembic and yaml — and an entrypoint that stopped at `ModuleNotFoundError`. `EV-0048` records the accepted Phase 5 journey as running against "a real uvicorn-served `surfaces.command` process", so that tier was reproducible only on a host that already happened to have it. **`uvicorn>=0.52` is now declared in `[project].dependencies`** by human ruling: the launcher is the real runtime entrypoint and uvicorn is its direct dependency. The floor is derived from the version mechanically verified during LIVE CHECKPOINT 1 (0.52.1) under this file's existing major.minor policy, not chosen.

**F-0043 — the test tier could not be collected at all, one layer deeper.** Four modules use `fastapi.testclient.TestClient`; `starlette.testclient` binds an HTTP backend that starlette declares only under its own `full` extra, and the manifest declared neither `httpx2` nor `httpx`. On the canonical install pytest reported `Interrupted: 1 error during collection` — **stricter than F-0042, because no evidence tier could run**. It survived three phases because the previous Python 3.12 environment carried `httpx 0.28.1` as a transitive of an ad-hoc `fastapi-cli`. **`httpx2>=2.0.0` is declared in the `dev` extra** — name and floor derived from starlette's own metadata and from its `testclient`, which binds `httpx2` and treats plain `httpx` as deprecated. It is `dev` and not `dependencies` because no module under `backend/arkali/` imports `TestClient` or `httpx`.

**One derived control now covers both.** `test_script_dependencies.py` derives every third-party import of every entrypoint script — from the AST, the live stdlib list and the installed distribution mapping, nothing transcribed — and requires the manifest to declare it; separately it asks the live starlette which HTTP backend it actually bound and requires that to be declared too, so a future backend change is followed rather than going stale. Both halves were mutation-proven: removing either declaration from the manifest fails the intended control.

**F-0044 — a C-17 contract control refused a contract that had not changed.** `test_contract_drift.canonical` accepted an opaque object only when the schema carried neither `properties` nor `additionalProperties`, conflating "the key is present" with "the key constrains". Pydantic 2.8 rendered `payload: dict[str, object]` as `{type: object}`; Pydantic 2.13 renders the identical declaration as `{type: object, additionalProperties: true}` — the explicit spelling of *unconstrained*. **Proven stale before being edited**: the backend field and the frontend types are byte-unchanged since `975b1df`, and both serialisers were run side by side to show only the spelling moved. The branch now asks whether `additionalProperties` **constrains**; `false`, a schema-valued `additionalProperties` and any declared `properties` all still fall through to the refusal, proven case by case. **No test was weakened, skipped or xfailed.**

**No lockfile was created** — the repository still has none, and that fact is preserved rather than papered over. **No accepted evidence was edited**: `EV-0048`, `phase_5_*.json` and `PHASE_HISTORY.md` are untouched. Full regression on Python 3.13.15: **1739 passed / 13 skipped**, mypy strict clean over 138 modules, 8 gates PASS, 9 budgets no violation, depth 4 of 4, all five validators exit 0.

**Phase 8 Package 2 (the admission decision).** `execution.scheduler` now answers `EXECUTION_AND_CAPABILITY.md` §4: a job is admitted only when **(a)** its capability resolves other than `NOT_CONFIGURED`, **(b)** an isolation composition satisfies its tier, and **(c)** resource budget is available. **All three are required**, and a control breaks each one alone to prove each is sufficient to refuse. The decision is decomposed by responsibility — `capability_admission`, `isolation_admission`, `resource_admission` and the `admission` composition — so no module touches more than one foreign context and none becomes a second authority for its dependency.

**Production admission refuses every request, and that is the correct answer.** The Capability Graph is schema-only until **Phase 9B**; before activation every query returns `NOT_CONFIGURED`, which §1 calls "a determinate answer, never a stub, default or assumption". Admission therefore returns `CAPABILITY_NOT_CONFIGURED` against the real `CapabilityGraph`, carrying the graph's own reason. **The mechanism is real and complete; only the answer is a refusal.** The `ADMITTED` path is exercised with a test-only resolver, and a control asserts that no module under `backend/arkali/` outside `control.capability` answers `can_perform` — so that resolver cannot be substituted into production.

**No cached capability verdict, proven by counting the authority's calls.** §1 names `execution.scheduler` at Phase 8 as a consumer that must not cache. The evaluator holds the resolver and never a result: it has exactly one attribute, and three evaluations produce three queries. A control also changes the authority's answer between calls and requires the second answer to be observed — a test that merely called twice would prove nothing.

**The canonical predicate was implemented as written, not as assumed.** §4 says "resolves other than `NOT_CONFIGURED`", so that is the predicate. It was deliberately **not** narrowed to `state is PASS`: a stricter rule the canonical set does not state is as much an invention as a weaker one, and post-activation resolution semantics belong to Phase 9B. A control pins the distinction.

**Admission decides; it allocates nothing.** Nothing is reserved, claimed, assigned, dispatched, queued, prioritised or started, no capacity is consumed, and no C-19 row is read or written. Availability is a frozen input per evaluation, not a resource universe this context owns — evaluating twice returns the same answer and leaves the snapshot untouched. Real host isolation is honoured rather than converted: TRUST-0/1 are satisfiable here, TRUST-2/3/4 report `UNSUPPORTED`/`DENY` unchanged. **A PEP denial stays distinct** — it raises `PolicyDenied` before any condition is evaluated and is never reclassified as a capability, isolation or resource failure.

**Package 1's controls were narrowed deliberately, not deleted.** `ADMISSION_NAMES` became `ALLOCATION_NAMES`: `admit`, `admission`, `can_perform` and `capability_state` were removed because Package 2 legitimately owns them, and `reserve`, `assign`, `spawn`, `consume_capacity` and `start_work` were added because those are what would turn a decision into an allocation. The provider control now distinguishes the canonical `provider` **worker class** from provider **runtime** authority, so it no longer risks banning the canonical vocabulary itself. The operation-class control was rebuilt to read the actual `PolicyRequest(operation_class=...)` argument instead of guessing from string shape — the old form would have condemned every uppercase enum value Package 2 introduced while still missing a class assembled at runtime.

**A real budget fired and was answered by decomposition.** `test_scheduler_authority.py` reached **466 logical lines** against the 400 budget, and was split along a real seam under ADR-0008 — declaration boundaries stay, admission authority moves to `test_admission_authority.py` — exactly the F-0032 precedent. No exception was requested. Orchestration depth is unchanged at **4 of 4** over **100** cross-context edges.

**The Package 2 change set is NORMAL** by `select_profile` from its own paths, with no unresolved path. Full regression **1736 passed / 13 skipped**, security 188, mypy strict clean over **138** modules, 8 gates PASS, 9 budgets no violation. **20 mutations injected, 20 caught** by their intended control, each verified after the decomposition as well as before, with every file restored and digest-verified.

**Phase 8 Package 1 (C-21 worker contract + worker declarations).** `execution.scheduler` now holds the C-21 declaration: `worker_vocabulary` parses the seven canonical worker classes and the six declaration dimensions from `EXECUTION_AND_CAPABILITY.md` §4 at call time, and `worker_contract` validates and holds declarations under an injected PEP. **Neither list is transcribed into code** — a control asserts the parser contains none of what it parses, and a second reconciles the canonical prose, the single binding table, the pydantic model and `docs/contracts/worker.md` in all directions. Canonical spelling is exact: `Agent`, `build_test`, `local-ai` and `computer use` are all refused, because a second spelling is a second vocabulary. **Tiers and isolation properties are asked of `control.isolation`, not copied** — rank 3 depending on rank 1, so no sibling edge was needed and an unknown tier raises that context's own `TrustTierViolation` unchanged.

**C-21 is INT, and Package 1 is honest about what that excludes.** No table, no migration, no ORM record, no engine, no session, no SQL, and no state machine — the canonical count stays at **12** and no machine names a worker or a scheduler. **No admission decision exists**: capability resolution, isolation composition and resource budget are all Package 2, and a control derives the scheduler's public operations and rejects any admission-, queue-, priority- or fairness-shaped name. Capability activation is Phase 9B and is not queried at all; failure-domain isolation is `ARK-REQ-0354` at **Phase 31** and is named as NOT CLAIMED. `execution.scheduler` imports no `execution.durable` module and names no C-19 concern, asserted from both sides.

**A real budget fired and was answered by decomposition, not an exception.** `kernel.contracts.errors` was already at `max_fan_in_per_module` (15), so the scheduler's error module would have been the sixteenth importer. Under ADR-0008 the abstract base layer — `ArkaliError`, `ContractViolation`, `GovernanceStateError`, `AuthoritativeSourceError` — moved to `kernel.contracts.error_base`, which `errors.py` re-exports, so **no existing call site changed** and the seam is the real one the module's own closing comment already described: what other contexts *derive from* versus what they *catch*. No GATE 8 exception was requested or authored. Orchestration depth is unchanged at **4 of 4**.

**F-0041 was found, opened, repaired and verified within the package.** Two governed phase titles disagreed with the canonical matrix — Phase 8 (`Execution Scheduler + Worker Contract`) and Phase 6 (`Artifact / Evidence Plane`) — because both were transcribed rather than derived, and nothing compared them. The new control reconciles **every** phase both documents name, and found the Phase 6 drift that was not being looked for. MEDIUM: `PhaseStatus.title` is consumed by no acceptance decision, so no accepted phase relied on it.

**Package 1 discharges no requirement, and none was claimed.** The register assigns Phase 8 **zero** `ARK-REQ` entries, so cumulative verified stays at **80**. No traceability record, no phase report and no gate run exist for Phase 8.

**Phase 7 Package 1 (C-19 persistence foundation).** `execution.durable` now holds the durable job record, its idempotency identity and the checkpoint record, with migration `0005_durable_job`. Two identities, one store: `job_id` is the primary key and (`job_type`, `idempotency_key`) is a named unique constraint, so a repeated submission resolves to the existing job across an engine dispose/reopen and cannot create a second one. Checkpoint ordering *is* identity — the primary key is (`job_id`, `sequence`) — and checkpoints refuse both update and delete. The canonical `Job` machine delivered in Phase 3 remains the sole transition authority; the store writes no state literal and no transition table, asserted structurally. Every governed operation passes an injected PEP under `READ_FILE`/`WRITE_WORKSPACE_FILE`, and a real PDP loaded from a denying authority map proves a refusal prevents the write. The clock is injected, so no test sleeps.

**The Phase 4 durable-surface tripwire fired as designed and was replaced, not weakened.** `test_surfaces_not_yet_built_are_still_absent` asserted no module existed under `execution/durable`; the first real module made it fail, naming its own obligation — "must be brought under a real PEP with runtime evidence". That obligation is now met, and the assertion is replaced by a stronger live control that derives which execution packages are built and requires each of them to consult a PEP, so it keeps failing for every unenforced package added later instead of going quiet. Same precedent as Phase 5 Package 4; not a finding, because nothing was defective.

**The Package 1 change set is PROTECTED_CORE** by `select_profile` from its own paths — member `control.architecture`, because completing the `refusal.py` decomposition that the kernel error fan-in budget demanded touches `authority_map.py` and `gates/runner.py`. All three canonical categories were executed at exit 0: *security review* (188), *adversarial review* (479 plus both negative-control validators) and *full regression* (1404 passed, 13 skipped).

**Phase 7 Package 2 (durability semantics).** `job_execution_attempt` records one row per execution attempt — owner, `started_at`, `heartbeat_at`, an absolute `deadline_at`, and `ended_at`/`outcome` when it closes — with migration `0006_durable_execution`, which also records `max_attempts` and `attempt_timeout_seconds` on the job. **Retry accounting is rows, not a counter**: the count is `count(job_execution_attempt)`, so it cannot be reset by restarting a process, set by a caller, or drift from what happened, and the primary key (`job_id`, `attempt`) is the concurrency backstop rather than a service check. The deadline is an absolute instant, so a restart cannot move the timeout basis. A failure disposes through the canonical machine: `RUNNING → FAILED`, then `RECOVERABLE` while the recorded bound leaves an attempt and `DEAD_LETTER` when it does not. `DEAD_LETTER` has no outgoing transition and `CANCELLED` is reachable only from `RUNNING`, so "cannot be silently retried" and "a queued job cannot be cancelled" are properties of the canonical relation rather than of code. A stale owner cannot heartbeat, complete or fail an attempt, and a closed attempt cannot be written again. Timing out twice is idempotent and a deadline that has not passed is refused.

**The Package 2 change set is NORMAL** by `select_profile` — it touches no Protected Core context. The battery was run in full regardless: full regression 1447 passed / 13 skipped, security 188, structural+governance 489, mypy strict clean over 124 modules, 8 gates PASS over **82** edges, 9 budgets no violation.

**Package 2 stopped where Package 3 begins.** It entered no `RESUMING`, ran no recovery sweep, and implemented no pause/resume or `supports_pause` registry. What it recorded (`heartbeat_at`, `deadline_at`, a `RECOVERABLE` disposition) is exactly what Package 3's recovery reads.

**Phase 7 Package 3 (job-type registry, pause/resume, crash recovery).** `durable_job_type` is the registry `ARK-REQ-0060`'s Appendix A rule reads — `job_type.supports_pause == true` — with migration `0007_job_type_registry`, which also adds `heartbeat_timeout_seconds` to the job. **The canonical relation was read before any code was written, and it decided the routes.** `RESUMING` has exactly two predecessors, `PAUSED` and `RECOVERABLE`, so pause and crash recovery share one lifecycle meaning because the machine says so; and `RUNNING → RECOVERABLE` is **not declared** and `evaluate` refuses it, so recovery travels `RUNNING → FAILED → RECOVERABLE → RESUMING` — the disposition Package 2 already owns, composed rather than repeated. `EXECUTION_AND_CAPABILITY.md` §3 names the states a crashed job resolves *to*; `STATE_MACHINES.md` §3 owns the edges it travels, and the two agree once the route is derived rather than assumed. **Nothing was added to the canonical machine.**

**Pause suspends; recovery disposes.** A paused job keeps its open attempt, its owner, its absolute deadline and its position in the retry budget — closing it would consume an attempt and pausing is not failing, while extending its deadline would reset the timeout basis the job was admitted under. A crashed job's attempt is closed, which is what makes the departed owner unable to act: a returning zombie worker cannot heartbeat, complete or fail it. A **paused** job is never swept however silent, because the sweep selects `RUNNING` — a filter that is load-bearing, not tidy. The sweep is idempotent by construction rather than by a guard, stops at `RESUMING` and starts nothing: choosing who runs a job next is `execution.scheduler` at Phase 8.

**F-0036 was found, opened, repaired and verified within the package.** Package 2's `heartbeat_expired` resolved to a comparison against `deadline_at` and never read `heartbeat_at` at all — proven by AST against the untouched tree before anything was edited. The name promised silence since the last proof of life; the behaviour measured elapsed work. Package 2 was unharmed because both callers meant timeout, but the canonical crash-recovery rule keys on the heartbeat, so consuming the name as written would have swept live workers and left dead ones holding their jobs. MEDIUM; no accepted phase relied on it.

**The Package 3 change set is NORMAL** by `select_profile` from its own paths — no Protected Core member touched, no unresolved path. The battery was run in full regardless: full regression **1535 passed / 13 skipped**, mypy strict clean over **126** modules, 8 gates PASS over **86** edges, 9 budgets evaluated. **14 mutations injected, 14 caught**, each verified to fail its *intended* control rather than incidentally. A real budget fired — `test_durable_recovery.py` reached 440 logical lines — and was answered by decomposition under ADR-0008 along a real seam (decide correctly / survive a restart), not by an exception.

**Phase 7 Package 4 (the enqueue surface for ARK-REQ-0027).** `POST /api/jobs` persists durable work through C-19 and returns a durable reference; `GET /api/jobs/{job_id}` resolves one. **The guarantee is architectural, not a latency budget** — the register gives `ARK-REQ-0027` `arch` evidence, and what is asserted is that after the response the work demonstrably has *not* started: the job sits in the state the canonical machine declares initial, with **zero** execution attempts and zero checkpoints. A handler that had run the job could not leave that state. The forbidden call set is **derived from `JobExecution` and `JobRecovery` themselves**, so a method added to either later is covered without editing a control. `202 Accepted` is the honest status: the request was accepted and the processing has not completed, and it avoids inventing a created-versus-existing distinction that C-19's persisted idempotency makes irrelevant. Idempotency is not reimplemented — a repeated `(job_type, idempotency_key)` resolves to the recorded job across a full application restart, which an in-memory cache could not do. The route builds no engine, issues no query, writes no lifecycle state and names no scheduler, worker, dispatch or provider concept. **The job-type registry is not an admission gate:** an undeclared type enqueues, because requiring registration to submit would be admission control and that is C-21 at Phase 8.

**The surface was decomposed before it was written, not after a gate fired.** `app.py` and `error_mapping.py` were each already at `max_contexts_touched_by_module` (3), measured first, so the routes went into `jobs.py` and the durable error table into `job_error_mapping.py` under ADR-0008. **`max_orchestration_depth` is now 4 of 4** — `surfaces.command → execution.durable → control.policy → kernel.contracts` — at its ceiling and not over it. The next package that adds a context hop to that chain will breach it and must decompose rather than ask for an exception.

**Three findings were opened and closed, two of them pre-existing Phase 5 defects this package exposed.** F-0037: a contract control assumed every backend route was also a browser-slice route, which expired on the repository's first backend-only route; routes now declare their audience and the control reads it off the live document. F-0038: one durable timestamp had two wire representations, aware or naive depending on whether the row was still in the session — reproduced on the Phase 5 routes alone. F-0039: `PolicyDenied` mapped to 403 but was unreachable from any route because `guard()` runs before the `try`, so a real denial surfaced as HTTP 500 — also reproduced on Phase 5 alone, and the control that should have caught it asserted the table rather than the route. All three MEDIUM; none failed open.

**Phase 7 Package 5 (final integration, evidence, traceability, acceptance).** The composed journey drives one durable job through its whole life across **six separate runtimes** on one real SQLite file: declared job type, enqueue through the real HTTP API, execution with ownership and heartbeat, a checkpoint, pause and resume, a full restart, recovery from an expired heartbeat, a second attempt, completion. Real fault injection at the available tier covers process loss with persisted `RUNNING` state, heartbeat expiry seen by a process that never watched the job start, a returning zombie owner, a failed transaction, a duplicate attempt refused by the primary key, retry exhaustion to `DEAD_LETTER`, timeout while the worker still reports, and sweep idempotency across restarts. **T12, `ARK-REQ-0327` and Phase 31 are named as NOT CLAIMED**, and a control derives from the register that `ARK-REQ-0327` is not Phase 7's to discharge. Phase 7 evidence is recorded through the Phase 6 C-14/C-15 authorities unchanged — no second evidence mechanism, no Phase 13 graph.

**Fifteen anti-vacuity cases and eight false-discharge variants were proven to fail before the gate ran.** Every critical Phase 7 property was broken in the shipping source and the control that claims to protect it rejected the break; every defective traceability shape was rejected by check C6, and the real record was restored byte-for-byte with its digest re-verified.

**PHASE 7 IS MACHINE-ACCEPTED** on its **first** gate submission: verdict `PHASE_ACCEPTED_BY_MACHINE`, progression PERMITTED, recorded in `docs/acceptance/phase_7_report.json` and `phase_7_traceability.json`. C1–C6 PASS; **PROTECTED_CORE COMPLETE** — the profile derived as PROTECTED_CORE from the phase's own changed paths (member `control.architecture`, from Package 1's kernel-error decomposition), with security review, adversarial review and full regression all at exit 0; RESCORING NOT_APPLICABLE; FINDINGS, PREREQ (2/2) and all 8 architecture gates PASS over **91** edges; HUMAN_GATE NOT_APPLICABLE. **ARK-REQ-0003, 0027, 0059, 0060 and 0061 are all DISCHARGED.**

**Phase 7 is the first phase to discharge a CONDITIONAL requirement, so two counts now differ and both are recorded.** Of the five, four are MANDATORY (0003, 0027, 0059, 0061) and one is CONDITIONAL and **APPLICABLE on its merits** (0060 — its Appendix A rule is now mechanically evaluable and evaluates true against persisted data, rather than being APPLICABLE by governing rule 7's unevaluable default). Cumulative discharged rises 75 → **80**; MANDATORY verified rises 75 → **79**. No applicability waiver was requested or granted.

Phase 5 is accepted and unchanged. Phase 6 was delivered in three atomic packages and accepted on its **first** gate submission: verdict `PHASE_ACCEPTED_BY_MACHINE`, progression PERMITTED, recorded in `docs/acceptance/phase_6_report.json` and `docs/acceptance/phase_6_traceability.json`. C1–C6 PASS; PROTECTED_CORE **COMPLETE**; RESCORING NOT_APPLICABLE (no prior acceptance record); FINDINGS, PREREQ and all eight architecture gates PASS; HUMAN_GATE NOT_APPLICABLE — the matrix Gate column for Phase 6 is empty, and `evidence.audit` being Protected Core does not invent one. **Re-running the gate for Phase 6 now returns `AWAITING_RESCORING_AUTHORITY`; that is GOV-001 working, not a regression.**

**Package 1 (C-14).** `evidence.artifact` holds the artifact descriptor, provenance record and parent edges. Identity *is* the content address, derived from the bytes and never supplied. Artifacts and provenance are immutable by ORM refusal; a tampered blob is detected and never repaired. Blob writes are PEP-governed under `WRITE_WORKSPACE_FILE`. C-12 was not touched.

**Package 2 (C-15).** `evidence.audit` — **Protected Core** — holds the append-only evidence chain: `record_hash` is a digest over every field *including* the predecessor's digest, so the chain is a real hash chain and `verify()` **recomputes** rather than reading any stored flag. Update and delete are both refused at the ORM; the public authority is `append/get/require/head/records/verify/superseded_by` with no amend or delete escape. Supersession appends and preserves. A record must name a requirement the canonical register declares and an artifact C-14 actually registered.

**The two evidence contexts share a mechanism, not an authority.** The content-address primitive moved to `kernel.contracts` so both siblings could reach it at rank 0; `allow_same_layer` stays false, no exemption was widened, and `test_live_repository_uses_no_exempt_edge` passes. Artifact linkage is enforced by the persisted foreign key rather than by importing the sibling's ORM record.

**The Package 2 change set is PROTECTED_CORE** by `select_profile` from its own paths — members `control.architecture` and `evidence.audit`. All three canonical categories were executed at exit 0: *security review* (185), *adversarial review* (413 plus both negative-control validators) and *full regression* (1293 passed, 13 skipped).

**Package 3 (integration, evidence, traceability, acceptance).** The two contracts were proved to compose as one Evidence Plane on real persistence: bytes registered through C-14, evidenced through C-15 against a real register requirement, the engine disposed, a fresh engine opened over the same SQLite file and blob root, the artifact resolved, its bytes re-verified against their content address, the record re-read, the chain recomputed, and a superseding record appended whose predecessor is proven unchanged and whose chain still verifies after a further reopen — four separate engines in one journey, asserted. Provenance evidence records every item `VDC §Provenance` and `MS §Artifact Fabric` name and re-verifies the binding to the content address after reopen. Graph-readiness asserts only that Phase 6 writes data Phase 13 could use; it builds no graph and computes no coverage.

**ARK-REQ-0004, ARK-REQ-0057 and ARK-REQ-0349 are DISCHARGED**, each SATISFIED with a named implementation and a named evidence source, reconciled by check C6. C-15 has no register row of its own and is mapped explicitly onto ARK-REQ-0004, so the audit half is traced rather than merely implemented. Cumulative verified rises 72 → **75**.

**F-0033 was found, opened, repaired and verified before acceptance.** Running the T11 tier standalone — the way this phase's report records it — showed `alembic/env.py` holding one table out of seven as its autogenerate target, because `PersistenceBase.metadata` is populated by import side effect. Autogenerate treats an unseen table as removed, so the tool was configured to propose dropping six real tables. MEDIUM: every migration in the chain is hand-written, none was autogenerated, and the comparison control failed in the **detecting** direction, so no defective schema was ever produced and Phase 5's acceptance is unaffected.

**Canonical source commit:** `079c925996034017855fb9d1f1fa532077d7e86d`
**Accepted Phase 0 candidate:** `007ebf6e9275fa99d932022004440b1b869701d4`
**HUMAN GATE 1:** ACCEPTED — record `HGR-001` in `docs/acceptance/HUMAN_GATE_RECORDS.md`
**Last updated by:** Phase 9B Atomic Package 2 (the activated deterministic query)
**Governance errata and rulings:** ERR-001 (closes F-0015) · **ERR-002** (closes F-0018 — Plugin `REMOVED` is terminal) · **ERR-003** (closes F-0020 — architecture-budget measurement contract) · **ERR-004** (confirms F-0024, orders remediation) · **GOV-001** (superseding re-acceptance rule; ratifies the Phase 4 re-acceptance) — see `docs/acceptance/HUMAN_GATE_RECORDS.md`

---

## Phase status

| Phase | Title | Status |
|---|---|---|
| 0A | Canonical Architecture | **ACCEPTED** |
| 0B | Governance & Executable Contracts | **ACCEPTED** |
| 0 | Acceptance package (0A + 0B) | **ACCEPTED — HUMAN GATE 1 granted** |
| 1 | Repository Bootstrap | **MACHINE-ACCEPTED** (validator 12/12, suite 7/7) |
| 2 | Foundation + Contracts + Phase Gate Checker | **MACHINE-ACCEPTED** (66 tests, 8 gates, mypy clean) |
| 3 | Formal State Machines + Capability Graph Schema | **MACHINE-ACCEPTED** (12 machines, C-13 schema, 444 new tests) |
| 4 | Security + Governance + Isolation Backends | **MACHINE-ACCEPTED** (re-accepted after the ERR-004 remediation; the defective first revision is retained as evidence) |
| 5 | Persistence + Project Registry + Minimal Backup/Restore | **MACHINE-ACCEPTED** (verdict `PHASE_ACCEPTED_BY_MACHINE`; C1–C6 PASS, PROTECTED_CORE COMPLETE. Five atomic packages; 7/7 requirements SATISFIED and discharged under C6. First real T10: 9 browser tests in Chromium against the production build, a live API and a real SQLite file) |
| 6 | Evidence Plane + Provenance + Artifact Store (C-14, C-15) | **MACHINE-ACCEPTED** (verdict `PHASE_ACCEPTED_BY_MACHINE` on first submission; C1–C6 PASS, PROTECTED_CORE COMPLETE over members `acceptance.engine`, `control.architecture`, `evidence.audit`. Three atomic packages; 3/3 requirements SATISFIED and discharged under C6. Migrations `0003_artifact_provenance` and `0004_audit_record`) |
| 7 | Durable Job + Workflow Core (C-19) | **MACHINE-ACCEPTED** (verdict `PHASE_ACCEPTED_BY_MACHINE`, progression PERMITTED, on the **first** submission; `docs/acceptance/phase_7_report.json`, `phase_7_traceability.json`). Five atomic packages: the C-19 durable-job persistence foundation, the durability semantics, the job-type registry with pause/resume and the crash-recovery sweep — migrations `0005_durable_job`, `0006_durable_execution`, `0007_job_type_registry` — the `ARK-REQ-0027` enqueue surface under `surfaces.command`, and final integration with real fault-injection evidence. C1–C6 PASS; PROTECTED_CORE **COMPLETE** (member `control.architecture`); RESCORING NOT_APPLICABLE; FINDINGS, PREREQ (2/2) and all 8 architecture gates PASS; HUMAN_GATE NOT_APPLICABLE. **5/5 requirements discharged.** Both prerequisites, Phases 5 and 6, are accepted; the matrix Gate column for Phase 7 is empty, so no human gate applies. *(This cell's prose is the exact wording F-0034 mis-read as accepting the phase; it is written plainly because the parser now reads the declared state and ignores prose — this row is the live proof.)* |
| 8 | Resource Scheduler + Worker Contracts (C-21) | **MACHINE-ACCEPTED** (verdict `PHASE_ACCEPTED_BY_MACHINE`, progression PERMITTED, on the **first and only** submission; `docs/acceptance/phase_8_report.json`, `phase_8_traceability.json`). C1–C6 PASS; PROTECTED_CORE PASS (no member touched); RESCORING NOT_APPLICABLE; FINDINGS PASS; PREREQ 1/1; HUMAN_GATE NOT_APPLICABLE; 8 gates PASS over 100 edges. Three atomic packages: the C-21 declaration, the three-condition admission decision, and final integration. **The first zero-denominator phase — 0/0 requirements, `claims: []`, cumulative verified unchanged at 80.** Production admission returns `CAPABILITY_NOT_CONFIGURED` until Phase 9B activates the Capability Graph; that is designed behaviour and is recorded as the phase's real outcome. |
| 9 | Provider + Model Runtime (C-11) | **MACHINE-ACCEPTED** (verdict `PHASE_ACCEPTED_BY_MACHINE`, progression PERMITTED, on the **first** submission; `docs/acceptance/phase_9_report.json`, `phase_9_traceability.json`). C1–C6 PASS; **EXTERNAL_RESULT PASS** — all 14 recorded runs declare their external-provider class and none claims an unsubstantiated result; PROTECTED_CORE **COMPLETE** (members `acceptance.engine` and `control.architecture`, derived over the whole Phase 9 candidate — security review 188, adversarial review 699, full regression 1915, all exit 0); RESCORING NOT_APPLICABLE; FINDINGS PASS; PREREQ 3/3 (Phases 4, 6, 8); HUMAN_GATE NOT_APPLICABLE; 8 gates PASS over 104 edges and all 9 budgets. Four atomic packages: the C-11 record and its parsed authority, `ARK-REQ-0053` enforced in consumer source, `ARK-REQ-0219` enforced in the acceptance path under decision **D-023**, and this composed integration. **3/3 requirements discharged** (`ARK-REQ-0052`, `0053`, `0219`); cumulative verified 80 → **83**. C-11 is `INT`, so no table and no migration. **No external provider was contacted and no external-provider result exists** — `ARK-REQ-0219` is earned by preventing fabrication, and the positive external path stays closed because the canonical set defines no verifiable binding. Production admission still returns `CAPABILITY_NOT_CONFIGURED`; the Capability Graph remains schema-only. **Phase 9B unlocked.** |
| 9B | Capability Graph Activation | **MACHINE-ACCEPTED** (verdict `PHASE_ACCEPTED_BY_MACHINE`, progression PERMITTED, on the **first** submission; `docs/acceptance/phase_9B_report.json`, `phase_9B_traceability.json`). C1–C6 PASS; **EXTERNAL_RESULT PASS** — all 14 recorded runs declare their external-provider class and none claims an unsubstantiated result; PROTECTED_CORE PASS (no member touched — `control.capability` is not Protected Core — so the normal profile applies); RESCORING NOT_APPLICABLE; FINDINGS PASS; PREREQ 4/4 (Phases 3, 4, 6, 9); HUMAN_GATE NOT_APPLICABLE; 8 gates PASS over 108 edges and all 9 budgets. Three atomic packages: query-time reference resolution parsed from the canonical block, the activated deterministic verdict, and the composed journey plus the acceptance record. **3/3 requirements discharged** (`ARK-REQ-0046`, `0047`, `0048`); cumulative verified 83 → **86**. C-13 is `INT`, so no table and no migration; no state machine was added and the count stays 12. **The activation mechanism is proven end to end and production admission can now reach `ADMITTED` in principle — but no capability is declared configured with real production identities**, every affirmative composes composition-root doubles for the provider, policy and isolation authorities, and no provider runtime, network call or external-provider result exists (D-023). **Phase 10 unlocked.** |
| 10 | Agent Runtime + Harness Engineering | **MACHINE-ACCEPTED** (verdict `PHASE_ACCEPTED_BY_MACHINE`, progression PERMITTED, on the **first** submission; `docs/acceptance/phase_10_report.json`, `phase_10_traceability.json`). C1–C6 PASS; **EXTERNAL_RESULT PASS** over all 14 recorded runs; **PROTECTED_CORE COMPLETE** — derived over the whole four-package candidate because Package 3 changed Protected Core `control.policy`; RESCORING NOT_APPLICABLE; FINDINGS PASS; PREREQ 2/2; HUMAN_GATE NOT_APPLICABLE; 8 gates PASS over 114 edges and all 9 budgets. Four atomic packages: C-22 the bounded harness task, C-23 the context package and its provenance, `ARK-REQ-0050`/`0051`, and the composed journey plus the acceptance record. **5/5 requirements discharged**; cumulative verified 86 → **91**. C-22 and C-23 are both `INT`, so no table and no migration; no state machine was added and the count stays 12. **No agent runtime, dispatcher or task executor exists** — a control over the shipping source asserts none is present — and no Context Compiler exists; C-23 is the compiler's output contract. **Phase 11 unlocked.** |
| 11 | Code Intelligence + Digital Twin | **MACHINE-ACCEPTED** (verdict `PHASE_ACCEPTED_BY_MACHINE`, progression PERMITTED, on the **first** submission; `docs/acceptance/phase_11_report.json`, `phase_11_traceability.json`). C1–C6 PASS; EXTERNAL_RESULT PASS over all 12 recorded runs; PROTECTED_CORE PASS (no member touched); RESCORING NOT_APPLICABLE; FINDINGS PASS; PREREQ 1/1; HUMAN_GATE NOT_APPLICABLE; 8 gates PASS over 118 edges and all 9 budgets. Four atomic packages deliver C-24: canonical graph/view vocabularies, the deterministic sourced graph store and Python AST builder, complete honest Digital Twin composition, and the composed journey plus acceptance record. **2/2 requirements discharged** (`ARK-REQ-0066`, `ARK-REQ-0067`); cumulative verified 91 → **93**. No table, migration or state machine was added, no external provider was contacted, and unavailable adapters/views are refused or explicitly absent rather than fabricated. **Phase 12 unlocked.** |
| 12 | Candidate Workspace + Semantic Assembly | **MACHINE-ACCEPTED** (verdict `PHASE_ACCEPTED_BY_MACHINE`, progression PERMITTED, on the **first** submission; `docs/acceptance/phase_12_report.json`, `phase_12_traceability.json`). C1–C6 PASS; EXTERNAL_RESULT PASS over all 13 recorded runs; PROTECTED_CORE COMPLETE (`control.policy`, `lifecycle.release`); RESCORING NOT_APPLICABLE; FINDINGS PASS; PREREQ 1/1; HUMAN_GATE NOT_APPLICABLE; 8 gates PASS over 125 edges and all 9 budgets. Six atomic packages deliver C-25, the isolated candidate workspace, immutable content-addressed manifest and assembly report, all eight canonical semantic checks, the authority-owned Stable progression boundary, direct-Stable-mutation refusal, the composed journey, traceability and acceptance record. **7/7 requirements discharged**; cumulative verified 93 → **100**. No Stable write, verification, acceptance or promotion capability is claimed. **Phase 13 unlocked.** |
| 13 | Acceptance Infrastructure + Evidence Graph | **MACHINE-ACCEPTED** (verdict `PHASE_ACCEPTED_BY_MACHINE`, progression PERMITTED, on the first and only submission; `docs/acceptance/phase_13_report.json`, `phase_13_traceability.json`). C1–C6 PASS; EXTERNAL_RESULT PASS over all 13 recorded runs; PROTECTED_CORE COMPLETE (`acceptance.engine`, `control.policy`); RESCORING NOT_APPLICABLE; FINDINGS PASS; PREREQ 2/2; HUMAN_GATE NOT_APPLICABLE; 8 gates PASS over 131 edges and all 9 budgets. Six atomic packages deliver C-16, register-derived applicability and coverage, Passport accounting, policy boundaries, the evidence-derived capability verdict and the composed real-source acceptance journey. **27/27 requirements discharged**; cumulative verified 100 → **127**. No release, Stable-promotion or human-gate authority is claimed. **Phase 14 unlocked.** |
| 14 | Repair / Root Cause / Convergence | **MACHINE-ACCEPTED** (verdict `PHASE_ACCEPTED_BY_MACHINE`, progression PERMITTED, on the first and only submission; `docs/acceptance/phase_14_report.json`, `phase_14_traceability.json`). C1–C6 PASS; EXTERNAL_RESULT PASS over all 11 recorded runs; PROTECTED_CORE PASS (no member touched, normal profile); RESCORING NOT_APPLICABLE; FINDINGS PASS; PREREQ 1/1 (Phase 13); HUMAN_GATE NOT_APPLICABLE; 8 gates PASS over 135 edges and all 9 budgets. Five atomic packages deliver C-26: the repair-fingerprint and budget-ledger evidence contract, the anti-loop repeated-strategy refusal, the mechanical proof that MS/BP's root-cause pipeline stage lists could not reconcile without governance authority (CANONICAL_AMBIGUITY), `ARK-REQ-0240`'s deterministic-transformer candidate-lifecycle confinement delegated to the existing Phase 10 `AgentAuthority`, and — after **D-024**, the human governance ruling naming BP's 11-stage Failure Protocol the single executable Phase 14 pipeline — the executable `RepairPipelinePath` composed with the unchanged Packages 1–4 mechanisms. **7/7 requirements discharged**; cumulative verified 127 → **134**. No deterministic-transformer execution engine exists; no state machine was added and the count stays 12. **Phase 15 unlocked.** |
| 15 | Requirement + Architecture Intelligence | **MACHINE-ACCEPTED** — verdict `PHASE_ACCEPTED_BY_MACHINE`, progression PERMITTED, on the first and only submission (`docs/acceptance/phase_15_report.json`, `phase_15_traceability.json`). C1–C6 PASS; EXTERNAL_RESULT PASS over all 11 recorded runs; PROTECTED_CORE PASS (no member touched, normal profile); RESCORING NOT_APPLICABLE; FINDINGS PASS; PREREQ 1/1 (Phase 13); HUMAN_GATE NOT_APPLICABLE; 8 gates PASS over 138 edges and all 9 budgets. The Phase 15 canonical gap identified in prior read-only research (zero requirements, zero contract, zero bounded context) was closed by human governance ruling **D-025**, extending `control.specification` and introducing `C-37`. One atomic package delivers the deterministic requirement/architecture blueprint engine — decomposition, provenance, ambiguity/contradiction detection, acceptance-criteria derivation, closed-vocabulary classification, and architecture/capability mapping composed live from `AuthorityMap.concerns`. **11/11 requirements discharged**, all MANDATORY; cumulative verified 134 → **145**. No product code generated, no acceptance verdict rendered, no second architecture authority created. **Phase 16 unlocked.** |
| 16 | AI Software Factory | **MACHINE-ACCEPTED** — verdict `PHASE_ACCEPTED_BY_MACHINE`, progression PERMITTED, on the first and only submission (`docs/acceptance/phase_16_report.json`, `phase_16_traceability.json`). C1–C6 PASS; EXTERNAL_RESULT PASS over all 11 recorded runs; PROTECTED_CORE PASS (no member touched, normal profile); RESCORING NOT_APPLICABLE; FINDINGS PASS; PREREQ 3/3 (Phases 12, 14, 15); HUMAN_GATE NOT_APPLICABLE; 8 gates PASS over 143 edges and all 9 budgets. Two atomic packages compose the real existing authorities: `execution_routing.py` evaluates D-026's exact eight-tier preference order (deterministic tool through human governance), composing the real `CapabilityGraph`/`RepairBudgetLedger` and honestly resolving `NOT_CONFIGURED` for every tier whose owning authority is unbuilt (Phase 18/22) or has no live runtime instance (no configured provider anywhere in this repository); `product_generation.py` deterministically scaffolds a real, importable, executable module from a fully-resolved C-37 blueprint, refusing any blueprint carrying an unresolved question. A real orchestration-depth violation (`engineering.candidate`'s chain was already 4 of 4) was answered by a structural `Protocol` rather than an import, per ADR-0008. **3/3 requirements discharged**, all MANDATORY; cumulative verified 145 → **148**. No AI provider contacted, no Golden Factory Acceptance claimed (Phase 30's own obligation), no execution engine built. **Phase 17 unlocked.** |
| 17 | Visual Workflow Studio | **MACHINE-ACCEPTED** — verdict `PHASE_ACCEPTED_BY_MACHINE`, progression PERMITTED, on the first fully-corrected submission (`docs/acceptance/phase_17_report.json`, `phase_17_traceability.json`). C1–C6 PASS; EXTERNAL_RESULT PASS over all 12 recorded runs; **PROTECTED_CORE COMPLETE** (member `control.policy`, via `workflow_approval.py`/`policy_errors.py` — security review 245, adversarial review 788 (2 deselected: the expected mid-phase-advance anti-vacuity quiet pair, see below), full regression 2610 (3 deselected: the same 2 plus a pre-existing Windows-symlink-privilege gap unrelated to this phase), all exit 0); RESCORING NOT_APPLICABLE; FINDINGS PASS; PREREQ 3/3 (Phases 7, 8, 13); HUMAN_GATE NOT_APPLICABLE; 8 gates PASS over 156 edges and all 9 budgets. Eight atomic packages deliver C-20: the graph domain model, the persistence/revision store, the ADR-0004 derived execution plan, `control.policy`'s HUMAN APPROVAL enforced policy stop, the executor (real evidence for all 10 node kinds and 10 control constructs), the `surfaces.command` HTTP API, the frontend Visual Workflow Studio (hand-rolled canvas; a real T10 Playwright journey — compose/drag/connect/publish, reload persistence, fresh-context persistence, execution pause, the ARK-REQ-0330 negative path with `ARK-ERR-0133`, a real approval to `SUCCEEDED`), and the ARK-REQ-0331 coverage check (a structural cross-check, `test_contract_drift.py`'s own idiom, since `execution.workflow`, `surfaces.command` and `acceptance.engine` were all at their 40-of-40 public-surface ceiling). **9/9 requirements discharged**, all MANDATORY; cumulative verified 148 → **157**. `test_handoff_transition.py`'s two "current phase claims NOT STARTED / no package exists" anti-vacuity negative controls go quiet the moment a phase honestly advances past its first package — confirmed via git history as the identical, expected, non-blocking pattern for Phases 10, 11, 13 and 14, deselected accordingly in the recorded adversarial-review and full-regression runs above. No AI provider contacted, no graph-drawing library dependency added (a hand-rolled canvas proves the interaction real without one). **Phase 18 unlocked.** |
| 18 | Knowledge + Verified Components (C-28) | **MACHINE-ACCEPTED** (see narrative above). `docs/acceptance/phase_18_report.json`, `phase_18_traceability.json`. 4/4 requirements discharged, all MANDATORY. Phase 19 unlocked. |
| 19 | Import / Reverse Engineering / Rescue (C-29) | **MACHINE-ACCEPTED** (verdict `PHASE_ACCEPTED_BY_MACHINE`, progression PERMITTED; `docs/acceptance/phase_19_report.json`, `phase_19_traceability.json`). C1–C6 PASS; EXTERNAL_RESULT PASS over all 11 recorded runs; PROTECTED_CORE PASS (no member touched, normal profile); RESCORING NOT_APPLICABLE; FINDINGS PASS; PREREQ 3/3; **HUMAN_GATE: `HUMAN_GATE_4` recorded ACCEPTED** (`HGR-002`, scoped to this exact candidate — HEAD `ef5cb3b`, digest `sha256:6e41dd0d...` — by the human acceptance authority; the record's own known-gap note states this does NOT cover Phase 21's separate `HUMAN_GATE_4` obligation); all 8 architecture gates PASS over 169 edges. Five atomic packages deliver C-29 under `engineering.import`: the descriptor/tier-assignment/rescue-mode contract, static inspection with a structural no-execution proof, the TRUST-3 execution gate (composing `control.isolation` and `control.policy`'s `WorkflowApprovalGate` unmodified), the composed pipeline (`WorkspaceAuthority` reused via a structural `Protocol`, not an import), and the composed journey proving all seven VDC acceptance conditions. **5/5 requirements discharged**, all MANDATORY — `ARK-REQ-0115`, `0116`, `0161`, `0162`, `0348` — taking cumulative verified 161 → **166**. **Phase 20 unlocked.** |
| 20 | Database Migration Safety + Full Backup/Recovery | **MACHINE-ACCEPTED** (verdict `PHASE_ACCEPTED_BY_MACHINE`, progression PERMITTED; `docs/acceptance/phase_20_report.json`, `phase_20_traceability.json`). C1–C6 PASS; EXTERNAL_RESULT PASS over all 11 recorded runs; **PROTECTED_CORE COMPLETE** (`acceptance.engine` touched by the new ARK-REQ-0337 check; security review 245, adversarial review 802, full regression 2762, all exit 0); RESCORING NOT_APPLICABLE (first submission); DATA_LOSS_RISK PASS (9 declared revisions, zero flagged); FINDINGS PASS; PREREQ 2/2 (Phases 5, 6); **HUMAN_GATE: `HUMAN_GATE_6` recorded ACCEPTED** (`HGR-003`, scoped to this exact candidate — evidence commit `73a968f`, digest `sha256:b94d28bb...202c2c` — by the human acceptance authority; the record's own scope section states this does NOT cover any other phase or any real `APPLY_MIGRATION` runtime operation); all 8 architecture gates PASS over 183 edges. Three atomic packages deliver the nine-step VDC "Migration safety" sequence under `lifecycle.recovery`, completing C-03's deferred runtime half: `kernel.persistence` gains `run_upgrade` and `migration_impact.py` (an AST-based data-loss-risk analyser); `lifecycle.recovery`'s `MigrationSafetySequence` composes Phase 5's `RecoveryService`/`BackupRestore`, Phase 4's PDP and Phase 19's `WorkflowApprovalGate` — all reused unmodified; `acceptance.engine`'s `check_migration_data_loss_risk` independently re-derives the same analysis over the live shipping chain on every gate evaluation. **4/4 requirements discharged**, all MANDATORY — `ARK-REQ-0151`, `0152`, `0336`, `0337` — taking cumulative verified 166 → **170** and MANDATORY verified 165 → **169**. **Phase 21 unlocked.** |
| 21 | Plugins + Integrations + Research (C-30) | **MACHINE-ACCEPTED** (verdict `PHASE_ACCEPTED_BY_MACHINE`, progression PERMITTED; `docs/acceptance/phase_21_report.json`, `phase_21_traceability.json`). C1–C6 PASS; EXTERNAL_RESULT PASS over all 11 recorded runs; **PROTECTED_CORE COMPLETE** (`control.policy` touched by `pdp.py`'s two new additive methods, `decide_or_deny_unmapped` and `decide_network_egress`; security review 251, adversarial review 805, full regression 2903, all exit 0); RESCORING NOT_APPLICABLE (first submission); DATA_LOSS_RISK PASS (9 declared revisions, zero flagged, unrelated to this phase); FINDINGS PASS; PREREQ 2/2 (Phases 4, 9); **HUMAN_GATE: `HUMAN_GATE_4` recorded ACCEPTED** (`HGR-004`, scoped to this exact candidate — evidence commit `34b841b`, digest `sha256:ca469acf...5468f0f7` — by the human acceptance authority; the record's own scope section states this does NOT cover Phase 19, Phase 20, Phase 22, or any modified Phase 21 candidate, and mechanically cannot leak into or be satisfied by `HGR-002`); all 8 architecture gates PASS over 188 edges. Four atomic packages deliver C-30 under `engineering.plugin`: the manifest domain contract composed with Phase 3's `PluginLifecycle` machine and Phase 4's operation-class vocabulary; a total, non-raising unmappable-action-is-DENY resolution added to the real PDP; the TRUST-4 execution gate composing `control.isolation`/`control.policy`'s `WorkflowApprovalGate` unmodified, bound per execution; and Research, official-sources-first ordering with real PDP-governed network egress including a real Local-Only DENY — all reused canonical authorities, zero duplicated. **5/5 requirements discharged**, all MANDATORY — `ARK-REQ-0117`, `0163`, `0164`, `0165`, `0166` — taking cumulative verified 170 → **175**. `ARK-REQ-0167` (OPTIONAL) not attempted; never blocks. **Phase 22 unlocked.** |
| 22 | Local AI + Model Laboratory | **MACHINE-ACCEPTED** — verdict `PHASE_ACCEPTED_BY_MACHINE`, progression PERMITTED, on the first and only submission (`docs/acceptance/phase_22_report.json`, `phase_22_traceability.json`). C1–C6 PASS; EXTERNAL_RESULT PASS over all 10 recorded runs; PROTECTED_CORE PASS (no member touched — `kernel.contracts`, the only canonical context this phase modifies, is not Protected Core, confirmed directly against `AUTHORITY_MAP.yaml` — normal profile applies); RESCORING NOT_APPLICABLE (first submission); DATA_LOSS_RISK PASS (9 declared revisions, zero flagged, unrelated to this phase); FINDINGS PASS; PREREQ 2/2 (Phases 4, 9); HUMAN_GATE NOT_APPLICABLE (Matrix Gate column empty for Phase 22); all 8 architecture gates PASS over **191** edges. `engineering.localai` delivers a real local-AI adapter architecture under its own already-declared concern (`local_model_adapters`), claiming no new contract family (`CONTRACT_INVENTORY.md`'s 37 rows include none owned by this context, and neither discharged requirement's evidence type is contract-category): `LocalRuntimeAdapter`, one structural Protocol satisfied by two real, transport-distinct, loopback-only adapters (Ollama's native API, an OpenAI-compatible local HTTP surface) proving "no hard dependency on one runtime" by construction (`ARK-REQ-0016`); read-only host/runtime/hardware probes and a hardware-aware, accelerator-independent suitability decision, genuinely `CapabilityGraph`-exposed capability (earned `configured_state`, no shadow-registry field) proving Phase 16's unmodified `execution_routing.select_execution_tier` resolves the local tier eligible, one real bounded local inference call against this development host's installed Ollama server, and the real PDP's Local-Only negative control (`ARK-REQ-0129`, genuinely APPLICABLE on this host — a real NVIDIA GPU and a real, loopback-reachable Ollama runtime were both probed directly, not assumed). `ARK-REQ-0130` is honestly NOT_APPLICABLE: no dataset-verification authority exists anywhere in this repository, so `dataset.verified_count` is 0 and no fine-tuning/distillation capability is attempted. Decomposes `kernel.contracts.results` (ADR-0008, the `error_base.py` → `contract_violation_base.py` shape): `HonestState` now lives in `honest_state.py`, re-exported unchanged, because `results.py` was already at its fan-in ceiling (15 of 15) before this phase's new importers — a real, measured budget hit and repaired during development, not requested as an exception. **2/2 requirements discharged**, 1 MANDATORY (`ARK-REQ-0016`) and 1 CONDITIONAL (`ARK-REQ-0129`) — taking cumulative verified 175 → **177**. No Provider/Model Registry entry, capability-graph instance, or routing-tier change is claimed in production source; Phase 16's `execution_routing.py` is composed unmodified. **Phase 22B unlocked** |
| 22B | Recovery Supervisor + Core Rollback Verification | **MACHINE-ACCEPTED** — verdict `PHASE_ACCEPTED_BY_MACHINE`, progression PERMITTED, on the first and only submission (`docs/acceptance/phase_22B_report.json`, `phase_22B_traceability.json`). C1–C6 PASS; EXTERNAL_RESULT PASS over all 10 recorded runs; **PROTECTED_CORE COMPLETE** (three Protected Core contexts touched — `control.policy` via `pdp.py`/`policy_contract.py`, `lifecycle.release` via `stable_pointer.py`, `lifecycle.recovery` via `recovery_supervisor.py`; security review 253, adversarial review 801, full regression 3000, all exit 0); RESCORING NOT_APPLICABLE (first submission); DATA_LOSS_RISK PASS (9 declared revisions, zero flagged); FINDINGS PASS; PREREQ 4/4 (Phases 5, 6, 13, 20); HUMAN_GATE NOT_APPLICABLE (Matrix Gate column empty for Phase 22B); all 8 architecture gates PASS over **197** edges. Five atomic packages deliver the minimal Stable-revision pointer primitive (D-017, `lifecycle.release`), the PDP's real conditional `ROLLBACK_STABLE` grant confined to the canonical invoker (`control.policy`), the independent deterministic Recovery Supervisor that is the sole production caller of it (`lifecycle.recovery`, composing the pointer and the C-14/C-15 evidence plane through two ADR-0008 structural Protocols — `RollbackAuthorization`, `EvidenceSink` — after running the real architecture gate found `evidence.audit`'s own chain already at `max_orchestration_depth` 4 of 4 and `control.policy.pep`/`.policy_contract` already at `max_fan_in_per_module` 15 of 15), and the composed VDC "Recovery Supervisor" journey (bad candidate → launch → health failure → known-good rollback → failure record → stable available) proving all eleven requirements against real SQLite, the real PDP and the real evidence plane. **11/11 requirements discharged**, all MANDATORY — `ARK-REQ-0135`, `0136`, `0154`–`0160`, `0338`, `0339` — taking cumulative verified 177 → **188**. No Self-Evolution capability is built (Phase 23's own obligation); no full release/supply-chain/deployment system is built (Phase 26, D-017 explicit); no production surface exposes `RecoverySupervisor` anywhere. **Phase 23 unlocked** |
| 23 | Self-Evolution | **MACHINE-ACCEPTED** (verdict `PHASE_ACCEPTED_BY_MACHINE`, progression PERMITTED; `docs/acceptance/phase_23_report.json`, `phase_23_traceability.json`). C1–C6 PASS; EXTERNAL_RESULT PASS over all 9 recorded runs; **PROTECTED_CORE COMPLETE** (`acceptance.engine` touched via `campaign_record_shape.py`; security review 254, adversarial review 807, full regression 3078, all exit 0); RESCORING NOT_APPLICABLE (first submission); DATA_LOSS_RISK PASS; FINDINGS PASS; PREREQ 3/3 (Phases 13, 16, 22B); **HUMAN_GATE: `HUMAN_GATE_2` recorded ACCEPTED** (`HGR-005`, scoped to this exact candidate — candidate freeze commit `54f9e8b`, digest `sha256:3ad6f5fe...1eb5c00e` — by the human acceptance authority; the record's own scope section states this does NOT cover any other phase, any modified candidate, or any real future `CORE_PROMOTION` runtime operation); all 8 architecture gates PASS over **202** edges. Eight atomic packages deliver C-33 under `lifecycle.evolution`/`lifecycle.recovery`/`control.policy`/`acceptance.engine`: the campaign declaration wired to the pre-existing `declaration_guard` (Package 1, `ARK-REQ-0139`); the campaign ledger's successor-eligibility and honest terminal-state derivation (Package 2, `ARK-REQ-0140`/`0141`/`0142`); a real restorable snapshot proved before every core-upgrade candidate (Package 3, `ARK-REQ-0137`); a real scoped `HUMAN_GATE_2` grant feeding the pre-existing `gate_2_guard` (Package 4, `ARK-REQ-0138`); the composed promotion reaching the real, unmodified `StableRevisionPointer.promote` (Package 5, `ARK-REQ-0134`'s pipeline half); structural negative/adversarial proofs of no direct mutation and no self-approval (Package 6, `ARK-REQ-0134`'s live-core half, `ARK-REQ-0359`); the campaign-record shape check (Package 7, `ARK-REQ-0360`); and the composed VDC journey, C-33 contract document, C-17 report and traceability record (Package 8, candidate frozen). **9/9 requirements discharged**, all MANDATORY — `ARK-REQ-0134`, `0137`–`0142`, `0359`, `0360` — taking cumulative verified 188 → **197**. No live self-evolution campaign is run against ARKALI's own source; no Product Evolution SDK, Golden Factory or Golden Repair capability is built; no production surface exposes any Phase 23 module. **Phase 24 unlocked** |
| 24 | Generated Product Evolution SDK (C-36) | **MACHINE-ACCEPTED** (verdict `PHASE_ACCEPTED_BY_MACHINE`, progression PERMITTED; `docs/acceptance/phase_24_report.json`, `phase_24_traceability.json`). C1–C6 PASS; EXTERNAL_RESULT PASS over all 10 recorded runs; PROTECTED_CORE normal profile (no protected-core member touched); RESCORING NOT_APPLICABLE (first submission); DATA_LOSS_RISK PASS; FINDINGS PASS; PREREQ 2/2 (Phases 13, 16); **HUMAN_GATE: `HUMAN_GATE_3` recorded ACCEPTED** (`HGR-006`, scoped to this exact candidate — candidate freeze commit `42390ee0fc71037f9bf96f493f909c005a26dd4a`, digest `sha256:ecf43065fcdedb13fceece933145c58085363cda775b67e41873f8e286c06dc1` — by the human acceptance authority; the record's own scope section states this does NOT cover any other phase, Phase 25, any modified Phase 24 candidate, or any real future `PROMOTE_CHILD_PRODUCT` runtime operation, which requires its own separate `RUNTIME_OPERATION`-scope grant); all 8 architecture gates PASS over **202** edges (unchanged since the candidate froze — the acceptance commits add no further production code). Seven atomic packages deliver C-36 under `lifecycle.evolution`, all reusing every Protected Core authority they touch unmodified: child-product identity and the three canonical modes, `uses_evolution_sdk` the single eligibility gate (`ARK-REQ-0131`); per-product version lineage mirroring `StableRevisionPointer`'s discipline without sharing its instance (`ARK-REQ-0132`/`0358`); working-copy isolation reusing Phase 12's `WorkspaceAuthority` (`ARK-REQ-0132`/`0358`); scoped `HUMAN_GATE_3` promotion reusing the `HUMAN_GATE_2`/`CORE_PROMOTION` grant mechanism unmodified, plus ADR-0010 (`ARK-REQ-0132`/`0358`); a separate rollback authority requiring no fresh grant (`ARK-REQ-0132`/`0358`); structural AST proof of no direct Stable/live mutation and no self-approval; and the composed real-authority journey (real filesystem, real SQLite, real scoped grants, two full evolution cycles, a real `campaign_id` collision defect found and fixed), `docs/contracts/product_sdk.md`, the C-17 report and traceability record. Two real architecture-budget violations were found by running the gate and repaired by decomposition/privatization, not exception. **4/4 requirements discharged**, all MANDATORY — `ARK-REQ-0131`, `0132`, `0133`, `0358` — taking cumulative verified 197 → **201**. No live goal-to-child-product generation pipeline exists (DEF-009, out of this phase's canonical scope); no AI-provider-driven multi-file product generation (Phase 30's own obligation); no production surface exposes any Phase 24 module; no change to ARKALI's own `StableRevisionPointer` or `RecoverySupervisor`. **Phase 25 unlocked** |
| 25 | Operations + Hardware Intelligence (C-34) | **MACHINE-ACCEPTED** — verdict `PHASE_ACCEPTED_BY_MACHINE`, progression PERMITTED, on the first submission (`docs/acceptance/phase_25_report.json`, `phase_25_traceability.json`). C1–C6 PASS; EXTERNAL_RESULT PASS over all 10 recorded runs; **PROTECTED_CORE COMPLETE** (`control.policy` touched via `pdp.py`'s additive `decide_computer_use` and `secret_reference.py`'s additive `redact_raw_secrets`; security review 284, adversarial review 111, full regression 3276, all exit 0); RESCORING NOT_APPLICABLE (first submission); DATA_LOSS_RISK PASS; FINDINGS PASS; PREREQ 3/3 (Phases 7, 9, 11); HUMAN_GATE NOT_APPLICABLE (Matrix Gate column is `—`); all 8 architecture gates PASS over **219** edges (up from 202). Seven atomic packages deliver C-34 under `surfaces.operations`/`control.policy`/`surfaces.command`: real hardware/runtime/storage telemetry, re-derived live on every call (Package 1, `ARK-REQ-0168` partial); Computer-Use action authorization composed exclusively through the existing, unmodified PDP/PEP (Package 2, `ARK-REQ-0170`); permission-aware process/file/browser execution boundaries (Package 3, `ARK-REQ-0170`); AST-based negative/adversarial structural proofs (Package 4, `ARK-REQ-0170`); Source Intelligence Export + secret-redacted AI Review Bundle (Package 5, `ARK-REQ-0169`/`0357`); the no-fabricated-evidence structural proof plus CONDITIONAL hardware-cost/quality-latency dimensions (Package 6, `ARK-REQ-0218`/`0356`/`0395`); the real Command Center HTTP surface with real Level 3 verification against this real host's real hardware (Package 7, `ARK-REQ-0168`/`0169`/`0170`/`0355`–`0357`); and the composed VDC journey, `docs/contracts/operations_api.md`, the C-17 report and traceability record (Package 8, candidate frozen). Two real `max_orchestration_depth` violations and one real `max_public_surface_per_context` violation were found by running the gate and repaired by Protocol-based decomposition/privatization, not exception. **8/8 requirements discharged** — 6 MANDATORY, 2 CONDITIONAL (both genuinely applicable and PASS on this real host) — `ARK-REQ-0168`, `0169`, `0170`, `0218`, `0355`, `0356`, `0357`, `0395` — taking cumulative verified 201 → **209**. No live goal-to-product pipeline exists (DEF-009, permanently open, out of this phase's canonical scope); no raw command execution reachable over HTTP; no live provider/agent/worker registry; no live browser automation; no new PDP/PEP/policy authority/operation class; no frontend increment. **Phase 26 unlocked** |
| 26 | Release / Supply Chain / Deployment (C-31) | **MACHINE-ACCEPTED** — verdict `PHASE_ACCEPTED_BY_MACHINE`, progression PERMITTED, on the first submission (`docs/acceptance/phase_26_report.json`, `phase_26_traceability.json`). C1–C6 PASS; EXTERNAL_RESULT PASS over all 10 recorded runs; **PROTECTED_CORE COMPLETE** (`lifecycle.release` touched — `control.policy` itself untouched this phase; security review 284, adversarial review 56, full regression 3332, all exit 0); RESCORING NOT_APPLICABLE (first submission); DATA_LOSS_RISK PASS; FINDINGS PASS; PREREQ 3/3 (Phases 6, 13, 22B); HUMAN_GATE NOT_APPLICABLE (Matrix Gate column is `—`); all 8 architecture gates PASS over **222** edges (up from 219). Six atomic packages deliver C-31 under `lifecycle.release`: `ReleaseAuthority`, the first real production caller of the already-declared `Release` machine, composing the unmodified `StableRevisionPointer` read-only (Package 1, `ARK-REQ-0007` partial); real C-14 provenance registration via a structural `ArtifactRegistrar` Protocol (Package 2, `ARK-REQ-0015`/`0350` partial); a real, deterministic SBOM parsed from this repository's own manifests (Package 3, `ARK-REQ-0124` partial, `ARK-REQ-0369`); a real, local, deterministic suspicious-package review (Package 4, `ARK-REQ-0125`); supply-chain negative/adversarial proofs (Package 5); the final manifest composition plus the real `HUMAN_GATE_7` singleton wiring closing "bypassed acceptance"/"bypassed release policy" (Package 6, `ARK-REQ-0007` completing); and the composed VDC journey, `docs/contracts/release_manifest.md`, the C-17 report and traceability record (Package 7, candidate frozen). A genuine mid-Package-6 canonical-discovery correction is recorded in full in that package's own commit: an early `release_gate.py` draft built a scoped grant lookup mirroring `HUMAN_GATE_2`/`3`, discarded once `BUILD_PROTOCOL.md`'s own Canonical HUMAN GATES list and `SINGLETON_GATES` showed Gate 7 ("Final Production Release") is a one-time, unscoped project event like Gate 1. Two real `max_fan_in_per_module` violations, one real `max_orchestration_depth` violation and one real `max_parameters_per_public_function` violation were found by running the gate and repaired by decomposition/Protocol-decoupling/parameter-grouping, never exception. **6/6 requirements discharged**, 5 MANDATORY, 1 CONDITIONAL (genuinely applicable and PASS on this real repository) — `ARK-REQ-0007`, `0015`, `0124`, `0125`, `0350`, `0369` — taking cumulative verified 209 → **215**. No real deployment, no real code signing, no live external SBOM query, no live goal-to-product pipeline (DEF-009, permanently open, out of this phase's canonical scope); `HUMAN_GATE_7` has never been recorded in this real repository, so `RELEASED` remains genuinely unreachable in production today. **Phase 27 unlocked** |
| 27 | Command Center Consolidation + Skill Modes (C-35) | **MACHINE-ACCEPTED** — `python scripts/run_phase_gate.py 27 28` returned `PHASE_ACCEPTED_BY_MACHINE`, progression PERMITTED, on the first submission. C1–C6 PASS; 10/10 recorded runs declare NO_EXTERNAL_RESULT; normal profile (no Protected Core member touched); FINDINGS and PREREQ PASS; HUMAN_GATE NOT_APPLICABLE; 8 architecture gates PASS over 222 edges and all 9 budgets. Five atomic packages consolidate the existing real Project Registry and Workflow Studio slices into one professional shell, present all fourteen canonical areas honestly, add Beginner/Professional/Expert presentation modes, hide internal identifiers progressively, and re-prove both slices through a real Vite-production/FastAPI/SQLite/Chromium journey. **2/2 MANDATORY requirements discharged** (`ARK-REQ-0176`, `0177`), cumulative verified **215 → 217**. DEF-009 remains open; no backend capability, external AI dependency or fake preview was added. **Phase 28 unlocked** |
| 28 | Tauri Desktop | **MACHINE-ACCEPTED** — `python scripts/run_phase_gate.py 28 29` returned `PHASE_ACCEPTED_BY_MACHINE`, progression PERMITTED, on the first submission. C1–C6 PASS; all 11 recorded runs declare NO_EXTERNAL_RESULT; normal profile (no Protected Core member touched); FINDINGS and PREREQ PASS; HUMAN_GATE NOT_APPLICABLE; 8 architecture gates PASS over 222 edges and all 9 budgets. The existing React/Vite Command Center remains the single UI and the existing FastAPI composition root remains the single backend. A minimal Tauri 2 host, fixed bundled Python runtime, app-data database lifecycle, fail-closed native boundary, real WebView2 journey and generic NSIS bundle proof discharge **2/2 MANDATORY requirements** (`ARK-REQ-0010`, `0179`), cumulative verified **217 → 219**. `ARK-REQ-0180` is NOT_APPLICABLE under ERR-005's Phase-28-only nested-field interpretation. DEF-009 remains open; no external AI dependency or fake product/live-preview capability was added. **Phase 29 unlocked** |
| 29 | Installer + Recovery Supervisor Integration | **UNLOCKED — IMPLEMENTATION COMPLETE, ACCEPTANCE CANDIDATE PENDING.** Prerequisites Phase 22B, 26 and 28 are MACHINE-ACCEPTED. Exact `ARKALI_Setup.exe`, real current-user install/upgrade/uninstall/reinstall and C-32 Recovery Supervisor composition are implemented; machine acceptance has not yet been recorded |
| 30 … 37 | all subsequent phases | NOT_STARTED — reachable in canonical order |

## What exists

- Canonical document set (4 files), unmodified by Phase 0.
- **Phase 0A architecture package (8):** `ARCHITECTURE.md`, `SECURITY_ARCHITECTURE.md`, `STATE_MACHINES.md`, `EXECUTION_AND_CAPABILITY.md`, `CONTRACT_INVENTORY.md` (36 families), `VERIFICATION_ARCHITECTURE.md` (test architecture + evidence graph strategy), `IMPLEMENTATION_DEPENDENCY_MATRIX.md`, `CONTRADICTION_ANALYSIS.md`.
- **Phase 0B governance package (5):** `REQUIREMENT_REGISTER.md` (313 entries), `AUTHORITY_MAP.yaml` (machine-readable, parses), `GOLDEN_REPAIR_CORPUS_DEFINITION.md`, `PHASE_GATE_CHECKER.md`, `ADR_INDEX.md` (9 ADRs, all **ACCEPTED** under HUMAN GATE 1, now immutable).
- Build state artifacts (this file and siblings), `EVIDENCE_INDEX.md`, `HUMAN_GATE_RECORDS.md`.
- **Executable governance (Phase 2):** `backend/arkali/` now contains 15 implementation modules across 8 bounded contexts — error taxonomy, honest states, correlation envelope, persistence schema contract, requirement register parser, authority map parser, 8 architecture gates, phase report, gate verdict and the deterministic Phase Gate Checker. Entry point: `scripts/run_phase_gate.py`.
- **Validation tooling (5):** `scripts/check_phase_graph.py`, `check_phase_graph_negative.py`, `check_phase0_deliverables.py`, `check_repository_structure.py`. Validators, not application source code — they analyse documents and repository structure.
- **Phase 1 repository bootstrap (candidate, not accepted):** `backend/` (31 bounded-context packages + 9 layer groupings, `pyproject.toml`, `tests/`, `alembic/versions/`), `frontend/` (workspace config + real `package-lock.json`), `src-tauri/`, `golden/`, `release/` roots.

## What does not exist

- **No application capability beyond the Command Center slice.** One FastAPI application (`surfaces.command`, seven routes) and one React page (the Project Registry) exist. No other module, route, dashboard or surface does.
- **No Python lockfile exists** (no lock tool on this machine). Node dependencies **are** installed: `npm ci` from the tracked `package-lock.json`, then the Vitest/Testing-Library tier, `@playwright/test` and `@types/node` added through real npm tooling, which updated `package.json` and `package-lock.json` as `INSTALL_DEPENDENCY`/`LOCKFILE_BOUND` requires. Nothing was hand-written into the lockfile. The Chromium runtime was downloaded by `npx playwright install chromium` and is not vendored into the repository.
- **No browser/E2E evidence beyond the Project Registry, and none outside Chromium.** T10 is configured and green for one capability: 9 Playwright tests against the production build, a live API and a real SQLite file. Firefox and WebKit are not installed and no cross-browser claim is made. No other capability has a browser journey, because no other capability has a frontend. The 23 jsdom component tests are component evidence and are never reported as T10.
- **The first execution surface now exists.** Phase 4's "no execution surface" statement is superseded for `surfaces.command` only: every route enforces through the Phase 4 PEP and is audited. Of the other five canonical paths, **`durable` (Phase 7) and `scheduler` (Phase 8 Package 1) now exist and both enforce through a real PEP** — the derived control in `test_pep_and_bypass.py` requires it of every execution package that is built, and passes. **`sandbox`, `workflow` and `operations` remain absent**, and the same control asserts that absence rather than assuming it. End-to-end bypass resistance across all six (ARK-REQ-0325, 0347) is still Phase 31 and is not claimed.
- **No Recovery Supervisor, Stable rollback or `ROLLBACK_STABLE` capability.** Phase 5 delivered *minimal* backup/restore only; the Supervisor is Phase 22B and `ROLLBACK_STABLE` remains DENY for every actor. `ARK-REQ-0153` and `ARK-REQ-0335` **are discharged** — Phase 5 is accepted and its traceability record claims both SATISFIED under C6 — but what they discharge is the minimal capability, not the Supervisor.
- No Phase 20 migration-safety workflow: restore requires an exactly matching schema revision rather than migrating across one.
- **The Evidence Plane and Acceptance Engine exist** (Phases 6 and 13, accepted): `evidence.artifact` registers content-addressed artifacts and provenance, `evidence.audit` owns the append-only chain and recomputes integrity, and C-16 derives the immutable graph, applicability, coverage, Passport accounting and capability verdict without another store, denominator or release authority. No release verdict exists. Release manifests with cryptographic hashes, SBOM and signing remain C-31 in Phase 26. No browser or external-provider evidence is claimed. There is no artifact deletion, garbage collection or compaction, by design.
- **One acceptance guard is weaker than Phase 6's own traceability record.** C-15 is mapped onto ARK-REQ-0004 in full, but the contract behind check C6 requires only that a SATISFIED claim *name* an implementation and an evidence source — it has no per-contract granularity. Proven mechanically, not assumed. No control was added, because no accepted phase's traceability record names a contract id at all and a repository-wide rule demanding one would retroactively invalidate Phases 2–5; strengthening the traceability contract is an Acceptance Engine change and belongs to Phase 13.
- No runtime database is committed. `.gitignore` excludes `*.db`, `*.db-wal`, `*.db-shm`; migrations are committed.
- **No Rust/Tauri manifest** — toolchain absent; nothing fabricated.
- No Phase Gate Checker implementation (Phase 2).
- No contract **schema files** — the 36 families are inventoried (Phase 0A); schemas are Phase 2 (DEF-008).
- No **instantiated** Golden Repair corpus. The definition is delivered (Phase 0B, ARK-REQ-0090/0091); instantiation is ARK-REQ-0187/0188 at Phase 30 and is **NOT_APPLICABLE** until an accepted Golden Product exists — it is not a closed requirement.

## Honest state of Phase 0 verification

| Item | State | Reason |
|---|---|---|
| Architecture package produced | PASS | documents exist and are internally consistent |
| Requirement register produced | PASS | 313 entries, 0 duplicates, mechanically counted |
| Phase 0A deliverables vs Build Protocol list | PASS | 16/16, count derived from the canonical list, each content-probed (EV-0005) |
| Phase 0B deliverables vs Build Protocol list | PASS | 11/11, count derived from the canonical list, each content-probed (EV-0005) |
| Phase dependency graph executable | PASS | 9/9, all four edge classes, negative control fails as required (EV-0004, EV-0004N) |
| Golden Repair corpus instantiated | NOT_APPLICABLE | no accepted Golden Product exists; ARK-REQ-0187/0188 open at Phase 30 |
| Authority map machine-readable | PASS | parses; 0 duplicate concern authorities |
| Architecture gates executed | NOT_TESTED | no code exists to analyse; gates run from Phase 2 |
| Phase Gate Checker executed | NOT_TESTED | implemented in Phase 2 |
| Any test suite executed | NOT_APPLICABLE | Phase 0 produces no implementation |
| Provider configured | NOT_CONFIGURED | no provider required or used in Phase 0 |
| Isolation backends probed | NOT_TESTED | probe runs at Phase 4 |
| Human Gate 1 | **ACCEPTED** | granted by the human acceptance authority after independent inspection and independent execution of the validators; record `HGR-001` |

## Cross-session handoff (mandatory maintenance)

`ARKALI_HANDOFF.md` at the repository root is the cross-session bootstrap index.
It is **not an authority** and can never override the canonical set, the
requirement register, the authority map, the accepted ADRs, the human-gate
records, this file, `PHASE_HISTORY.md` or accepted Git history. On disagreement
the repository wins and continuation stops with `HANDOFF_DRIFT`.

**It MUST be regenerated from authoritative repository state after:**

- every machine-accepted phase;
- every Human Gate decision;
- every accepted governance erratum;
- Stable Core promotion;
- any rollback;
- any new BLOCKER or HIGH that changes continuation strategy;
- any change to NEXT EXACT ACTION.

Refresh means re-derive from the repository, never hand-edit to match a
recollection. Verify with `python scripts/check_handoff.py` (exit 0 required).

## Next exact action

**Begin Phase 14 — Repair / Root Cause / Convergence (C-26).** Read the seven rows returned by `RequirementRegister.for_phase("14")` and derive C-26 from `CONTRACT_INVENTORY.md` before implementation. Preserve the accepted Phase 13 evidence graph and policy boundaries; repair fingerprints and all six budget dimensions must be bounded, evidence-driven and confined to the candidate lifecycle.

**Do not re-run the Phase 13 gate.** A second run against an accepted phase requires GOV-001 re-scoring authority; the first submission is final.

*(superseded guidance retained for continuity)*

**Phase 11 Atomic Package 4 — the composed journey, traceability, the C-17 report and ONE gate run.** Done. Packages 1–3 were delivered and committed; Package 4 composed the real graph and twin mechanisms, recorded two claims, and received `PHASE_ACCEPTED_BY_MACHINE` on its single gate run.

*(superseded guidance retained for continuity)*

**Phase 11 Atomic Package 2 — the graph set itself (`ARK-REQ-0066`, evidence `unit, integ`).** Done. Package 1 was delivered and committed. Package 2 must build the graphs `ARCHITECTURE.md` §11 specifies, for Python via native AST, and satisfy **C-24's declared verification: rebuild determinism**. `CONTRACT_INVENTORY.md` row 24 names that as the contract's evidence responsibility, so rebuilding from identical source must produce an identical graph — `NonDeterministicRebuild` (`ARK-ERR-0102`) already exists for the refusal, and `kernel.contracts.content_address` is the canonical way to address a rebuild.

**The twin is a DERIVED store and never an authority** (`ARCHITECTURE.md` §11), "rebuildable from source plus the owning authorities". `DerivedStoreTreatedAsAuthority` (`ARK-ERR-0101`) exists for that refusal. A `GRAPH` contract is where a second store of somebody else's concern is easiest to introduce by accident, and the `shadow_registry` gate scans **24** consumer modules.

**Then Package 3:** the Digital Twin composing the nine views (`ARK-REQ-0067`, evidence `integ`). **Then Package 4:** the composed journey, traceability, the C-17 report and **one** gate run.

*(superseded guidance retained for continuity)*

**Begin Phase 11 — Code Intelligence + Digital Twin (C-24).** Done for Package 1. Phase 11 was unlocked and not started. Its sole prerequisite, Phase 10, is MACHINE-ACCEPTED, and the matrix Gate column is empty, so no human gate applies. Derive the requirement set with `RequirementRegister.for_phase("11")` rather than assuming anything carries over — it returns **two** MANDATORY entries, `ARK-REQ-0066` and `ARK-REQ-0067`, both owned by `engineering.codeintel`. Phase 10's five are discharged and closed.

**C-24 is the first `GRAPH` contract in this build.** `CONTRACT_INVENTORY.md` row 24 declares its kind as `GRAPH` with **ADDITIVE** compatibility, unlike every `INT` contract delivered so far — so it owes a real graph structure rather than an interface, and ADDITIVE means a later revision may add but not remove. Read that row and `MS §Code Intelligence and Digital Twin` before designing.

**Perform the mandatory section-12 handoff refresh first**, as a SEPARATE commit immediately after the Phase 10 acceptance commit. Re-derive section 3 from the gate mechanism rather than transcribing it (F-0047), and add the Phase 11 row so `current_work_phase()` does not return `None` — marking a phase accepted without adding its successor's row left nine governance controls red at Phase 9B, and the same trap applies here.

**Do not re-run the Phase 10 gate.** Like Phases 2, 3, 8, 9 and 9B, a second run against an accepted phase returns `AWAITING_RESCORING_AUTHORITY` under GOV-001. That is the rule working, not a fault.

*(superseded guidance retained for continuity)*

**Phase 10 Atomic Package 4 — the composed journey, traceability, the C-17 report and ONE gate run.** Done. Packages 1, 2 and 3 were delivered and committed. Package 4 must produce the composed integration journey over real repository mechanisms, `phase_10_traceability.json` carrying **five real claims** (`ARK-REQ-0050`, `0051`, `0054`, `0055`, `0231`, all MANDATORY), the C-17 report, and exactly one gate run.

**The candidate carries the STRONGER VERIFICATION PROFILE.** `control.policy` is Protected Core and Package 3 changed it, so `select_profile` will derive `PROTECTED_CORE` from the change set across all four packages — never from Package 4's files alone, which would understate it. The report must therefore carry real execution records for **security review**, **adversarial review** and **full regression**, each naming its category in the run summary and each at exit 0, or the PROTECTED_CORE check returns INCOMPLETE.

**Every recorded run owes an `external_result` declaration (D-023)**, and no provider may be contacted. **Read F-0045 before writing the report**: re-run every recorded command against the exact candidate after the last file change.

*(superseded guidance retained for continuity)*

**Phase 10 Atomic Package 3 — `ARK-REQ-0050` and `ARK-REQ-0051`.** Done. Packages 1 and 2 were delivered and committed. Package 3 must deliver the two requirements neither contract covers:

- **`ARK-REQ-0050`** — agents are roles, providers are backends, separation maintained. Owner `engineering.agent`, evidence **`arch`**. `MS §Provider and Agent separation` is the source: "A Backend Engineer role may use Claude, GPT, Gemini or a local model." The architecture obligation is that a role never becomes a provider store — the Provider/Model Registry is the sole authority for provider identity, model identity, configuration, health, availability, cost metadata and fallback, and the `shadow_registry` gate already reads consumer source.
- **`ARK-REQ-0051`** — agents cannot mutate canonical requirements or self-accept output. Owner **`control.policy`**, evidence **`sec, prop`**. This is NOT `engineering.agent`'s to discharge, and it must be enforced in the policy path, not asserted in the agent context.

**Then Package 4:** the composed journey, traceability, the C-17 report and one gate run.

*(superseded guidance retained for continuity)*

**Phase 10 Atomic Package 2 — C-23, the context package and its provenance (`ARK-REQ-0055`).** Done. Package 1 was delivered and committed. Package 2 must deliver the Context Compiler's output contract: only relevant context is sent, and **context provenance is recorded**. `CONTRACT_INVENTORY.md` row 23 assigns its verification as the **no-secret assertion**, so a context package must be structurally incapable of carrying a raw secret — `control.security`'s `SecretReference` handle is the C-09 precedent, and a raw-secret shape scan is the C-11 precedent. C-22 currently carries a context package as a bare reference; Package 2 is where that reference resolves.

**Still to come after that:** `ARK-REQ-0050` (agents are roles, providers are backends — evidence `arch`) and `ARK-REQ-0051` (agents cannot mutate canonical requirements or self-accept output — owned by **`control.policy`**, evidence `sec, prop`), then the composed journey, traceability, the C-17 report and one gate run.

**Phase 10 has a NON-ZERO denominator — five requirements.** `ARK-REQ-0050`, `ARK-REQ-0051`, `ARK-REQ-0054`, `ARK-REQ-0055`, `ARK-REQ-0231`, all MANDATORY, none discharged.

*(superseded guidance retained for continuity)*

**Begin Phase 10 — Agent Runtime + Harness Engineering (C-22, C-23).** Done for Package 1. Phase 10 was unlocked and not started. `IMPLEMENTATION_DEPENDENCY_MATRIX.md` row 10 records prerequisites **9 and 9B**, both now MACHINE-ACCEPTED, and an empty Gate column, so no human gate applies; the next human gate remains **GATE 2** at Phase 23. Derive the requirement set with `RequirementRegister.for_phase("10")` rather than assuming anything carries over — Phase 9B's three are discharged and closed.

**Perform the mandatory section-12 handoff refresh first**, as a SEPARATE commit immediately after the Phase 9B acceptance commit. Re-derive section 3 from the gate mechanism rather than transcribing it (F-0047), and carry the newly accepted phase row without leaving transition residue in either the phase table or the continuation brief (F-0048): cumulative verified is now **86**, `unlocked_phase` and `next_exact_action_phase` both become **10**, and `verified_by_phase` gains `"9B": 3`.

**Do not re-run the Phase 9B gate.** Like Phases 2, 3 and 8, a second run against an accepted phase returns `AWAITING_RESCORING_AUTHORITY` under GOV-001. That is the rule working, not a fault.

**What Phase 9B leaves ready, and what it deliberately did not build.** The Capability Graph is activated: `can_perform` resolves every external `*_ref` at query time through its owning authority, reads `configured_state`, walks `prerequisites` transitively, and answers `PASS` or `NOT_CONFIGURED` and nothing else — which is what keeps the unchanged accepted Phase 8 predicate safe. Nothing is cached at any layer. **No provider runtime, no network call and no external-provider result exists**, and no capability is declared configured with real production identities; every affirmative in the evidence composes composition-root doubles. A later phase supplying a real provider runtime inherits a graph that will resolve against it without modification.

**Budget facts carried in, unchanged.** `max_orchestration_depth` is at **4 of 4** on `surfaces.command → execution.durable → control.policy → kernel.contracts`; `kernel.contracts.errors` and `kernel.contracts.state_machine` are both at **15 of 15** fan-in, so a context needing a base error type must import `kernel.contracts.error_base`; `acceptance.engine` public surface is at **40 of 40**. Measure before adding and decompose under ADR-0008; do not request a GATE 8 exception.

*(superseded guidance retained for continuity)*

**Phase 9B Atomic Package 3 — composition, traceability, the C-17 report and the gate.** Done: the composed journey, `phase_9B_traceability.json` carrying three real claims, `phase_9B_report.json`, and the single gate run returning `PHASE_ACCEPTED_BY_MACHINE`.

**Phase 9B's denominator is non-zero.** Unlike Phase 8, C6 refuses an empty record against a non-empty denominator just as hard as the reverse; derive the claims from `RequirementRegister.for_phase("9B")`, never carry anything over.

**Every recorded run owes an `external_result` declaration (D-023).** No provider was contacted anywhere in 9B and none may be. `EXTERNAL_PROVIDER_RESULT` is refused by derivation until a later canonical phase supplies a verifiable binding; declare honest absence.

**Report the honest capability position.** Production admission can now succeed in principle — Package 2 made `PASS` reachable — but no capability is actually declared configured with real provider identities in production, and no provider runtime exists. State plainly what is proven (the mechanism, through the real service, with real authorities standing in for a provider that does not yet run) versus what is not claimed (an actual production admission).

**Read F-0045 before writing the report.** Re-run every recorded command against the exact final candidate after the last file change; never transcribe an earlier result.

**Budget facts carried in.** `max_orchestration_depth` is at **4 of 4**; `kernel.contracts.errors` and `kernel.contracts.state_machine` are both at **15 of 15** fan-in; `acceptance.engine` public surface is at **40 of 40**. Measure before adding and decompose under ADR-0008; do not request a GATE 8 exception. The canonical runtime is Python **3.13.15** in the git-ignored repository-local `.venv`, and there is still no Python lockfile.

*(superseded guidance retained for continuity)*

**Phase 9B Atomic Package 2 — the activated query.** Done: `can_perform` composes Package 1's reference resolutions, `configured_state` and transitive `prerequisites` into a determinate `PASS`/`NOT_CONFIGURED`, derived safely under the unchanged Phase 8 predicate and proven through the real `AdmissionService`. Both designed pre-activation tripwires replaced with stronger permanent controls.

*(superseded guidance retained for continuity)*

**Phase 9 Atomic Package 2 — source-level enforcement of `ARK-REQ-0053`.** Package 1 established the authority side; Package 2 must make "no component may store, cache, mirror, default or re-derive these values" checkable in the **consumers**. Today the `shadow_registry` gate validates the *declaration* — owner, consumer set, copying and caching flags — and not consumer source; that gap is stated in `docs/contracts/provider_record.md` §3 rather than papered over. Derive the five reference-only consumers from the map, never a list.

**Then Package 3 — `ARK-REQ-0219`,** the never-fabricate control. Note its owner is **`acceptance.engine`**, not the provider registry, and its evidence keys are `prov` + `sec`. Touching `acceptance.engine` makes the change set **PROTECTED_CORE**, so `select_profile` will require security review, adversarial review and full regression; derive the profile rather than assuming NORMAL.

**Then Package 4 — final integration, traceability carrying three real claims, the C-17 report and the gate.** Unlike Phase 8, Phase 9's denominator is **non-zero**: C6 refuses an empty record against a non-empty denominator just as hard as the reverse.

*(superseded guidance retained for continuity)*

**Begin Phase 9 — Provider + Model Runtime (C-11).** Done for Package 1: `docs/contracts/provider_record.md`, the parsed provider authority and the C-11 record. Matrix prerequisites 4, 6 and 8 are all accepted, and the Gate column is empty, so no human gate applies.

**Derive the scope from repository authority before writing anything.** Read `REQUIREMENT_REGISTER.md` for the rows whose Phase column is 9 — there are three: `ARK-REQ-0052`, `ARK-REQ-0053` and `ARK-REQ-0219` — `CONTRACT_INVENTORY.md` for C-11's row, and `ARCHITECTURE.md` §3 for what `control.registry.provider` owns. Phase 9 has a **non-zero denominator**, unlike Phase 8, so its traceability record must carry real claims.

**What Phase 8 leaves ready, and what it deliberately did not build.** `AdmissionService.evaluate` answers §4 for one request and refuses every one with `CAPABILITY_NOT_CONFIGURED` until Phase 9B. `provider` is one of the seven canonical worker classes, which is a *vocabulary* entry only: no provider runtime exists, and a control separates the canonical worker class from provider execution authority so building the runtime will not be mistaken for widening C-21.

**Three budget facts to carry in.** `max_orchestration_depth` is at **4 of 4** on `surfaces.command → execution.durable → control.policy → kernel.contracts`; a further hop on that chain must be decomposed under ADR-0008, never excepted. `kernel.contracts.errors` and `kernel.contracts.state_machine` are both at **15 of 15** fan-in, so a context needing a base error type must import `kernel.contracts.error_base`. The canonical runtime is **Python 3.13.15** in the git-ignored repository-local `.venv`; there is still no Python lockfile.

*(superseded guidance retained for continuity)*

**Phase 8 Atomic Package 3 — final integration, evidence, traceability, C-17 report and the first machine-acceptance submission.** Done: the composed journey, the zero-denominator record, the C-17 report and one gate run returning `PHASE_ACCEPTED_BY_MACHINE` on the first submission.

**The traceability record is `claims: []` and that is the only truthful shape.** The register assigns Phase 8 **zero** `ARK-REQ` entries, so `ark_req_ids_closed` is `[]` too. This is legal because and only because the denominator is empty — F-0040's repair made C6 judge emptiness against the denominator rather than absolutely. **No synthetic claim, no contract-id-as-requirement claim, no foreign-phase claim**: C6 refuses all three, and a non-empty record against an empty denominator is now refused just as hard as the reverse. Cumulative verified stays **80**.

**C-21 is evidenced as a contract, never as an `ARK-REQ`.** Use the report's `public_contracts`, `tests_executed` and `evidence_created`. Note that C-15's `EvidenceInput` requires a register-validated `requirement_id`, so a C-21 audit-chain record has no legal anchor — keep contract evidence in the report, and produce Phase 8 evidence from the **test tier** as Phases 6 and 7 did. A `scheduler → evidence.*` production edge measures depth 5 and is forbidden; a control now asserts its absence.

**Report the honest capability position, and do not dress it up.** Production admission returns `CAPABILITY_NOT_CONFIGURED` for every request because Phase 9B has not activated the graph. Package 3 must state that plainly and must not present the test-only-resolver success path as production capability. `ARK-REQ-0354` (failure-domain isolation) is **Phase 31** and must be named NOT CLAIMED.

**Three budget facts to carry in.** `max_orchestration_depth` is at **4 of 4** on `surfaces.command → execution.durable → control.policy → kernel.contracts` — a further hop on that chain breaches it and must be decomposed under ADR-0008, never excepted. `kernel.contracts.errors` fan-in is at **15 of 15**, so a context needing a base error type must import `kernel.contracts.error_base`; `kernel.contracts.state_machine` is also at **15 of 15**. Phase 8's own prerequisite is Phase 7, accepted; the matrix Gate column for Phase 8 is empty, so no human gate applies.

*(superseded guidance retained for continuity)*

**Phase 8 Atomic Package 2 — the admission decision.** Done: the three canonical conditions composed as a conjunction, honest `CAPABILITY_NOT_CONFIGURED` before Phase 9B, no cached capability verdict, no allocation, and a PEP denial that stays distinct from the three refusals.

**Begin Phase 8 — Resource Scheduler + Worker Contracts (C-21).** Done for Package 1: `docs/contracts/worker.md`, the parsed worker vocabulary, the PEP-governed declaration authority, and the `kernel.contracts.error_base` decomposition. F-0041 opened and closed.

**Phase 7 Atomic Package 5 — final integration, evidence, traceability, report and acceptance.** Done: the composed durable journey, real fault-injection evidence at the available tier, `phase_7_traceability.json`, `phase_7_report.json` and one gate run returning `PHASE_ACCEPTED_BY_MACHINE` on the first submission.

**Phase 7 Atomic Package 4 — the enqueue surface for `ARK-REQ-0027`.** Done: `POST /api/jobs` and `GET /api/jobs/{job_id}` under `surfaces.command`, persisting durable work through C-19 and returning a reference without executing it. F-0037, F-0038 and F-0039 opened and closed.

**Phase 7 Atomic Package 3 — pause/resume and crash recovery.** Done: the `durable_job_type` registry that makes `ARK-REQ-0060` evaluable, `RUNNING → PAUSED → RESUMING → RUNNING`, and the sweep that resolves a silent `RUNNING` execution through `FAILED → RECOVERABLE → RESUMING`. F-0036 opened and closed.

**Phase 7 Atomic Package 2 — durability semantics.** Done: attempts, heartbeat and execution ownership, bounded retries with row-derived accounting, absolute deadlines, cancellation and dead-lettering.

*(superseded guidance retained for continuity)*

**Begin Phase 7 — Durable Job + Workflow Core (C-19).** Phase 7 is unlocked and not started; the matrix records prerequisites 5 and 6, both accepted, and no human gate.

Before any Phase 7 work, note what Phase 6 deliberately did **not** deliver, so it is not assumed: the evidence **graph** does not exist. No coverage is computed, no requirement verdict is derived and no acceptance conclusion is drawn from any evidence record — that is C-16 and the Acceptance Engine at **Phase 13**. Phase 6 supplies the `Artifact` and `Evidence` nodes and the identifiers a later graph would need, and nothing more.

Out of scope and not to be pulled forward: Phase 13 evidence-graph computation, coverage and verdicts (C-16); provider runtime (9); Capability Graph activation (9B); Recovery Supervisor and `ROLLBACK_STABLE` (22B); Stable Core promotion (23); release manifests, SBOM and signing (26); the Golden corpus (30); T7/T8/T12 (31); clean-baseline packaging (36).

*(superseded guidance retained for continuity)*

**Phase 6 Atomic Package 3 — Final Integration + Evidence + Traceability + Phase Acceptance.** Done: the integration evidence, `phase_6_traceability.json`, `phase_6_report.json` and the gate run, which returned `PHASE_ACCEPTED_BY_MACHINE`.

*(superseded guidance retained for continuity)*

**Begin Phase 5 — Persistence + Project Registry + Minimal Backup/Restore.** Phase 5 is unlocked and not started.

*(superseded guidance retained for continuity)*

**Remediate F-0024 before any Phase 5 work.** Done: `ARK-REQ-0111` implemented and verified, F-0024 and F-0025 closed, Phase 4 re-accepted. Cumulative verified is **65** again, now genuinely.

*(superseded guidance retained for continuity)*

**Begin Phase 5 — Persistence Foundation.** Phase 5 is unlocked in phase-graph terms but blocked by an open HIGH finding.

*(superseded guidance retained for continuity)*

**Begin Phase 4 — Security + Governance + Isolation Backends.** Phase 4 delivered C-07, C-08, C-09 and C-10; the single PDP and the PEP enforcement contract; Protected Core boundary enforcement; the Secret Vault boundary; Local-Only policy; and the isolation backend availability probe, closing **DEF-003**.

*(superseded guidance retained for continuity)*

**Begin Phase 3 — Formal State Machines + Capability Graph Schema.** Phase 3 delivered the twelve canonical state machines and the Capability Graph schema (C-13). Per ADR-0003 the graph is schema-only until Phase 9B; every capability query returns `NOT_CONFIGURED` before activation.

*(superseded guidance retained for continuity)*

Phase 2 delivers contract definitions C-01, C-03 (definition), C-04, C-17, C-18; the eight architecture gates with their negative-control fixtures; and the deterministic Phase Gate Checker. At that point Phase 0's `NOT_TESTED` items become testable for the first time, and the vacuous dependency-direction check gains real imports to reject.

*(superseded guidance retained for continuity)*

Phase 2 (Foundation + Contracts + Phase Gate Checker) follows, at which point the eight architecture gates and the deterministic Phase Gate Checker become executable and Phase 0's `NOT_TESTED` items become testable for the first time.

From here, normal phases are accepted by machine verdict without human approval. The next human gate is **GATE 2** at Phase 23 (Stable Core promotion), unless a phase reaches GATE 4, 5, 6 or 8 earlier.

## Carried forward

Acceptance of Phase 0 closed no finding other than the gate itself:

- **90 of 303 MANDATORY requirements are verified**, plus **1 CONDITIONAL** requirement (`ARK-REQ-0060`) discharged while APPLICABLE — **91 discharged in total** (5 Phase 1 + 23 Phase 2 + 4 Phase 3 + 33 Phase 4 + 7 Phase 5 + 3 Phase 6 + 5 Phase 7 of which 4 are MANDATORY + 0 Phase 8 + 3 Phase 9 + 3 Phase 9B + **5 Phase 10**). Every figure here is reconciled by check C6 against each phase's own traceability record; the authority is those records and the phase table above, never this summary. The lines below are retained as written at Phase 9B, Phase 7 and Phase 6 and are superseded by this one.
- **85 of 303 MANDATORY requirements are verified**, plus **1 CONDITIONAL** requirement (`ARK-REQ-0060`) discharged while APPLICABLE — **86 discharged in total** (5 from Phase 1, 23 from Phase 2, 4 from Phase 3, 33 from Phase 4, 7 from Phase 5, 3 from Phase 6, 5 from Phase 7 of which 4 are MANDATORY, **0 from Phase 8** — a zero-denominator phase — 3 from Phase 9 and **3 from Phase 9B**). Every figure here is reconciled by check C6 against each phase's own traceability record; the authority is those records and the phase table above, never this summary. The two lines below are retained as written at Phase 7 and Phase 6 and are superseded by this one.
- **79 of 303 MANDATORY requirements are verified**, plus **1 CONDITIONAL** requirement (`ARK-REQ-0060`) discharged while APPLICABLE — **80 discharged in total** (5 from Phase 1, 23 from Phase 2, 4 from Phase 3, 33 from Phase 4, 7 from Phase 5, 3 from Phase 6, **5 from Phase 7 of which 4 are MANDATORY**). Phase 7 is the first phase whose discharged set is not entirely MANDATORY, which is why the two figures are stated separately rather than collapsed. The line below is retained as written at Phase 6 and is superseded by this one.
- **75 of 303 MANDATORY requirements are verified** (5 from Phase 1, 23 from Phase 2, 4 from Phase 3, 33 from Phase 4, 7 from Phase 5, 3 from Phase 6), each recorded in its phase's traceability record with a named implementation and a named evidence source. The line below is retained as written at Phase 4 and is superseded by this one.
- **65 of 303 MANDATORY requirements are verified** (5 from Phase 1, 23 from Phase 2, 4 from Phase 3, 33 from Phase 4). All 33 Phase 4 entries were re-derived from the register after F-0024 and each is recorded in `phase_4_traceability.json` with a named implementation and a named control; check C6 refuses any discharge that record does not support.
- **A false discharge can no longer pass.** C6 reconciles the phase report against the phase's traceability record; a requirement in any non-SATISFIED state may not appear in the discharged set.
- **Protected Core changes now carry a stronger profile** from Phase 4 onward (`ARK-REQ-0111`): security review, adversarial review and full regression, each backed by a real execution record. The eight gates now evaluate 41 real cross-context edges.
- **Security is enforced, not described.** One PDP, deterministic and pure; every decision audited including AUTO; unmappable action DENY; `WRITE_STABLE_FILE` DENY for every actor at every tier; Protected Core direct mutation refused. All governed vocabularies are parsed from the authority map — no security module holds a private copy.
- **Host isolation is partial and honestly reported.** TRUST-0/1 satisfiable; TRUST-2/3/4 **UNSUPPORTED** on this host because `NET_EGRESS_CONTROL` (WFP needs elevation) and `KERNEL_ISOLATION` (Windows Sandbox and Hyper-V not enabled) are genuinely unavailable. No Windows feature was enabled to improve the result.
- **No execution surface exists.** Bypass resistance is verified at contract level only; API, UI, agent, workflow, plugin and computer-use are NOT_YET_IMPLEMENTED and are not claimed.
- The twelve canonical state machines are executable and reconciled against `STATE_MACHINES.md` on every run, so the inventory can no longer drift from the code that implements it.
- The Capability Graph exists as schema only. ARK-REQ-0046, 0047 and 0048 remain open at Phase 9B and are **not** claimed by Phase 3.
- The architecture remains a declaration until the Phase 2 gates run against real code (M-P0-2).
- Register exhaustiveness is by construction, not mechanical extraction; Phase 2 reconciles it (M-P0-1).
- 14 MEDIUM and 9 LOW findings remain open in `OPEN_BLOCKERS.md`. Every Phase 3 finding (F-0018 … F-0021) is now **closed**; two by human erratum.
- All **nine** declared numeric architecture budgets are evaluated by the gate under ratified measurement contract 1.0.0 (ERR-003). Max measured complexity **12** of 12; max orchestration depth **3** of 4.
- Every recorded defect remains in `KNOWN_FAILURES.md` as permanent evidence. That file is the authoritative defect ledger; its composition is deliberately **not** mirrored here, because a transcribed count re-rots the moment a defect is added (F-0002, F-0011). Read the ledger. F-0015 is CLOSED by ERR-001 but retained in full.
