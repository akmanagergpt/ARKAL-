# OPEN BLOCKERS — ARKALI GENESIS v2

**As of:** Phase 0 candidate

## Internal blockers

**Status is declared, never inferred.** Each finding's state comes only from its
`Status` cell. Free text in any other column may contain OPEN, CLOSED, HIGH,
`ark_req_ids_closed` or any other token without effect (F-0025).

| ID | Finding | Severity | Status | Owner | Blocks |
|---|---|---|---|---|---|
| F-0068 | **`PRODUCT_UX_MODULE_PARITY_NORMALIZATION_GAP`: `product_ux_spec.py`'s own `_module_parity_findings` compared a UX module's name against a backend-declared data model's name with plain case/separator-insensitive normalization only, never the "model"-suffix normalization the sibling action/route check (`_matching_methods`) already applies — blocking real generation, never producing a false PASS.** Real, repeated, independently-reproduced evidence, not invented: `golden-work-095` and `golden-work-098` (prior session) and `golden-work-130` (this session, real `Family 1 — Task/Work Management` staged-generation attempt, real `qwen2.5-coder:14b`, real elapsed 1379.8s) all independently hit the identical shape — a real `backend/task_model.json` with no explicit `table_name` fell back to `_path_stem` naming the model `"task_model"`, an awkward, redundant name; the real model's own natural UX-module choice was `"task"`. The stage's own retry feedback correctly disclosed the literal real name (`"task_model"`) on every attempt (a prior, already-applied partial fix), but the real model still never converged across all 4 real attempts — `golden-work-130` exhausted its full budget and reached a real, honest, terminal `STAGE_FAILED` before ever reaching `frontend_ui`/`frontend_forms`, so no frontend, acceptance, or F-0066/F-0067 transfer evidence exists for it. `_matching_methods` had already solved the identical asymmetry for action/route reconciliation (via `_resource_matching_name`, stripping a genuine trailing "model" suffix) — `_module_parity_findings` never reused it. Found via a real, honest, non-patched real-candidate generation run at the human acceptance authority's own explicit request | HIGH | CLOSED | engineering.factory | resolved same session: `_module_parity_findings` (`product_ux_spec.py`) now reuses `_resource_matching_name` unchanged (no second normalization system) for its own real/UX-module name comparison, applied symmetrically to both sides; the "missing"/"invented" findings still report the real, un-stripped original names, never the normalized form, so retry feedback stays literal and copy-pasteable. Explicit `table_name` precedence (`_model_names_in_document`) is completely unchanged — normalization only affects which two already-derived real names are judged the same concept, never which one wins when both a `table_name` and a filename exist. Action/route reconciliation (`_matching_methods`, `_module_action_findings`) is untouched — proven by a dedicated regression test showing the real over-declared-`"delete"`-action finding from `golden-work-130`'s own real second half still fires unchanged. Proven by 7 new behavioral tests (`test_product_ux_spec.py`): a `task`/`task_model` parity match; a second, structurally different domain (`inventory`/`inventory_model`) reproducing the identical behavior; explicit `table_name` precedence preserved; genuinely different resource names still refused; a natural word merely containing "model" without it as a trailing suffix (e.g. a real `"modeltrain"` resource) never wrongly truncated; the action/route mismatch check firing independently and unchanged; and `golden-work-130`'s own real, frozen `backend/task_model.json`/`backend/routes.json` bytes, copied verbatim as read-only evidence, no longer failing on name parity. `golden-work-130` itself is completely untouched — its real, terminal `STAGE_FAILED` ledger state and every generated file are preserved unrewritten as historical evidence; no candidate file was edited, no retry was started this session |
| F-0067 | **`GENERATED_FORM_INITIAL_STATE_WIRING_GAP`: a form component `frontend_forms` generates and reuses for both create and edit never wires the real record's own data, passed to it at the edit route, into its own visible field values.** Root-cause fix for the generator-side half of F-0066's own real evidence (a distinct root cause from F-0066's acceptance-measurement fix; not a duplicate manifestation — F-0066 fixed what the acceptance journey ever OBSERVES, this fixes what the GENERATOR ever PRODUCES). `golden-work-127/128/129` (real repository evidence, real qwen2.5-coder:14b, frozen, independently reproduced three times with zero variation — `golden-work-129`'s own three real repair attempts inherited the identical shape unchanged): `StudentEdit` correctly fetches the real record by the route's own real id and threads it into `<StudentForm ... initialData={student} />`; `StudentForm`'s own real definition, `({ onSubmit, fields }) => ...`, never destructures `initialData` at all, so its own `formData` state stays `useState({})` unconditionally and every real edit route renders a genuinely blank form. Investigated first via REUSE→EXTEND→CREATE: no existing checker (`frontend_callback_arity_preflight`, `frontend_route_param_preflight`, `_route_component_missing_props_findings`, `_form_quality_findings`) covers "a JSX call site passes a prop the receiving component never consumes" for a generic, non-route-injected prop; the two small shared regex helpers this fix needed (`_component_destructured_props`, `_balanced_brace_body`) already existed as private, independently-duplicated copies in `frontend_manifest_preflight.py` and `frontend_callback_arity_preflight.py` and were promoted into the shared `frontend_js_semantics.py` (both call sites now reuse the promoted versions, zero behavior change, 92 pre-existing tests re-verified green) rather than written a third time | HIGH | CLOSED | engineering.factory | resolved same session: a new `frontend_forms`-scoped preflight, `frontend_form_initial_state_preflight._form_initial_data_ignored_findings`, wired into `component_generation._frontend_forms_findings` alongside its siblings. Two detection tiers, both real repository evidence, not invented: (1) the call site passes `initialData={<a real identifier>}` (never a literal — proven intent to thread real fetched data) and the receiving component's own definition (found by name across the whole frontend) never destructures a prop of that name at all — the exact golden-work-127/128/129 shape; (2) the component DOES destructure `initialData` but the name never appears again anywhere in its own body — a real, plausible shallowest-patch retry shape, the same one-step-further posture `frontend_ui_route_id_read_but_not_passed` already takes for `useParams()`'s own id. `initialData` is treated as this pipeline's own real, evidenced, pipeline-wide naming convention (verified independently across all three real candidates' own frozen source, never one candidate's local choice) — the identical posture already established for `_ROUTE_IDENTIFIER_PARAM_NAME = "id"` — never a golden-domain concept; no field, resource or entity name appears anywhere in the check. `STAGED_GENERATION_STAGES.md#9`'s own prose rule gained a short paragraph citing the same real evidence, alongside (not instead of) the hard preflight gate — the same posture `frontend_mutation_contract.py`'s own docstring already documents as necessary (prose alone was previously shown insufficient for this exact stage). Proven by 9 static behavioral tests (`test_frontend_form_initial_state_preflight.py`: mandatory-missing-prop fails, correctly-wired form passes, the destructured-but-unused shallow patch still fails, a second structurally different domain — `task`/`title`/`due_date`, never `student`/`name` — reproduces the identical behavior, a literal prop value is never treated as real threaded data, silent when the component definition or any frontend file is absent, one finding per component even at multiple call sites) plus one REAL, browser-driven test (`test_frontend_form_initial_state_behavior.py`, real React 18 UMD + real Chromium via the same `@playwright/test` dependency `test_golden_browser_journey_capability_coverage.py` already uses, never a mock of React's own hook semantics) proving the fix pattern the finding's own `detail` text recommends — destructure, seed `useState(initialData || {})`, resync via `useEffect(() => setFormData(initialData || {}), [initialData])` — genuinely rehydrates the form after a real ASYNC update arrives (not merely on first render) and never clobbers a real user's own typed edit across an unrelated real re-render, run over two structurally unrelated domains inside the one browser session. `golden-work-129` itself, its candidate directory (`var/factory/candidates/golden-work-129/**`, confirmed untouched via `git show --stat` and a direct `ledger.verify_integrity` re-run, PASS, latest state still `ACCEPTED`), `golden/repair/golden_repair_corpus.json`, and F-0066's own acceptance-layer fix (`run_golden_browser_journey.mjs`, `acceptance_scenario.py`, `acceptance_plan_compiler.py`) are entirely untouched this session — this finding fixes the GENERATOR layer only, F-0066 remains the measurement-layer fix, and neither is loosened to accommodate the other. No `golden-work-130`, no new candidate, no real staged-generation model invocation this session — fresh-candidate validation of this fix is deferred pending explicit human authorization |
| F-0066 | **`GOLDEN_ACCEPTANCE_DECLARED_CAPABILITY_COVERAGE_GAP`: `run_golden_browser_journey.mjs`'s own real acceptance journey let a genuinely MISSING, spec-declared-mandatory UI capability silently pass, and never verified the edit form actually showed a real record's own current data before mutating it.** Root-cause-consolidated from a real ARKALI LIVE PRODUCT CHECKPOINT against `golden-work-129` (historical specimen/evidence source, not a rule about that one product): two distinct manifestations of the SAME root invariant gap — "declared mandatory capability → observable UI control/route/state → exercised behavior" was never enforced, `if (control exists) then test it` silently substituted for it. (a) The related-resource create step ran `if (await relatedCreate.count())` — a resource whose own `product_ux_spec.json` AND real backend routes both declare "create" (`acceptance_plan_compiler.py`'s own `_resolved_actions`, already computed, previously discarded rather than acted on) produced zero failure signal when no reachable control existed; `golden-work-129`'s own real `Payments.js` has exactly this shape (`apiClient.js`'s real, working `createPayment` never called by any UI). (b) The edit step filled `browser_update_values` and asserted only the post-mutation result — never reading what the form showed BEFORE typing, so a form that silently renders blank (as `golden-work-129`'s own real `StudentForm` does — accepts `initialData`, never reads it) is indistinguishable from one that was genuinely pre-filled. Neither manifestation caused a false PASS by corrupting the mechanical acceptance predicate itself — both are gaps in what the journey ever attempted to observe. Search/filter was investigated separately and explicitly EXCLUDED from this invariant: `STAGED_GENERATION_STAGES.md` stage 6 requires `product_ux_spec.json` to DECLARE `search_filter`, but no stage 7–11 rule, and no canonical `REQUIREMENT_REGISTER.md` entry, ever obligates its implementation or verification — adding it to the coverage gate would have been inventing a requirement, not enforcing an existing one. Found by a real, non-mutating live product checkpoint, at the human acceptance authority's own explicit request | HIGH | CLOSED | acceptance.engine | resolved same session: `_ResourceScenario` (`acceptance_scenario.py`) gains a real `actions` field — the exact, already-computed `_resolved_actions` result (create/edit/delete only; "view" is deliberately excluded, since it names no verb `_ACTION_TO_VERB` maps and a rendered resource is already its own "view" observable), now persisted instead of discarded (`acceptance_plan_compiler.py`, one line). `run_golden_browser_journey.mjs`'s related-create step now throws a real, named error when `related.actions` declares "create" but no control is found, and legitimately skips only when "create" was never declared by BOTH the candidate's own spec and its own backend (never invented, never guessed); the edit step now reads every editable field's real current value via `input.inputValue()` immediately after reaching the edit route, before any new value is typed, and fails with the exact expected-vs-found values if it does not match the record's own real creation values. Both `golden/scenarios/*.json` regression oracles (`student_fee_management`/`inventory_management`/`task_management`) gained real `actions` data, matching the compiler's actual reconciled output, not guessed. Proven by 7 new behavioral tests (`test_golden_browser_journey_capability_coverage.py`) running the REAL, unmodified `run_golden_browser_journey.mjs` as a real subprocess, against a real (stdlib-only, no framework) HTTP backend and frontend and a real Chromium instance — never a mock of the journey's own logic — across TWO structurally unrelated synthetic domains (`widgets`/`gadgets`, `notes`/`tags`) proving no hardcoding: mandatory-and-missing fails, mandatory-and-present passes, genuinely-optional-and-absent is a legitimate skip, wrong/empty prefill fails, correct prefill passes, a single-resource product with no related resource at all regresses clean. `golden-work-129` itself, its candidate directory, `golden/repair/golden_repair_corpus.json`, and every product-generation stage (`STAGED_GENERATION_STAGES.md` stages 1–11, `product_ux_spec.py`, frontend/backend generators) are entirely untouched — this finding fixes the MEASUREMENT layer only. `golden-work-129`'s own historical `ACCEPTED` state is unchanged and not reinterpreted as invalid; the live checkpoint's own added interpretation is explicit and separate: its historical `GOLDEN_ACCEPTANCE_PASS` evidence proves the mechanical acceptance predicate it was measured against, never full user-facing feature completeness or usability, which no version of this journey — before or after this fix — was ever built to certify in full |
| F-0065 | **`CURRENT_UNREPAIRABLE_INSTANCE_INVALIDATED`: the instantiated corpus's `CORPUS-permission_check_removal-001` entry declares `repairable: false` on a rationale that current repair-harness semantics have made unreliable.** The entry's own recorded rationale is specifically that "no pytest test... ever asserts on CORS/access-control headers, so a bounded repair strategy gets no real test-based convergence signal for this class... the repeated-strategy/budget-exhaustion path is expected to fire." That rationale assumes a test-signal-only repair harness. `_make_attempt_repair` instead grounds every real attempt in source-derived structural evidence (`root_cause_evidence`, AST-derived from the candidate's own real code and test; `real_test_probe_output`; `current_files`) — independent of whether any test asserts on the affected behavior at all. **This independently invalidates the entry's stated rationale regardless of F-0064**: even under a hypothetical leak-free harness, the test-signal-absence argument does not hold, because the harness was never relying on test signal alone in the first place. `golden-work-129-repair-3`'s own real run then supplied direct confirming evidence: the declared-unrepairable entry was genuinely, mechanically RESOLVED on its first real attempt (injection confirmed applied, detector confirmed the defect present pre-repair and absent post-repair, a real non-vacuous `CORS(app)` restoration diffed and verified, protected paths unchanged). **Explicit nuance preserved, not overclaimed: repair-3 ran under the historical leaking harness (F-0064, closed the same session) — it does NOT prove `permission_check_removal` is repairable under leak-free conditions specifically, and no such claim is made here.** The rationale's own invalidation stands on its own, independent of the leak; whether the entry would still resolve under the now-fixed, leak-free prompt is a genuinely open, untested question. A survey of the other 7 canonical defect classes (read-only audit, prior session) found none currently supports a robust, model-independent replacement without either a code-level injection-strategy redesign (proposed, not authorized) or a weak, unpromoted candidate (`migration_model_mismatch`). Found by a real, non-mutating unrepairable-corpus audit, at the human acceptance authority's own explicit request, before authorizing any further Golden Repair run | MEDIUM | OPEN | engineering.repair | **blocking** for `golden-work-129-repair-4` (re-running against the unchanged corpus risks reproducing the same non-canonical outcome, making a valid `GOLDEN_REPAIR_PASS` structurally unreachable) and for any `ARK-REQ-0188` discharge claim on this corpus instance; **non-blocking** for unrelated Phase 30 work. Not repaired this session — no corpus edit, no version bump, no `repairable` flag change, no injector/detector redesign, no model invocation. `golden-work-129-repair-1/2/3`'s own historical ledger states, evidence, and the corpus's own real content hash (`sha256:76520265...`) are unchanged; `golden/repair/golden_repair_corpus.json` is byte-identical to every prior session. Resolution requires a human governance decision on a leak-free, model-independent replacement design (see the prior session's unrepairable-corpus and information-leak audits for the full survey and candidate analysis) before `golden-work-129-repair-4` may run |
| F-0064 | **`GOLDEN_REPAIR_EVALUATOR_INFORMATION_LEAK`: `_make_attempt_repair`'s real repair prompt sent `defect_class`, `defect_title`, `defect_rationale` and `injection_target` — corpus-authored, evaluator-only ground truth — to the repair model for every one of the 8 real corpus entries, not just `golden-work-129-repair-2/3`'s own contested `permission_check_removal` entry.** Mechanically confirmed against the real corpus JSON: every one of the 8 entries' `title` field is near-answer-revealing (e.g. `persistence_not_committed`: "INSERT reaches the connection but is never committed"; `boundary_off_by_one`: "Parameterised route loses its integer type boundary"), and `permission_check_removal`'s own `rationale` names internal benchmark mechanics verbatim ("The mandatory unrepairable entry (corpus definition §4)... the repeated-strategy/budget-exhaustion path (`RepeatedFailedStrategyError`/`RepairBudgetExceededError`)... is expected to fire"). This contradicts the canonical Root-Cause and Convergence Engine's own pipeline (`docs/ARKALI_GENESIS_V2_MASTER_SPECIFICATION.md` — "Reproduce→Observe→Evidence→Hypotheses→Experiment→Root Cause→Minimal Repair"), which is evidence-driven by design, not answer-driven — corpus-authored explanatory text is not a real-world repairer's observable evidence. A second, narrower leak vector was found in `_analyze_repair_target`: `probe_failure or entry.title` fell back to the corpus title whenever the real pytest probe captured empty text (never observed in practice — pytest always emits a summary line — but a real, reachable code path). Neither leak causes an incorrect PASS/FAIL verdict on its own (the F-0058 promotion predicate is unaffected), but both undermine the evidentiary meaning of any resulting resolution: a mechanically correct `GOLDEN_REPAIR_PASS` obtained this way would not prove genuine root-cause capability. Found by a real, non-mutating information-leak audit, at the human acceptance authority's own explicit request, before authorizing a corpus redesign or `golden-work-129-repair-4` | HIGH | CLOSED | engineering.repair | resolved same session: the repair prompt now carries only `root_cause_evidence` (AST/structure-derived from the candidate's own real code and test), `real_test_probe_output` (the real captured pytest output) and `current_files` (the real, already-scoped-by-F-0060 structural context) — `defect_class`/`defect_title`/`defect_rationale`/`injection_target` are never serialized into it. `entry` stays available to the closure for genuinely evaluator-side bookkeeping only (fingerprint `strategy=model_repair_{defect_class}`, `per_defect_evidence` in the ArtifactStore payload) — never the prompt. The `probe_failure or entry.title` fallback is replaced with a fixed, generic, non-leaking placeholder (`_NO_CAPTURED_FAILURE_TEXT`), never corpus text. `injection_strategy`/`repairable`/`expected_detection`/`budget_profile` were already never read by the prompt-construction path — confirmed, not merely assumed, by new structural tests. Corpus file itself is untouched — the corpus schema still declares these fields for real authoring/versioning/evaluator purposes (`GOLDEN_REPAIR_CORPUS_DEFINITION.md` §1); only the repair AGENT's own input contract changed. Proven by 11 new `TestPromptFairness` tests (`backend/tests/engineering/test_golden_corpus.py`) against the REAL constructed prompt payload object, not substring matching alone: title/rationale/repairable/defect_class/injection_target absence, `injection_strategy`/`expected_detection` never read (structural source checks), legitimate `current_files`/`real_test_probe_output` presence, `root_cause_evidence` free of injected title/rationale markers even when both branches of `_analyze_repair_target` are exercised with an empty probe, and corpus metadata still fully recoverable from the entry object (evaluator-side evidence is not lost, only kept out of the model's own context). **`golden-work-129-repair-1/2/3`'s own historical results are NOT rewritten and remain real** — they must now be read as *real repairs completed under the historical leaking harness*, not as leak-free proof of black-box root-cause capability; whether `permission_check_removal` (or any of the 7 resolved entries) would still converge under this fixed, leak-free prompt is unproven and requires a future real run, not asserted here. The separate current-unrepairable-instance corpus-design question (repair-3's `permission_check_removal` entry) remains its own, unwritten, OPEN gap — this fix does not resolve it, and does not by itself unblock `golden-work-129-repair-4` |
| F-0063 | **`DYNAMIC_REGRESSION_PROBE_ENVIRONMENT_INSUFFICIENCY`: F-0059's own new dynamic `_measure_regression` reused ARKALI's own interpreter (`_run_pytest_probe`) rather than a real, candidate-appropriate runtime environment — on this host, that interpreter has no `flask` installed, so even `golden-work-129`'s own real, genuinely ACCEPTED parent measured a nonzero `_pytest_failure_and_error_count` (2, a generic `ModuleNotFoundError: No module named 'flask'` collection error) rather than the 0 its real acceptance evidence proves it should be.** Since the delta is still computed correctly as a subtraction, this never produced a WRONG delta in the cases tested — but it meant the dynamic measurement was, for a Flask-based candidate family, only capable of detecting a NEW collection-level break, never a genuine functional test regression hidden behind the identical shared collection failure both baseline and child would show — a real, if narrow, fails-open risk against the canonical PASS predicate's own `regression_status == "measured" and regression_count == 0` requirement, the same family as F-0058/F-0059's original wiring defect. F-0059's own original wiring defect (hardcoded `regression_delta=0`, the missing `inspect_gate(..., baseline=...)` argument) is UNRELATED and remains genuinely fixed — this is a distinct root cause (execution ENVIRONMENT, not measurement WIRING), tracked separately rather than reopening F-0059. Found by a real, non-mutating inference-feasibility preflight, at the human acceptance authority's own explicit request, before authorizing `golden-work-129-repair-3` | HIGH | CLOSED | engineering.repair | resolved same session: `_measure_regression` now executes in a real, freshly-prepared venv with the candidate's OWN real `backend/requirements.txt` installed, reusing `run_golden_acceptance.py`'s own real `_run` subprocess helper and its own exact venv/pip-install/pytest sequence — the same real mechanism a genuine `GOLDEN_ACCEPTANCE_PASS` already proves works — never a second, divergent environment-preparation implementation, and never `run_golden_acceptance.py`'s own lifecycle (`begin_acceptance`/`ACCEPTANCE_RUNNING`/browser journey/ledger transition are never invoked; proven structurally by `TestRegressionMeasurementNoLifecycleSideEffect`/`TestExistingAcceptanceBehaviorUnchanged`). TEST-SUITE IDENTITY is pinned to the PARENT's own real accepted `tests/` bytes for BOTH the baseline and the child run (`_with_parent_test_suite`) — the child's own product code, including any repaired `backend/requirements.txt`, is what is genuinely under test; a child that changed or planted its own `tests/` can never affect the measurement (proven by `TestParentTestSuiteIdentity`, independent of and unaffected by the existing `changed_protected_paths` no-test-weakening guard). `measure_regression` now returns `(delta, status)`: `status='unavailable'` propagates, never a synthetic 0, when the real environment itself could not be prepared — `run_isolated_golden_repair`'s promotion predicate was updated to require `status=='measured'` before ever reading `delta`, so `None`/`unavailable` still correctly refuses `GOLDEN_REPAIR_PASS` (`TestRegressionMeasurement`/`TestPromotionMatrix::test_e2`). Evidence payload now separates `dynamic_regression_count`/`dynamic_regression_measurement_status`/`static_regression_findings`/`protected_path_violations` as distinct fields, plus a `regression_evidence_note` naming `dynamic_regression_count` as the one ARK-REQ-0093-authoritative fact (never `aggregate_consumption.regression_delta`, which stays a clearly-labeled, non-authoritative per-attempt ledger sum). Proven end-to-end with 2 REAL venv/pip-install/pytest runs (not mocked): an identical parent/child pair measures a real `(0, 'measured')`; a child whose product code genuinely breaks the PARENT's own unchanged test measures a real `(1, 'measured')` |
| F-0062 | **`REPAIR_TIMEOUT_BUDGET_ROBUSTNESS_GAP`: the declared `golden-repair-standard-v1` profile's single-call `--timeout-seconds=600.0` and per-defect `elapsed_seconds=3600` ceiling satisfy `attempts(6) x timeout(600s) == elapsed_budget(3600s)` EXACTLY — a coincidental, never-reasoned-about self-consuming relationship first recorded, but not resolved, under F-0060.** In the theoretical worst case — every one of an entry's 6 attempts genuinely timing out at exactly 600s — the `elapsed_seconds` budget dimension would be exhausted at PRECISELY the same moment the `attempts` ceiling is, carrying zero independent bound beyond what `attempts x timeout` already guarantees. `docs/contracts/repair.md`'s six-dimensional budget does not relate the per-call timeout (a script-level `run_golden_repair.py` CLI argument, not one of the six ledger dimensions) to `elapsed_seconds` at all, so this is NOT a canonical-contract violation — but it is a real design fragility the contract is silent on. A real, non-mutating inference-feasibility preflight (before authorizing `golden-work-129-repair-3`) measured 3 real `qwen2.5-coder:14b` calls at 68.95s, 426.54s and 144.50s — none near the 600s timeout, leaving genuine real headroom under the current numbers in normal operation (`6 x 426.54s ~= 2559s < 3600s`) — so there is no evidence of functional impossibility today. Found by that same real preflight, at the human acceptance authority's own explicit request after reviewing F-0060's original text, which had recorded this relationship without ever resolving it while still being marked CLOSED | MEDIUM | OPEN | engineering.repair | non-blocking unless repository governance says otherwise: real smoke evidence shows no functional impossibility under the current numbers, so this does not, by itself, refuse `golden-work-129-repair-3`. Not repaired this session by explicit instruction — no numeric value (`--timeout-seconds`, `elapsed_seconds`, `attempts`) may be changed without real hardware/provider evidence beyond a handful of smoke samples, and no new timeout-manager/service may be built to work around it. `_budget_has_convergence_headroom` (`run_golden_repair.py`) remains the existing documentation/test-tooling detector, not a hard refusal. The real timing distribution from an eventual `golden-work-129-repair-3` run itself is expected to supply the evidence a numeric change would need |
| F-0061 | **`HARDCODED_FINGERPRINT_TARGET`: `_make_attempt_repair` hardcoded `backend_path = "backend/app.py"` for every corpus entry regardless of `entry.injection_target`, feeding both `RepairFingerprint.files` and the Python-AST `analyze_python_product_failure` root-cause classifier the same wrong evidence for 6 of 8 real defect classes.** `lockfile` targets real `backend/requirements.txt`, `migration` targets real `backend/db.py` + `backend/data_model.json` — a dependency-lock-mismatch repair attempt's own recorded fingerprint and root-cause evidence pointed at a file the defect never touched, and the Python-AST analyzer was invoked against `backend/app.py` regardless of whether the entry's own real target was even Python source. Found by the same read-only audit as F-0058/F-0059/F-0060, before authorizing `golden-work-129-repair-3` | MEDIUM | CLOSED | engineering.repair | resolved same session: fingerprint `files=` now derives from `context_paths = tuple(sorted(_context_files(entry, current))) or (entry.injection_target,)` — the same real per-entry context F-0060 introduces, reused rather than duplicated — for every fingerprint construction (success, rejection and malformed-response paths alike). Root-cause evidence comes from a new `_analyze_repair_target(entry, context, probe_failure)`: `analyze_python_product_failure` is called ONLY when `backend/app.py` is genuinely part of the entry's own resolved context; every other target gets a real `ProductRootCause` using the EXISTING `unclassified_runtime_failure` fallback vocabulary `product_root_cause._classify` already uses — never an invented class, never a mis-applied Python parse over a non-Python/non-entrypoint target. Proven by `TestFingerprintTargetAccuracy` (`backend/tests/engineering/test_golden_corpus.py`): two different defect targets (source_module vs lockfile) get genuinely different `fingerprint.files`, with the lockfile target correctly falling back to `unclassified_runtime_failure`; a rejected/timed-out attempt still points at the correct target's real file, never the old hardcoded one; and anti-loop strategy identity (`model_repair_{defect_class}`, independent of the target fix) is proven unaffected, so F-0056's own anti-loop semantics are undisturbed |
| F-0060 | **`BENCHMARK_CONFIGURATION_MISMATCH` (SCOPE CORRECTED — see note): `num_ctx=8192` (a class default, never overridden) equalled `max_output_tokens=8192` — mathematically self-contradictory regardless of input, since Ollama's own input and requested output compete for the SAME context window.** `golden-work-129`'s real candidate (20,827 bytes / 16 files) measured ~7,200–7,600 estimated prompt tokens (chars/4, matching the existing golden-work-125 documented estimation convention) sent whole on every real attempt, leaving well under 1,000 tokens of real headroom against an 8,192-token output request. Found by a read-only audit of `golden-work-129-repair-2`'s own real per-attempt evidence, before authorizing a second real benchmark run. **SCOPE CORRECTED:** this finding's own text originally also described a second, separate mismatch — `6 attempts x 600s timeout == 3600s == elapsed_seconds` — and recorded it as part of this same CLOSED finding despite its own resolution text admitting that half was "deliberately NOT resolved... not wired into a hard refusal." A later real, non-mutating inference-feasibility preflight caught this as `FINDING_STATUS_DRIFT` before authorizing `golden-work-129-repair-3`; at the human acceptance authority's own explicit direction, that unresolved half is now tracked as its own separate finding, **F-0062**, so this finding's CLOSED status accurately reflects only the token/context mismatch it genuinely fixed | MEDIUM | CLOSED | engineering.repair | resolved same session, without changing the timeout value, the model, or the provider (`qwen2.5-coder:14b` unchanged): `ollama_adapter.py` gains real, reusable `estimate_tokens`/`resolve_output_budget` functions — the generic invariant `input_tokens + output_tokens + safety_margin <= num_ctx` every real call must now satisfy, clamping the requested output down (or raising, never silently exceeding the window) instead of a fixed `max_output_tokens`. `run_golden_repair.py`'s own `_make_attempt_repair` calls it per-call (the safe budget varies with the real prompt) before constructing `OllamaAdapter`. Separately, addressing whether sending the WHOLE candidate was ever required: `_context_files`/`_TARGET_FILES` map each entry's own real, generic `injection_target` (a corpus-schema field, never a domain name) to the real canonical convention file(s) STAGED_GENERATION_STAGES.md already names — `route_contract`/`migration` deliberately stay two-file so cross-file drift/mismatch classes keep both sides of the contract visible, everything else narrows to its one real file, and an unrecognised target falls back to the whole candidate rather than silently sending nothing. Proven by `TestContextSelection` (5 cases: single-file, cross-file/both-sides, migration/both-schema-files, unrelated files never enter context, unrecognised target falls back whole) and `TestOllamaContextWiring` (the real computed safe budget, never a fixed value, reaches the real `OllamaAdapter` constructor via a DI-seam fake; an infeasible prompt is refused before any network call, proven with a monkeypatched tiny `DEFAULT_NUM_CTX` so the refusal is proven to be the real arithmetic, not a coincidence of one fixture's own prompt length). Real-runtime confirmation: a real inference-feasibility preflight ran this exact mechanism against all 8 real corpus entries (8/8 static feasibility) and 3 real `qwen2.5-coder:14b` calls (all PASS, correctly targeted proposals) — see F-0062/F-0063 for what that same preflight found still needed correction |
| F-0059 | **`SYNTHETIC_REGRESSION_SIGNAL`: `regression_delta` was hardcoded `0` in every `RepairBudgetLedger.record()` call inside `run_corpus_repair`, and separately `inspect_product_files`'s own real `_regression_findings(files, baseline)` static check was permanently vacuous because `run_isolated_golden_repair` never passed `baseline=` (defaults to `None`, and the check iterates `(baseline or {}).items()` — zero iterations).** ARK-REQ-0093's canonical "zero regressions in the pre-existing accepted test suite" PASS condition was therefore never genuinely measured for any Golden Repair run to date, including `golden-work-129-repair-2`'s own promoted evidence — a claimed-satisfied MANDATORY PASS condition with no real measurement behind it, the same class as F-0024/F-0058. Found by the same read-only audit, before authorizing `golden-work-129-repair-3` | HIGH | CLOSED | engineering.repair | resolved same session via two independent, deliberately separate fixes. STATIC half: `run_isolated_golden_repair` now calls `inspect_gate(repair_result.files, baseline=parent_files)` — the missing keyword argument, reusing the EXISTING `inspect_product_files`/`_regression_findings` wiring exactly as it already exists, no new check invented. DYNAMIC half (the real test-suite-execution ARK-REQ-0093 actually asks for): a new `_measure_regression(parent_files, child_files)` in `scripts/run_golden_repair.py` re-runs the real, shared `_run_pytest_probe` subprocess mechanism (ARKALI's own interpreter, real `pytest tests -q --tb=no`) once against the parent's own real accepted files as a live baseline and once against the final repaired child's files, and reports the real new-failure-plus-new-error delta — never assumed to be `0`; `None` (never a synthetic `0`) propagates if either real run cannot complete, so a caller refuses promotion rather than trusting an unmeasured zero. Measured ONCE at the final whole-candidate level, deliberately never forced into a per-attempt figure: a per-attempt measurement would be confounded by every OTHER corpus entry's own not-yet-resolved injected defect present throughout the run. `RepairBudgetLedger.consumption.regression_delta` (the C-26 per-attempt ledger field) deliberately stays `0` for every attempt — a documented scope choice, not a synthetic value pretending to be real: `_measure_regression` is Golden Repair's ONE authoritative regression fact, never duplicated in two authoritative places. Proven by `TestRegressionMeasurement` (identical files measure `0`; a newly-broken test measures a real `1`; an unmeasurable probe propagates `None`, never `0`) and `TestStaticRegressionBaselineWiring` (a spy `inspect_gate` proves the real parent baseline, not `None`, now reaches the call) |
| F-0058 | **`FALSE_POSITIVE_LIFECYCLE_PROMOTION`: `run_isolated_golden_repair`'s promotion decision was exactly `not violations and gate_passed` — it never read `repair_result.resolved`/`repair_result.escalated`, never checked any entry's `repairable` flag, and never consulted a real regression measurement.** Discovered by a read-only audit of `golden-work-129-repair-2`'s own real evidence (`GOLDEN_REPAIR_PASS`, only 2 of 7 corpus entries resolved, the other 5 — all declared repairable — escalated) requested before authorizing a second real benchmark run, mechanically confirmed against `scripts/run_golden_repair.py`'s then-current source, not inferred from the evidence alone. The canonical PASS predicate (Master Specification §Golden Repair Benchmark, ARK-REQ-0093) requires every repairable defect resolved, the declared-unrepairable defect escalated, and zero regressions — repair-2 satisfied neither of the first two, yet was promoted, because the code checked only "no protected-path violation" and "the generic structural gate found nothing." A fails-open false-positive lifecycle promotion, the same class as F-0024 ("a MANDATORY requirement was reported as satisfied without an implementation"), against which severity is set | HIGH | CLOSED | engineering.repair | resolved same session: `run_isolated_golden_repair`'s promotion predicate now requires ALL of (A) every `repairable:true` entry in `repair_result.resolved`, (B) every `repairable:false` entry in `repair_result.escalated`, (C) no protected-path violation, (D) `measure_regression(parent_files, repair_result.files) == 0` (F-0059's own new dependency-injected regression signal), (E) the structural whole-product gate PASS (`inspect_gate(files, baseline=parent_files)`, F-0059's static half) — else the EXISTING `FINAL_GATE_FAILED` state is recorded, never a new lifecycle state. `GoldenRepairOutcome` gains `all_repairable_resolved`/`unrepairable_escalated`/`regression_count` fields so the evidence payload records WHY promotion succeeded or failed, per-predicate. Proven by a new `TestPromotionMatrix` suite (`backend/tests/engineering/test_golden_corpus.py`, 8 behavioral cases A–H) plus a dedicated `TestF0058RegressionTest`: a generic proxy for repair-2's own real shape (some repairable entries resolved, some repairable entries escalated, no unrepairable entry present, structural gate PASS) correctly now yields `FINAL_GATE_FAILED`, never `GOLDEN_REPAIR_PASS`, and such a child is proven NOT `begin_acceptance`-eligible; the fully-passing shape (every repairable entry resolved, the declared-unrepairable entry escalated, zero regression, gate PASS) still correctly promotes and remains `begin_acceptance`-eligible; `CandidateLedger`'s own staged-generation promotion logic is untouched, confirmed both by source inspection and `TestPromotionMatrix::test_the_normal_staged_generation_promotion_predicate_is_unaffected`. **`golden-work-129-repair-2`'s own historical ledger state and evidence are NOT rewritten** — its real recorded `GOLDEN_REPAIR_PASS` lifecycle state remains as historical evidence of the bug this finding fixes; the true canonical benchmark verdict for that run was FAIL (2 of 7 resolved, not 7 of 7), which this finding documents rather than hides |
| F-0057 | **`BENCHMARK_BUDGET_SCOPE_MISMATCH`: `run_corpus_repair` held one shared `RepairBudgetLedger` for the WHOLE 8-entry corpus run, while the canonical contract scopes a ledger to one candidate's convergence attempt on ONE DEFECT.** Three independent canonical sources converge on per-entry scope: `docs/contracts/repair.md` §"Anti-loop refusal" states plainly "a ledger is scoped to one candidate's convergence attempt on one defect"; the Master Specification's own Golden Repair Benchmark PASS conditions name each repairable defect resolved "within **its** declared budget" and the unrepairable defect escalated "within budget" (singular defect, singular budget); and `GOLDEN_REPAIR_CORPUS_DEFINITION.md` §1 declares `budget_profile` as a field on each `corpus_entry` individually, which only makes sense if enforcement is genuinely per-entry. Found by a read-only audit requested after the F-0056 fix, before authorizing a second real benchmark run: proven mathematically infeasible under the implemented global scope — by pigeonhole, with `attempts=6` shared across 8 entries, at least 2 entries could never record even one successful attempt regardless of model quality, so entries starved of shared budget escalated on their own first attempt with zero genuine bounded-non-convergence evidence. `golden-work-129-repair-1`'s own real `FINAL_GATE_FAILED` result (already attributed to F-0056) was in fact caused by this same underlying global-scope defect manifesting through `touched_files` first; F-0056's own fix (real diff-based accounting, real timing) remained independently correct but did not resolve this deeper scope defect, which would have caused a second run to fail again via `attempts`/`ai_calls` exhaustion instead. Severity MEDIUM, same "fails in the rejecting direction" family as F-0027/28/29/30/56 (causes premature `ESCALATED`, never a false `PASS`) | MEDIUM | CLOSED | engineering.repair | resolved same session: `run_corpus_repair` now instantiates one fresh `RepairBudgetLedger` per corpus entry (never reused across entries), `RepairRunResult.ledger` (singular) becomes `RepairRunResult.ledgers: Mapping[str, RepairBudgetLedger]` keyed by entry id — the minimal shape change needed, no new aggregate-budget authority, no new ledger/service/registry. `scripts/run_golden_repair.py`'s evidence payload gains real per-defect visibility (`per_defect_evidence`, one real consumption/fingerprint set per entry) plus a clearly-labeled derived `aggregate_consumption` sum, never presented as authoritative. A new behavioral regression test runs the real committed corpus (`golden/repair/golden_repair_corpus.json`) through the real `golden-repair-standard-v1` numeric profile (`attempts=6`/`ai_calls=6`) via a deterministic fake model callback, proving all 8 entries now genuinely process under the real declared ceiling, entry isolation holds (one entry's own budget exhaustion never pre-empts another's), and the real unrepairable entry still reaches genuine anti-loop `ESCALATED`. `golden-work-129-repair-1`'s own historical terminal state, ledger history and evidence remain untouched |
| F-0056 | **`run_corpus_repair`'s `touched_files`/`elapsed_seconds` budget consumption did not measure what their names claim, collapsing Golden Repair's bounded multi-attempt convergence to effectively one attempt per run regardless of the declared per-entry ceiling.** `touched_files` was `len(candidate_files) if candidate_files else 0` — the size of the WHOLE returned file mapping, not the files an attempt actually changed; a real `attempt_repair` that merges its proposal into the full current product (as the real script's own implementation does) charges ~17 touched_files per attempt against a real candidate. `elapsed_seconds` was a hardcoded constant `1` per attempt, never real wall-clock time. Discovered by the first real Golden Repair benchmark run against `golden-work-129` (`golden-work-129-repair-1`, `2026-09-01`): all 8 corpus entries reached `escalated` and 0 of 7 repairable entries resolved, with only 3 real fingerprints ever recorded for the whole run — the `touched_files` ceiling (64) was exhausted by entry 1's own first three attempts (3×17=51), so every subsequent entry escalated on its own first attempt via `RepairBudgetExceededError`, never through genuine repeated-failure evidence specific to that entry. This is a real, benchmark-run-discovered defect, not a corpus-contract violation and not a model-quality issue: the deliberately-unrepairable entry did reach `ESCALATED`, but for the wrong reason, and the real terminal state (`FINAL_GATE_FAILED`) was still honestly recorded — the system never fabricated a `GOLDEN_REPAIR_PASS`. Severity MEDIUM, matching the repository's established "fails in the rejecting direction" family (F-0027/F-0028/F-0029/F-0030): the defect caused premature `ESCALATED`/`FAIL`, never a false `PASS`, so no accepted evidence is affected. Found by a real, explicitly-authorized benchmark run, not by any validator | MEDIUM | CLOSED | engineering.repair | resolved same session: `golden_repair_runner.py` gains a private `_changed_file_count(before, after)` helper (the same set-algebra idiom `changed_protected_paths`/`ledger._manifest_diff` already use, reused not reinvented) that counts only added/removed/content-changed paths between an attempt's input and its proposed output, and real `time.monotonic()` wall-clock timing (ceil'd to whole seconds) replaces the constant `elapsed_seconds=1`. `RepairConsumption`'s own cumulative-counter semantics (`docs/contracts/repair.md` "six-dimensional budget") are unchanged — only what is added per attempt is corrected. Budget enforcement order (real `attempt_repair` call always precedes the ledger's post-hoc refusal) was investigated and left unchanged: it is the documented contract behavior (`docs/contracts/repair.md`: "Recording ... refuses any candidate that would cross a ceiling"), not a defect. `golden-work-129-repair-1`'s own recorded terminal state, ledger history and evidence are historical and were not altered. 8 new behavioral regression tests added (`TestChangedFileCount`, `TestBudgetAccountingFixes`); repeated-strategy anti-loop and unrepairable-entry escalation semantics reconfirmed unchanged by the pre-existing test suite, unmodified |
| F-0055 | **Acceptance-stage outcomes (`GOLDEN_ACCEPTANCE_PASS`/`GOLDEN_ACCEPTANCE_FAILED`/`ACCEPTANCE_INTERRUPTED`) carry no persistent candidate/checker/harness fault classification anywhere.** `GenerationCampaignLedger` (`campaign_budget.py`) records a real `failure_class` for STAGE_FAILED/FINAL_GATE_FAILED outcomes, and `run_staged_generation.py --failure-class` requires one — but `campaign.record()` is only ever called from `run_staged_generation.py`, never from `run_golden_acceptance.py`; `CandidateLedger` records the real terminal acceptance state but has no classification field at all. Investigated whether this is a small, natural extension of the existing campaign-ledger schema: it is not. `CandidateLedger` has no `campaign_id` field at all (the link between a campaign and its candidates exists only inside `GenerationCampaignLedger`'s own ledger.jsonl); `run_golden_acceptance.py` takes no `--campaign-id` and is designed to run independently of any campaign concept, against any frozen candidate a human names; and the `FAILURE_CLASSES` vocabulary (`false_positive_harness_defect`/`model_variance`/`candidate_defect`/`infrastructure_failure`) was designed for STATIC staged-generation preflight failures and does not cleanly cover a DYNAMIC acceptance-harness crash (e.g. golden-work-127/128's own real `ACCEPTANCE_INTERRUPTED` outcomes) — a genuine lifecycle/ownership-boundary crossing, not mere plumbing. Found by read-only repository-grounded investigation (governance-convergence session), not by any validator | MEDIUM | OPEN | engineering.factory | requires a real design decision before implementation: whether `run_golden_acceptance.py` should gain campaign awareness (and `CandidateLedger`/`GenerationProvenance` a `campaign_id` field), and how the acceptance-failure taxonomy should relate to the existing staged-generation `FAILURE_CLASSES` vocabulary (extend it, or a sibling vocabulary under the same owner). Not designed or coded this session, per explicit scope instruction — reported as a gap only |
| F-0054 | **`docs/build/PHASE_HISTORY.md` had gone stale at row 29 (Phase 13) for sixteen real accepted phases (14–29), despite its own declared canonical role ("phase outcomes and commits", `ARKALI_HANDOFF.md` §2) and its listing among the documents `ARKALI_HANDOFF.md`'s own front matter says it "cannot override, amend, reinterpret or substitute for."** Not a benign gap: no control anywhere detects this file's own staleness (only its *existence* is checked, as a `GOVERNED_PATHS` entry in `check_handoff.py`), and a document holding a declared authoritative role that silently stopped being maintained is exactly the "authority decay" pattern F-0046–F-0050 exist to catch elsewhere — never extended to this file. Classified precisely, without assuming "no parser reads it → unimportant": governance debt, not HIGH/BLOCKER (nothing currently depends on its content being current, so no operational risk today), repair required. Found by read-only repository-grounded investigation (governance-convergence session), not by any validator | MEDIUM | CLOSED | control.architecture | resolved: rows 30–46 (Phases 14–29) added, each mechanically re-derived from `git log --diff-filter=A --follow -- docs/acceptance/phase_N_report.json` (cross-checked against commit hashes already independently cited in `BUILD_STATE.md`'s own Phase status table) for Outcome/Commit, and that same table for discharged-requirement counts — never guessed, never taken from conversation memory. Full per-phase narrative deliberately **not** re-authored: `BUILD_STATE.md`'s own Phase status table already carries the equivalent real narrative for every one of these phases, and duplicating it here would be exactly the second, divergent copy F-0002/F-0011 already forbid. A header note in `PHASE_HISTORY.md` records this design choice explicitly, so it reads as deliberate, not incomplete |
| F-0053 | **`test_unrecorded_human_gate_blocks` (`backend/tests/governance/test_phase_gate_checker.py::TestPrerequisitesAndHumanGates`) is a vacuous negative control, not a security defect.** Its own docstring states "the checker must never self-accept a human gate" and attempts to prove it by clearing `checker.state.accepted_human_gates = frozenset()` before calling `checker.check_human_gate("23")`. Mechanically confirmed by reading `arkali/acceptance/checker.py`: `check_human_gate` never reads `state.accepted_human_gates` at all — it calls `_evaluate_human_gate`, which performs an independent, real, scoped lookup against `HUMAN_GATE_RECORDS.md`'s own per-phase grant table (added during the HUMAN_GATE_SCOPE_GAP remediation). The test's own mutation has zero effect on the code path it targets; the test currently passes only because Phase 23's `HUMAN_GATE_2` is genuinely, legitimately granted (`HGR-005-SCOPED`, a real recorded human decision), never because the checker honors the cleared field. Confirmed NOT a self-acceptance security hole — the real scoped-grant lookup is unaffected and correctly independent of the field this test mutates — but a stale test fixture left behind when the real enforcement point moved off `accepted_human_gates`, the F-0017 anti-vacuity class wearing a different hat. Found by read-only repository-grounded investigation (governance-convergence session), not by any validator | MEDIUM | OPEN | acceptance.engine | rewrite the test to exercise the real enforcement point (`_evaluate_human_gate`'s scoped-grant lookup) rather than the retired `accepted_human_gates` field — e.g. a synthetic phase/gate/digest combination with no real `HGR-*-SCOPED` entry must return `BLOCKED`. Not repaired this session; scope was read-only classification only |
| F-0052 | **`handoff_architecture.check_architecture`'s orchestration-depth-path control is unanchored to the live claim; it can validate an incidental historical sentence instead.** `stated("orchestration depth", r"max_orchestration_depth is\s+(\d+)\s+of\s+(\d+)")` runs a plain, unanchored `re.search` over the ENTIRE §3 "Current verified state" section, not scoped to any notion of "the live/current claim" versus historical prose in the same section. Mechanically confirmed: the only text in the current `ARKALI_HANDOFF.md` matching this exact phrase is one incidental sentence buried in the Phase 22B historical narrative ("...restoring the ceiling — max_orchestration_depth is 4 of 4, on the unchanged, unrelated path acceptance.engine -> control.specification -> ..."), not any live statement — `check_depth_path` reads the import chain following THAT sentence, which happens to equal the real measured path, so the control reports PASS by accident rather than by validating anything live. Two regression tests fail as a direct, confirmed consequence (`test_a_stale_orchestration_path_is_detected`, `test_a_removed_orchestration_path_is_detected`, `test_handoff_architecture_drift.py`): both mutate the REAL current live-summary path text (the unicode-arrow `→` form), but `check_depth_path` never sees that occurrence, since the unanchored search already bound to the historical, ASCII-arrow (`->`) sentence instead. One root cause, two test manifestations — recorded as one finding, not two, per this repository's own convention (`docs/build/OPEN_BLOCKERS.md`'s "Status is declared, never inferred" preamble and the F-0046/F-0047 precedent of one finding covering multiple derived controls). Same MEDIUM-severity family as F-0047 (a validator blind-spot in the manifest's own drift-detection fidelity for one narrative claim; the real architecture gates run live and independently PASS, and no accepted phase's evidence is affected). Found by read-only repository-grounded investigation (governance-convergence session), not by any validator | MEDIUM | OPEN | control.architecture | repair requires the outer "orchestration depth" claim match to be bound to the live claim specifically, not merely the first literal-phrase occurrence in the whole section — needs a design decision (e.g. reusing the `(superseded guidance retained for continuity)` marker convention `handoff_transition.py`/BUILD_STATE.md already use to separate live from historical text, or narrowing the search to the row's own opening paragraph) before implementation. Not repaired this session; scope was read-only classification only |
| F-0050 | **`docs/build/BUILD_STATE.md` carries its own separate "Next exact action" section, distinct from the handoff's, and nothing checked it.** F-0046 through F-0049 each added a real, derived control over `ARKALI_HANDOFF.md`'s own prose — but every one of those controls, including `handoff_transition.check_next_action`, read only the handoff's text, never `BUILD_STATE.md`'s. Found by read-only repository-grounded inspection, not by the validator: `docs/build/BUILD_STATE.md`'s top "Current state" banner still read "PHASE 26 ... PHASE 27 unlocked, not started" and its own unnumbered "## Next exact action" section still read "**Begin Phase 14 — Repair / Root Cause / Convergence (C-26).**" while `GovernanceState.current_work_phase()` returned `30` — sixteen real phases later, with the file's own separately-maintained "## Phase status" table already correctly showing Phases 27–29 `MACHINE-ACCEPTED` and Phase 30 unlocked. `check_handoff.py` reported PASS throughout: its `check_head` control only compares `ARKALI_HANDOFF.md`'s claimed generation commit against `GOVERNED_PATHS` changing since, and no existing narrative control ever opened `BUILD_STATE.md` at all. Same root shape as F-0046/F-0048 (a real document read by implementers, never read by the validator that exists specifically to prevent this), on a second document the earlier repairs had no reason to anticipate | MEDIUM | CLOSED | control.architecture | resolved in the F-0050 drift repair: `handoff_transition.check_next_action` gains an optional `label` parameter (default `""`, reproducing the handoff's own original, unlabelled messages exactly) and `NEXT_ACTION_HEADING`'s numeric section prefix becomes optional, so the identical function — not a second mechanism — now also runs against `docs/build/BUILD_STATE.md`'s own "Next exact action" section from `check_transition`, findings distinguished as `"BUILD_STATE.md: ..."`. The check is bounded to the ACTIVE guidance only: both documents already mark historical entries with a literal `(superseded guidance retained for continuity)` marker, and the control now truncates at the first one so an older entry's own foreign requirement ids and phase numbers are read as real history, never as the live brief's claimed subject — proven necessary directly: without the truncation, BUILD_STATE.md's own preserved Phase 14/11/10/9/7/4 historical entries triggered thirteen false positives on ids those phases already discharged. `BUILD_STATE.md`'s own banner, "## Phase status" Phase 30 row and "## Next exact action" section were then corrected to state the real, current, in-progress Phase 30 status with no candidate-level detail duplicated from `ARKALI_HANDOFF.md` §8/§9 (F-0002/F-0011: a second copy re-rots). Nothing is hard-coded: no phase, requirement id or count is named in `handoff_transition.py`, matching `TestTheControlsAreDerived`'s existing structural proof, extended to cover the new code. Three new regression tests in `test_handoff_transition.py::TestBuildStateCarriesItsOwnNextExactAction` prove the real, current `BUILD_STATE.md` passes; a synthetic brief naming a stale phase is detected; and a synthetic brief naming the real current phase with a real denominator is not flagged (anti-vacuity) |
| F-0049 | **The Phase 13 Package 5 handoff refresh advanced the machine-readable next-action claim but left the live §8/§9 narrative on Package 5 and omitted §9's denominator.** `check_handoff.py` therefore reported PASS on the committed tree while the permanent transition mutation control could not construct its wrong-denominator mutation and failed its own anti-vacuity assertion. The stale prose could misdirect continuation into already delivered work. This is the F-0048 transition-residue family, found before any Phase 13 gate run | MEDIUM | CLOSED | control.architecture | resolved before Package 6 acceptance: §8/§9 now state Packages 1–5 delivered and Package 6 next with the register-derived denominator; `handoff_transition.py` requires the denominator claim to exist before reconciling its value, so deletion is detected rather than making the mutation control vacuous |
| F-0024 | **A MANDATORY requirement was reported as satisfied without an implementation.** `ARK-REQ-0111` (stronger verification profile for Protected Core changes) is assigned to **Phase 4** by the register's Phase column and owned by `acceptance.engine`. The Phase 4 report listed it among the requirements it discharged — exactly what Phase Gate Checker C2 consumes — so C2 passed on an assertion never earned | HIGH | CLOSED | acceptance.engine | resolved: `acceptance/verification_profile.py` implements the profile, checker gains C6 and PROTECTED_CORE, `phase_4_traceability.json` records every claim, and the guard rejects a discharge the traceability record does not support |
| F-0026 | **The superseding re-acceptance rule was governance-only.** GOV-001 required explicit re-scoring authorization before a previously accepted phase may be re-scored, but nothing mechanically prevented an implementing actor from re-running `run_phase_gate.py` against an already-accepted phase and recording the verdict. The rule depended on the actor following it — the dependency F-0024 showed to be unsafe, and the F-0020 lesson that a threshold without a mechanism is not a control | MEDIUM | CLOSED | acceptance.engine | resolved: `acceptance/rescoring_authorization.py` is consulted by the checker. An already-accepted phase without a granted authorization bound to its exact evidence package yields `AWAITING_RESCORING_AUTHORITY`, never acceptance. 35 controls |
| F-0025 | **The open-findings parser decided state by substring search.** `_parse_open_findings` skipped any row whose text contained `CLOSED` anywhere, so a finding could be hidden by its own prose. Discovered when F-0024 failed to register because it quoted a field name ending in `_closed`. A governance parser that hides a finding fails **open** | MEDIUM | CLOSED | acceptance.engine | resolved: `acceptance/findings.py` reads a declared Status cell only; malformed rows stop acceptance instead of vanishing; 34 controls |
| F-0027 | **Structure check 12 is phase-scoped and would reject legitimate Phase 5 constructs.** "No forward-phase runtime construct" rejects ORM-engine and web-application construction anywhere in shipping source; its own comment concedes those "belong to Phase 5 and beyond". Sixth instance of the family (F-0008, F-0013, F-0016, F-0019, F-0022). Proven mechanically before repair: a `create_engine(` probe in `kernel.persistence` — the context `ARCHITECTURE.md` §3 assigns the DB engine to — drove the validator to exit 1. Severity MEDIUM, matching F-0019/F-0022: the check fails in the **rejecting** direction, so no defective work was ever admitted and no accepted phase is affected | MEDIUM | CLOSED | control.architecture | resolved: check 12 is now context/authority-scoped. A runtime construct is permitted only inside the context canonical architecture makes responsible for it, resolved from `ARCHITECTURE.md` §3 against `AUTHORITY_MAP.yaml` with no path list in the validator. Tracked and untracked scope; fails closed on unresolvable authority or ownership. 23 controls |
| F-0028 | **`ForbiddenDependencyDirectionGate` does not consume `dependency_rules`.** `AUTHORITY_MAP.yaml` declares `policy_callable_from_any_layer: true` and `evidence_write_from_any_layer: true`; `ARCHITECTURE.md` §4 rules 5 and 6 declare the same exceptions; `AuthorityMap` never parsed the section and the gate implemented rank plus sibling edges only. Proven with temporary fixtures: both canonically permitted edges were reported as violations. Severity MEDIUM — the divergence fails **closed**, so no forbidden edge was ever admitted and Phases 2–4 remain validly accepted | MEDIUM | CLOSED | control.architecture | resolved: `AuthorityMap` parses `dependency_rules` and owns `edge_permitted()`; the gate consumes it. Exemption subjects are read from the rule keys, resolved namespace → final segment → layer, so `evidence` grants `evidence.*` and not `acceptance.engine`. Omission defaults strict; an unresolvable enabled subject raises. 39 controls including map-only drift proofs. No sibling edge added; the live tree uses neither exemption |
| F-0029 | **`check_handoff.py` could not express a phase that had started but was not accepted.** The current-phase derivation required both `UNLOCKED` and `NOT_STARTED` in the status text, which held only while every phase was delivered in one commit. Phase 5 is the first delivered in atomic packages. Severity MEDIUM: it fails in the **detecting** direction — the manifest would have reported drift, not a false PASS — and no accepted phase is affected | MEDIUM | CLOSED | control.architecture | resolved: the current work phase is the single phase declared `UNLOCKED` that `PhaseStatus.is_accepted` reports as not accepted. No second acceptance model; exactly one such phase still required; no progress state is enumerated, so a further package cannot expire it |
| F-0030 | **Package 1's engine-confinement control was stricter than ARK-REQ-0012.** It banned the `sqlalchemy` import outside `kernel.persistence`; the canonical rule forbids *engine-specific SQL* and ADR-0006 presupposes the ORM. It would have refused C-12 its declarative mapping. Severity MEDIUM: it fails in the **rejecting** direction and no accepted phase relied on it | MEDIUM | CLOSED | control.registry.project | resolved: rule restated to the canonical property and strengthened — drivers, dialect modules and engine-construction calls are now named and confined; new controls prove the neutral ORM is permitted, the owning context really does build the engine, and `records.py` binds to no engine |
| F-0031 | **Two re-scoring controls hard-coded Phase 5 as "an unaccepted phase" and expired on its acceptance.** The mechanism was correct throughout — the checker returned BLOCKED for a phase that now has an acceptance record, which is GOV-001 working. Verified mechanically before editing: phase 6 still takes the first-acceptance path. Eighth instance of the family; the subject is now derived from `GovernanceState` rather than named, and the control set was strengthened with a proof that one phase's authorization cannot be reused by another. MEDIUM, failing in the **detecting** direction | MEDIUM | CLOSED | acceptance.engine | repaired in the Phase 5 acceptance commit |
| F-0032 | **Three tests transcribed the migration-chain head and expired when Phase 6 added a revision.** The mechanism was correct throughout - the substantive assertion in each test was already passing, and the chain remained linear and forward-only. Repaired by deriving the head from the declared revision files rather than naming it. Ninth instance of the family. MEDIUM, failing in the **detecting** direction | MEDIUM | CLOSED | kernel.persistence | repaired in the Phase 6 Package 1 commit |
| F-0033 | **Alembic's autogenerate target held one table out of seven.** `alembic/env.py` set `target_metadata = PersistenceBase.metadata` after importing only the base. That object is populated by the *import side effect* of the modules declaring mapped classes, so the migration tool's picture of the schema was whatever happened to be imported - one table. Autogenerate does not under-report a table it cannot see; it proposes DROPping it. The same import dependency made `test_migrated_schema_matches_the_orm_metadata` order-sensitive: it passed under `pytest -q` and failed under `pytest tests/persistence -q`. Latent since Phase 5 and made visible by Phase 6 adding two contexts' tables. MEDIUM: no migration in the chain was autogenerated, all four are hand-written, and the comparison control failed in the **detecting** direction, so no defective schema was ever admitted and no accepted phase is invalidated | MEDIUM | CLOSED | kernel.persistence | resolved: `env.py` declares `MAPPED_RECORD_MODULES` and imports each, at the migration composition root (outside `backend/arkali`, so no layer direction is crossed - the kernel cannot import rank 1 and rank 2 contexts). Three new controls in `TestAutogenerateTargetIsComplete` derive the mapped-module set from the source tree by AST and fail if `env.py` omits one, declares one that maps nothing, or declares one without importing it; the schema comparison now loads the derived set first, so its verdict no longer depends on collection order. Both mutations - dropping a module from the list, and declaring it without importing it - were proven to fail the controls |
| F-0034 | **The phase-status parser decided acceptance by prose substring search, and could report an unaccepted phase as accepted.** `PhaseStatus.is_accepted` returned `"ACCEPTED" in status_text.upper() and "NOT ACCEPTED" not in status_text.upper()`. Two conjoined mistakes: the search ran over the whole free-text cell, and acceptance was inferred from the *absence* of a negation rather than from an affirmative declaration. Any explanatory prose containing the token therefore accepted the phase — "Prerequisites Phases 5 and 6 are accepted" on **Phase 7's own row** made Phase 7 accepted, as did quoting `MACHINE-ACCEPTED` as history, as did writing `NOT_ACCEPTED` with an underscore. This is a **fail-open in the acceptance path**: `checker.check_prerequisites` reads the same property, so a phase that was never implemented could satisfy a later phase's prerequisite check. Same mechanism as F-0025 (governance parser deciding state by substring); same consequence class as F-0024 (acceptance asserted without being earned), which is why the severity follows F-0024 rather than F-0025. Discovered during Phase 6 post-acceptance recording and worked around by wording; the trap was already documented in F-0029's record and is now repaired rather than avoided | HIGH | CLOSED | acceptance.engine | resolved: acceptance is read from a **declared** state. `parse_declared_state` takes only the leading segment of the cell — the bold run every live row already uses — splits it into a primary state and an optional qualifier, and matches the primary against the canonical vocabulary `DeclaredPhaseState`. Only `ACCEPTED` and `MACHINE-ACCEPTED` grant acceptance, so absence of a negation can no longer accept anything. A qualifier may narrow a state but never reverse it: an accepting head carrying a negation, or a non-accepting head carrying a bare acceptance token, is **refused** as ambiguous, as is an unknown or empty state — the `findings.py` shape from F-0025. `is_locked` no longer reads `LOCKED` out of `UNLOCKED`, and `check_handoff.py`'s unlockedness comes from the same declared state rather than a second prose search. Backward compatibility proven over every live row: no phase changed meaning. 37 controls in `test_phase_status_parser.py`, none naming a phase number; 8 mutations injected and all 8 caught |
| F-0036 | **A durability predicate was named for the heartbeat and measured the deadline.** Package 2 shipped `JobExecution.heartbeat_expired`, which resolved through `_expired` to `clock >= attempt.deadline_at` and **never read `heartbeat_at` at all** — proven by AST before anything was edited: the function's attribute reads were `{_clock, deadline_at}`, and the only reader of `heartbeat_at` in the module was the writer that sets it. The name promised *silence since the last proof of life*; the behaviour measured *elapsed work*. The two are independent — a worker that heartbeated this instant is past its deadline eventually, and a worker silent for a week is not — so the function was a correct `timed_out` under a name that described a different fact. Package 2 was unaffected because both of its callers meant timeout and `timed_out` merely delegated to it. It would have become a real defect at the moment of consumption: `EXECUTION_AND_CAPABILITY.md` §3 keys crash recovery on a job "without a live heartbeat", so Package 3 reading the existing name would have recovered live workers past their deadline and left genuinely dead ones holding their jobs for the whole timeout. MEDIUM: no accepted phase relies on it — Phase 7 is not accepted — and the misnamed predicate never produced a wrong answer to the question its callers were actually asking | MEDIUM | CLOSED | execution.durable | resolved in the Phase 7 Package 3 commit: the conflated name is **gone rather than reinterpreted**. `timed_out` is now the primary and reads `deadline_at`; heartbeat liveness is `JobRecovery.heartbeat_stale`, reading `heartbeat_at` against a new persisted per-job `heartbeat_timeout_seconds` (migration `0007_job_type_registry`), because a bound that is not recorded on the job cannot survive the restart it exists for. Two derived permanent controls in `TestTheHeartbeatTermStaysDistinctFromTheDeadline`: any function in the context whose name mentions the heartbeat must read `heartbeat_at` (resolved one level through private helpers, so delegation cannot hide it), and the timeout rule must read `deadline_at` and must **not** read `heartbeat_at` — so the repair cannot swing the other way. Both directions are also proven behaviourally: a silent execution is recovered long before its deadline, and one reporting normally past its deadline is not stale. The old name is banned by a negative control |
| F-0037 | **A contract control assumed every backend route was also a browser-slice route, and expired when one legitimately was not.** `test_the_client_covers_every_route_the_backend_serves` asserted that the TypeScript client's endpoint table covered every path the FastAPI application publishes — "an unused route is a gap, not slack". True and useful for the Phase 5 vertical slice, where every route existed to be driven by the browser. Package 4 added `POST /api/jobs` and `GET /api/jobs/{job_id}` for `ARK-REQ-0027`, which the register owns to **`execution.durable`** with `arch, integ` evidence and no `e2e`; re-derived mechanically, **no Phase 7 requirement is owned by any `surfaces.*` context**, so no frontend obligation exists and none was invented. Proven stale before anything was edited: the assertion failed on exactly those two routes and nothing else, every Phase 5 slice route was still covered, and the invariant the control actually protects — the client never calling a route the backend does not serve — still held. **Tenth instance of the premise-scoped-check family** (F-0008, F-0013, F-0016, F-0019, F-0022, F-0027, F-0029, F-0031, F-0032). MEDIUM: it fails in the **rejecting** direction, so no defective work was ever admitted and no accepted phase is affected | MEDIUM | CLOSED | surfaces.command | resolved in the Phase 7 Package 4 commit: each route now DECLARES its audience as an OpenAPI tag on the router — `browser-slice` or `backend-only` — and the control reads it back off the live document. The audience is not a list in the test, so a route added later with no tag, two tags, or a tag nobody declared **fails closed** instead of choosing its own exemption. The surviving half is kept and narrowed to the routes it was really about (`test_the_client_covers_every_browser_slice_route`), and a control the old one could not express at all was added: the browser client must call **no** backend-only route, which is drift in the opposite direction and would have passed the old coverage assertion by making the two sets match |
| F-0038 | **One durable timestamp had two wire representations.** Every persisted timestamp in the repository is UTC, but SQLite has no timezone type, so a row still live in the session returns an *aware* datetime while the same row read back off the disk returns a *naive* one. `ProjectResponse.created_at` therefore serialised as `2026-08-09T19:10:47.252643Z` from `POST /api/projects` and as `2026-08-09T19:10:47.252643` from `GET /api/projects/{id}` — proven on the **Phase 5 routes alone**, with no Package 4 code involved, so this is a pre-existing defect that Package 4's restart evidence exposed rather than caused. The instant is identical; only its rendering differs, and it differs on nothing the caller can see or control. A client comparing, caching, hashing or diffing a reference saw drift with no domain meaning behind it. Package 2 had already met the same SQLite behaviour internally and answered it with `_as_utc`; the transport boundary had no equivalent. MEDIUM: no value is wrong and nothing fails open — the defect is contract stability on a shipping surface | MEDIUM | CLOSED | surfaces.command | resolved in the Phase 7 Package 4 commit: a `TransportModel` base normalises every `datetime` field crossing this boundary to UTC-aware, and every timestamp-carrying response model derives from it. The value is never converted — a naive column is already UTC by `kernel.persistence` contract, so this attaches the timezone it always had and changes no instant. Normalising at the base rather than in each projection means a route added later cannot forget, and the rule sits with the boundary it governs. Both directions are asserted across a real dispose/reopen |
| F-0039 | **A policy denial could not reach the status the mapping declared for it, and the control that should have caught it proved the wrong thing.** `error_mapping.STATUS_BY_ERROR` maps `PolicyDenied → 403`, but every route calls `guard()` *before* its `try`, so a real denial escaped the handler entirely and surfaced as **HTTP 500**. Reproduced on the **Phase 5 routes alone** under a real PDP loaded from an authority map with `WRITE_WORKSPACE_FILE` fixed to DENY: `POST /api/projects` returned 500 where the table said 403. Pre-existing since Phase 5 and exposed by Package 4's error-contract obligation. **The existing control asserted `status_for(PolicyDenied("x")) == 403`** — it proves the *table* and never the *route*, which is the F-0017 shape: a control that passes for a reason unrelated to the property it claims. MEDIUM, not HIGH: nothing failed open. The PEP still refused, the operation never ran and no row was written; what was wrong was the client's ability to tell a refusal from a broken server, and a 500 is not a success | MEDIUM | CLOSED | surfaces.command | resolved in the Phase 7 Package 4 commit: the application registers one exception handler for the single base every mapped refusal derives from, so the mapping is reachable from anywhere a domain error can be raised — including `guard()`, and including routes added later. Fixing it there rather than moving each `guard` inside a `try` means no route can reintroduce it by forgetting. An unmapped error still propagates unchanged: `status_for` returning `None` still means this surface has no opinion, and inventing a status would be the failure mode the mapping exists to prevent. Two controls replace the table-only assertion — a real denying PDP driven through a real route must return 403 and leave no row, and every mapped type must derive from the registered base, so a type whose mapping is unreachable fails |
| F-0040 | **The traceability loader assumes every phase has at least one requirement, and four canonical phases do not.** `TraceabilityRecord.load` refuses a record whose `claims` list is empty (`requirement_claim.py:88`, `raise refuse("traceability record declares no claims", source)`). The refusal consults no denominator: reproduced against the untouched tree, an empty record is refused **identically** for Phase 4 (denominator 33) and Phase 8 (denominator 0). `RequirementRegister.for_phase` returns **zero** requirements for phases **8, 15, 33 and 34**, all four of which appear in the canonical implementation phase list, so for those phases the only truthful record is refused while every non-empty record would assert a claim the phase does not own. C6 therefore fails either way and a zero-denominator phase cannot be machine-accepted. **The invariant has no canonical source:** the string "traceability" appears nowhere in `CLAUDE_ARKALI_GENESIS_V2_BUILD_PROTOCOL.md` or the VDC and no ADR governs it — the record is `acceptance.engine`'s own mechanism, introduced by the F-0024 remediation, whose stated purpose is to make a *false discharge* impossible. Proven in memory that the refusal is not load-bearing for that purpose: against an empty record, `reconcile_discharge` already refuses every non-empty discharged set ("discharged but has no traceability claim", "discharged but absent from the register"). The only case the loader's refusal uniquely blocks is the empty-discharge case — which is exactly the honest zero-denominator shape. **The C-17 report contract is already zero-aware**: `PhaseReport.ALLOWED_EMPTY` explicitly contains `ark_req_ids_closed`, so the report half of the same acceptance package permits what the traceability half forbids. MEDIUM: the rule fails in the **rejecting** direction — it refuses legitimate work and has never admitted defective work — and no accepted phase is affected, because phases 2–7 all had non-empty denominators | MEDIUM | CLOSED | acceptance.engine | resolved by the standalone pre-Phase-8 governance repair: emptiness is now judged against the **denominator** instead of absolutely. `TraceabilityRecord.load` keeps every well-formedness refusal (missing file, unparseable, non-list `claims`, duplicate id, unknown state) and stops treating an *empty but well-formed* list as malformed; the entitlement question moves to check C6, where the register already lives, as `discharge_shape.check_claim_shape`. **Both directions fail closed, and the second is strictly stronger than anything that existed before:** a phase claiming nothing while the register assigns it requirements is refused (the pre-F-0040 protection, preserved exactly), and a phase whose denominator is empty may not claim *anything* — so a synthetic, contract-id or foreign-phase claim can no longer be manufactured to satisfy an invariant, which the old rule permitted for every phase. `reconcile_discharge` is **unchanged**, and a false discharge against an empty record is still refused by name. The subject is derived from `RequirementRegister`, never listed, so assigning a requirement to phase 8/15/33/34 moves the controls instead of expiring them. `checker.py` hit the 400 logical-line budget and `test_false_discharge.py` hit it too; both were answered by decomposition under ADR-0008 (`discharge_shape.py`, `test_zero_denominator_shape.py`), not by an exception. Classified **PROTECTED_CORE** by `select_profile` (member `acceptance.engine`); security review 188, adversarial review 547 and full regression 1623 all at exit 0. **9 mutations injected, 9 caught** — one initially missed and exposed a real gap: every shape control exercised `check_claim_shape` directly, so none proved C6 actually *calls* it, and a mutation that made C6 ignore the result passed. A real end-to-end control over a copied canonical document tree now drives `PhaseGateChecker.check_discharge_integrity` itself. No accepted phase's record changes meaning, asserted over all six live records |
| F-0035 | **Blank lines split this findings table, and every row below the first fragment was invisible to the acceptance gate.** `findings._collect_tables` ends a table at a blank line and treats the next row as a new header, so a data row became a header whose cells are prose — `declares_id` was false and `is_finding_table` refused the fragment. Four blank lines had accumulated inside the internal blockers table, hiding **F-0027 … F-0034** from `stopping_findings` entirely. Every hidden row happened to be CLOSED, so no false PASS was ever produced; but the first OPEN HIGH written into the natural place — F-0034, above — was silently ignored, which is how this was found. Same fail-**open** class as F-0025 and the same control, reached through document layout rather than through the parser. Severity MEDIUM follows F-0025, whose blast radius is likewise finding *visibility* rather than the acceptance decision itself | MEDIUM | CLOSED | acceptance.engine | resolved: the four blank lines were removed, so every finding row sits under the one header again and all thirteen rows are visible to `stopping_findings`. No row's content was altered — this is layout, not a rewrite of recorded history. A permanent control derives the subject from the document rather than listing it: any line shaped like a finding row must appear in `parse_findings`, so the next blank line that hides one fails the suite. `findings.py` was deliberately **not** changed: its table detection is Protected Core and correct for a well-formed document, and the defect was the document's layout |
| F-0041 | **Two governed phase titles disagreed with the canonical matrix, and nothing compared them.** `BUILD_STATE.md` restates each phase's title beside its status. Phase 8 read `Execution Scheduler + Worker Contract` against the matrix's `Resource Scheduler + Worker Contracts`, and Phase 6 read `Artifact / Evidence Plane` against `Evidence Plane + Provenance + Artifact Store`. `IMPLEMENTATION_DEPENDENCY_MATRIX.md` is the canonical authority for a phase title and `ARCHITECTURE.md` §79 corroborates it for Phase 8. The titles were transcribed rather than derived, so they drifted silently - the F-0002/F-0032 family. Discovered mechanically by the new reconciliation control, not by reading | MEDIUM | CLOSED | control.architecture | resolved in the Phase 8 Package 1 commit: both titles now match the matrix, and `test_every_build_state_phase_title_matches_the_matrix` reconciles **every** phase both documents name, so a third drift cannot hide. Severity MEDIUM: `PhaseStatus.title` is parsed but consumed by no acceptance decision, so no accepted phase relied on the wrong value and no gate verdict is affected |
| F-0046 | **The handoff validator reported PASS while the handoff contradicted itself.** `check_handoff.py` compared only the machine-readable `ARKALI-HANDOFF-CLAIMS` block — head, branch, tree, requirement counts, accepted phases, unlocked phase, gates, ADR counts, open findings, cumulative verified, source existence, next-action phase — and read **nothing** in the prose around it. A §12 refresh therefore updated the YAML plus whichever sections the author remembered, and the validator confirmed the result, which is worse than no check because it manufactured confidence. Found by read-only inspection, not by the validator. Concretely at `15542e7`: §8 was still titled "Phase 8" and declared it IN PROGRESS with Package 3 not started, while §3 and the summary correctly said MACHINE-ACCEPTED; §4 carried a duplicate ledger row and **two** generation markers naming different commits, and was missing its final rows; §6 described the canonical Python 3.13 as absent/NOT_CONFIGURED while the suite was running on 3.13.15, and carried five stale package versions; §7 and §3 both asserted execution surfaces absent that Phases 5, 7 and 8 built; §8 also mis-stated Package 1's verification profile as PROTECTED_CORE when `select_profile` derived NORMAL. **Contributing cause:** commit `f9c983a` appended a duplicate ledger row out of band, and every later refresh asserted only that ITS OWN anchor was unique, so none could see the stray line | MEDIUM | CLOSED | control.architecture | resolved in the F-0046 drift repair: `check_handoff.check_narrative` adds seven controls that read the document itself — exactly one generation marker, no commit cited twice, the ledger reaches the recorded head, the current-phase section names `GovernanceState.current_work_phase()`, that section states the phase's declared state, no accepted phase is described as unaccepted there, the canonical interpreter is not reported absent while `requires-python` is satisfied, and no claim that execution surfaces are absent while one is built. **Every subject is DERIVED** from `GovernanceState`, the register, `pyproject.toml`, the filesystem and git; no phase number, version or commit is hard-coded, so the controls follow the repository instead of expiring with it. All seven were verified to FIRE against the stale document before it was repaired. Severity MEDIUM: the handoff is explicitly an index and not an authority, no acceptance decision consumed the stale prose, and repository truth was correct throughout — the defect is in the index and its validator, following F-0029 and F-0035 |
| F-0047 | **The handoff's live architecture summary was never compared with the architecture gates.** F-0046 added seven narrative controls, but every one is a predicate over GOVERNANCE state — which phase is current, what it declares, which commit a revision was generated at, whether the interpreter and the execution surfaces exist. `check_handoff.py` never imported `control.architecture` at all, so **no architecture number in the manifest had ever been compared with anything**: §3's gate count, cross-context edge count, nine budgets, measurement contract, two fan-in figures, orchestration depth and depth path were author-maintained transcriptions with zero mechanical backing — the F-0002/F-0011/F-0041 transcribed-value family. Phase 9 Package 1 added exactly two cross-context edges (`control.registry.provider → kernel.contracts` via `error_base`, from `errors.py` and `provider_authority.py`), the mandatory §12 refresh at `dc938e8` did not re-derive §3, and the manifest reported **100** edges while the live gates measured **102** — with the validator reporting PASS. Confirmed by recomputation before any edit: `ForbiddenDependencyDirectionGate` and `ForbiddenCyclesGate` both report 102, and removing Package 1's three files from the scan reproduces exactly 100, so the prior figure was truthful when written and expired on the next package. **Three further self-reference claims drifted by the same mechanism and were found in the same audit:** §1 carried a `Current HEAD` commit twelve commits stale (`3574e04` against the recorded `8102f60`); the `← HEAD at generation` marker sat on ledger row 83 while the recorded head is row 85, and §11 asserted that "a derived control" bound the marker to that commit when the control only COUNTED markers — the manifest overstating its own validator; and rows 84–85 were written above rows 80–83, so the ledger was out of order. Every other audited architecture metric was verified CORRECT and left alone: 8 gates all PASS, 0 violations, 9 numeric budgets under ratified contract 1.0.0, `kernel.contracts.errors` and `kernel.contracts.state_machine` both 15 of 15 fan-in, depth 4 of 4 on `surfaces.command → execution.durable → control.policy → kernel.contracts`. The historical figures in §4 and §11 (91, 86, 82, 77 edges) are truthful records of past commits and were **not** rewritten | MEDIUM | CLOSED | control.architecture | resolved in the F-0047 drift repair: `check_handoff.py` gains `architecture_truth`, `check_architecture`, `check_depth_path` and `check_fan_in`, plus three self-reference controls. **NO SHADOW MODEL** — the F-0028 rule applied to a validator. `GateRunner.run_all` is executed and every number is read back out of the `CheckResult` objects it produced; the depth and path come from `ArchitectureBudgetGate.measure_depth`, which `control.architecture` had already made public so that "the gate's own evidence and the drift controls read the same measurement rather than two implementations of it"; and fan-in is obtained by re-running the canonical budget gate against an IN-MEMORY authority map whose `max_fan_in_per_module` is lowered to zero, so the gate reports its own measured value for every module. The edge count is taken from **two independent gates** and refused unless they agree, so one gate being reworded cannot quietly become its sole source. **Nothing is hard-coded**: no edge count, no phase number, no commit, no module name — the fan-in subjects are resolved out of the measurement and an unresolvable or ambiguous subject fails closed. **Anti-vacuity is explicit (F-0017):** every claim must be PRESENT, so deleting or rewording a metric fails the run instead of comparing nothing, and the depth-path search is anchored to the depth claim because §3 legitimately names other import chains. **Scope is the live section only** — §4 and §11 record what was measured at past commits and re-deriving them would destroy evidence. `backend/arkali/` is untouched: no provider runtime, no consumer enforcement, no Protected Core module and no accepted evidence was modified, and no phase gate was run. **17 mutations injected, 17 caught**, each against the real file through the real CLI with the original bytes restored and re-verified by sha256 between every case; 26 permanent negative controls added to `test_handoff_drift.py`, every mutation derived from live truth so none can expire (F-0021). All four live drifts were proven to FIRE against the stale document before it was repaired. Severity MEDIUM, following F-0046, F-0029 and F-0035: the handoff is explicitly an index and not an authority, no acceptance decision consumes §3, the architecture gates themselves were correct and PASSING throughout, and the manifest UNDERSTATED a metric in a repository whose real posture was compliant — the defect is in the index and its validator, and it failed in neither the accepting nor the rejecting direction of any gate. **COMPLETING REPAIR — the first F-0047 pass was incomplete, and this record is extended rather than a new finding being opened.** The audit above was scoped to architecture metrics and to the manifest's commit self-references, so it did not reach the adjacent **finding summary**, which is the same transcribed-value family and which the act of recording F-0047 itself made stale: §3 still read "every finding through **F-0046** is closed" while `OPEN_BLOCKERS.md` recorded F-0047, and `check_handoff.py` still reported PASS. The machine-readable `open_blocker_high` claim was validated throughout, but it carries only the STOPPING set, so a MEDIUM finding could be recorded without any control noticing that the prose summarising the recorded set had stopped one identifier short. Closed by `scripts/handoff_findings.py`: the finding set is parsed by `arkali.acceptance.findings` from the file `governance_state` declares canonical, so the manifest and the acceptance gate read one record through one parser, and seven controls reconcile the live summary with it — it must name the latest recorded identifier, no "through F-xxxx" statement may close the set at an older one, no identifier newer than the record may be invented, the latest finding's declared severity and status must both be stated, no mention may assert the opposite status, the stated BLOCKER/HIGH pair must equal the live stopping count, and every stopping finding must be named. **Nothing is hard-coded** — no identifier, no severity, no count — and status is read from the token NEAREST a mention, so ordinary prose and the file name `OPEN_BLOCKERS.md` are not misread as assertions. The MEDIUM and LOW totals are deliberately still not checked, because the manifest's decision not to mirror them is itself the F-0002/F-0011 remedy and a control demanding a number would recreate the defect. **No self-referential recursion:** the controls require the summary to name whatever identifier is latest, never a finding about the manifest, and a new finding and the summary naming it are written in the SAME repair commit, so no commit is ever internally inconsistent and no finding forces another. 9 further mutations injected and 9 caught, across both the manifest and the canonical finding record, with sha256-verified restoration of every mutated file |
| F-0048 | **A phase moving from in-progress to ACCEPTED left transition residue in the two places no control looked.** The §12 refresh after Phase 9's machine acceptance PREPENDED the acceptance sentence and rewrote the *opening* of NEXT EXACT ACTION, but left the previous state's tail intact in both. §3's Phase 9 row therefore read `**MACHINE-ACCEPTED** … **3/3 requirements discharged**` and then, in the same cell, "**Packages 1, 2 and 3 of 4 complete**", "**none is discharged yet**" and "cumulative verified stays 80" — three statements the phase's own accepted `phase_9_report.json` and `phase_9_traceability.json` contradict. Worse, the tail of §9 still instructed the reader to "Read `REQUIREMENT_REGISTER.md` for the rows whose Phase column is **9**", "`CONTRACT_INVENTORY.md` for **C-11**'s row" and "**Phase 9 has a NON-ZERO denominator — three requirements.** `ARK-REQ-0052`, `ARK-REQ-0053` and `ARK-REQ-0219`" — *inside the live continuation brief for Phase 9B*, whose real denominator is `ARK-REQ-0046`, `0047` and `0048`. That is not merely stale prose: it points the next implementer back into an accepted phase's closed scope, and the three ids it names are discharged. **WHY NOTHING CAUGHT IT.** Every existing narrative control is scoped either to the machine-readable claim block or to the CURRENT-PHASE section. F-0046's controls check the ledger, the generation marker, the interpreter, the surfaces and the current-phase section; F-0047's check the live architecture and finding summaries. None reads §3's **per-phase rows** for self-consistency against an accepted phase's own evidence, and the "NEXT EXACT ACTION targets the unlocked phase" control validates the **YAML claim**, not the body — so the section's opening was rewritten, passed, and its tail was never examined. Third instance of the same scoping habit: each repair covered the class of drift it had just seen rather than the class of claim (F-0046, F-0047). The machine-readable block was correct throughout — `head`, `accepted_phases`, `unlocked_phase`, `verified_by_phase` and `cumulative_verified: 83` all agreed with the repository — which is exactly why `check_handoff.py` reported PASS | MEDIUM | CLOSED | control.architecture | resolved in the F-0048 drift repair: `scripts/handoff_transition.py` adds eight derived controls. An accepted phase's live row may not claim its requirements undischarged, may not describe its packages as incomplete, and may not describe itself as in progress; a live cumulative total may not fall below what the accepted traceability records already account for; the **whole** NEXT EXACT ACTION body must name the current work phase, may present no requirement a previous accepted phase discharged as a continuation subject, and any denominator it states must be the current phase's size. **Anti-vacuity is derived, not asserted**: every phase whose accepted record discharges anything must HAVE a live row, because the first draft was defeated by its own mutation battery — deleting the row simply removed the thing to be judged. **Nothing is hard-coded** — no phase, requirement id, contract id, count or package total; the accepted set, each phase's discharge, the current phase and its denominator all come from `GovernanceState`, the accepted traceability records and the register, so a further phase moves these controls instead of expiring them. Package completion is **not** parsed from prose: the live row now points at the phase's accepted evidence as the authority for what each package delivered, and the control only refuses a *partial* claim, which is what the residue actually was. Severity MEDIUM, following F-0046 and F-0047: the handoff is explicitly an index and not an authority, no acceptance decision consumed the stale prose, Phase 9's own artifacts and verdict were correct and byte-identical throughout, and the defect is in the index and its validator — but it is the first of this family whose consequence would have been *misdirected work* rather than a merely inaccurate summary, which is why it is not LOW. **9 mutations injected, 9 caught**, eight against the manifest and one against the record side (accepting the current phase while the manifest keeps its in-progress prose), each restored with sha256 verification |
| F-0045 | **The Phase 8 report recorded two validator runs at exit 0 that were not in that state when the gate ran.** `phase_8_report.json` claims `check_repository_structure.py` exit 0 / 12 passed and `check_repository_structure_negative.py` exit 0 / 23 passed. Both validators were genuinely green earlier in the phase, but they were **not re-run after `test_phase_8_journey.py` was added**, and that file's docstring quoted the canonical phrase "never a stub" — which structure check 10 (no fake-implementation markers) forbids in tracked Python outside `scripts/`. Reproduced mechanically against the submitted commit `bb845d8`: the validator reports **11 pass, 1 fail**. Had the true exit codes been recorded, C3 ("non-zero exit code reported under a PASS status") and C5 would have FAILED and the verdict would have been **PHASE_BLOCKED** rather than `PHASE_ACCEPTED_BY_MACHINE`. This is the F-0024 family: an unearned claim in a phase report, discovered by the implementing actor's own post-commit verification rather than by the gate | HIGH | CLOSED | acceptance.engine | **SEVERITY REMAINS HIGH; it was not downgraded to obtain closure.** Closed under a LIMITED GOV-001 re-scoring authorization (`RSA-002`) granted by the human acceptance authority solely for this remediation. Closure evidence, all five conditions met together: (1) the false records are corrected in a SUPERSEDING `phase_8_report.json` whose every test record was genuinely re-executed, with no value transcribed from the defective revision; (2) `check_repository_structure.py` and its negative control are genuinely exit 0 - the offending docstring token is removed; (3) the acceptance guard was re-proven with the CORRECT exit codes, including a case that records the real failing validator run and is refused by C3/C5, which is precisely what should have happened at the first submission; (4) the new candidate passes full verification from scratch; (5) the superseding gate run was executed by the real checker under `RSA-002`. **The defective first revision is retained as `docs/acceptance/phase_8_report_rev1_defective.json` and its verdict is not deleted, altered or hidden** - it is immutable evidence that the first submission was accepted on an inaccurate record. `RSA-002` is scope-limited to this supersession and is explicitly not a precedent or a general re-scoring power |
| F-0042 | **The real runtime entrypoint imported a dependency no manifest declared.** `scripts/run_command_center.py` — the Command Center launcher and the T10 browser-evidence harness — imports `uvicorn` directly and serves the application with it, but `backend/pyproject.toml` declared it in neither `dependencies` nor any extra, and this repository has no Python lockfile. A canonical editable install on a clean Python 3.13 therefore produced fastapi, pydantic, sqlalchemy, alembic and yaml but **not** uvicorn, and the entrypoint stopped at `ModuleNotFoundError: No module named 'uvicorn'`. Accepted Phase 5 evidence depends on it: `EV-0048` and `phase_5_traceability.json` discharge the `ARK-REQ-0178`/`ARK-REQ-0229` e2e obligation against "a real uvicorn-served `surfaces.command` process", so that tier was reproducible only on a host that happened to have uvicorn already. Reproduced mechanically during ARKALI LIVE CHECKPOINT 1 against the untouched tree, before any repair | MEDIUM | CLOSED | surfaces.command | resolved in the Phase 8 pre-Package-3 remediation: `uvicorn>=0.52` is declared in `[project].dependencies` — the floor derived from the version verified on Python 3.13 during the checkpoint (0.52.1) under this file's existing major.minor policy, not chosen — and `test_script_dependencies.py` now derives every third-party import of every entrypoint script and requires the manifest to declare it, so the next undeclared dependency cannot hide. Severity MEDIUM: nothing fails open in the acceptance path, the gate never consults uvicorn, and `EV-0048` is not a false claim — the journey it records really did run against a real uvicorn. What was defective is reproducibility from the manifest, not the truth of the record, so no accepted phase is reopened and no historical evidence is edited. **No lockfile was created**; the repository still has none |
| F-0043 | **The test tier could not be collected from a canonical install, for the same reason as F-0042 and one layer deeper.** Four test modules — `test_vertical_slice_linkage.py`, `test_command_api.py`, `test_command_jobs_api.py` and `phase_7_harness.py` — use `fastapi.testclient.TestClient`. `starlette.testclient` binds an HTTP backend that starlette declares only under its own `full` extra, and `backend/pyproject.toml` declared neither `httpx2` nor `httpx` in any group. On the canonical Python 3.13 install the import raised `RuntimeError: The starlette.testclient module requires the httpx2 package`, pytest reported `Interrupted: 1 error during collection`, and **no evidence tier could run at all** — stricter than F-0042, which only stopped the entrypoint. It survived three phases because the previous Python 3.12 environment carried `httpx 0.28.1` as a transitive of an ad-hoc `fastapi-cli`, and the older `starlette 0.37.2` accepted it; the manifest never declared it and no control compared the two. Discovered mechanically while remediating F-0042 | MEDIUM | CLOSED | surfaces.command | resolved in the same Phase 8 pre-Package-3 remediation: `httpx2>=2.0.0` is declared in the `dev` extra — name and floor DERIVED from starlette's own metadata (`httpx2>=2.0.0; extra == 'full'`) and from its `testclient`, which binds `httpx2` and treats plain `httpx` as deprecated. It is `dev` rather than `dependencies` because no module under `backend/arkali/` imports `TestClient` or `httpx`: it is test-only, and shipping it to every install would overstate what the product needs. `test_script_dependencies.py` now asks the live starlette which backend it bound and requires the manifest to declare it, so a future backend change is followed rather than going stale. Severity MEDIUM: the failure is a loud collection error in the detecting direction — no PASS was ever obtained from an unrunnable suite, every historical test run really did execute, and no accepted phase is reopened. **No lockfile was created** |
| F-0044 | **A C-17 contract control refused a contract that had not changed.** `test_contract_drift.canonical` reduced an opaque object to an open record only when the schema carried NEITHER `properties` NOR `additionalProperties`, which conflates "the key is present" with "the key constrains". `additionalProperties: true` is the explicit spelling of *unconstrained* — precisely the case the branch exists to accept. Pydantic 2.8 rendered `payload: dict[str, object]` as `{type: object}`; Pydantic 2.13, resolved by the canonical `fastapi>=0.115` floor on the new Python 3.13 environment, renders the identical declaration as `{type: object, additionalProperties: true}`. Four controls in `TestTransportTypesMatchTheBackend` then failed, including both negative controls, because they call the same helper. **Proven stale before it was edited**: the backend field and the frontend types are byte-unchanged since `975b1df`, and both serialisers were run side by side to show only the spelling moved | MEDIUM | CLOSED | surfaces.command | resolved in the Phase 8 pre-Package-3 remediation: the branch now asks whether `additionalProperties` **constrains** — absent or `true` is an open record, `false` or a schema value is a real structure. **Strength is unchanged for every real constraint**, proven case by case: `false`, a schema-valued `additionalProperties` and any declared `properties` all still fall through to the refusal. Severity MEDIUM: the control failed in the **refusing** direction, so no contract drift was ever admitted and no accepted phase relied on a wrong answer; the four failures were a false alarm, not a missed defect |

Internal BLOCKER = 0, internal HIGH = 0. F-0015 was resolved by governance erratum ERR-001; F-0024 and F-0025 by the ERR-004 remediation. F-0036 was opened and closed within Phase 7 Package 3. F-0041 was opened and closed within Phase 8 Package 1. F-0042, F-0043 and F-0044 were opened and closed in the Phase 8 pre-Package-3 remediation. F-0037, F-0038 and F-0039 were opened and closed within Phase 7 Package 4; F-0038 and F-0039 are pre-existing Phase 5 defects that Package 4 exposed, reproduced on the Phase 5 routes alone before repair. F-0040 was opened and closed by the standalone pre-Phase-8 governance repair, before Phase 8 implementation began. F-0058, F-0059, F-0060 and F-0061 were opened and closed within the same Golden Repair pass-semantics/regression/context/fingerprint remediation session, before authorizing `golden-work-129-repair-3`; F-0058's own historical trigger (`golden-work-129-repair-2`'s real `GOLDEN_REPAIR_PASS`) is preserved unrewritten as evidence and is not itself reopened by this closure. A subsequent real, non-mutating inference-feasibility preflight found F-0060's own CLOSED status had overclaimed a second, unresolved mismatch; at the human acceptance authority's own explicit direction, F-0060 is SCOPE CORRECTED (still CLOSED, now naming only the genuinely-fixed token/context half), that unresolved half is tracked separately as **F-0062** (`REPAIR_TIMEOUT_BUDGET_ROBUSTNESS_GAP`, MEDIUM, **OPEN**, non-blocking), and a real regression-measurement environment gap the same preflight found is tracked and closed the same session as **F-0063** (`DYNAMIC_REGRESSION_PROBE_ENVIRONMENT_INSUFFICIENCY`, HIGH, CLOSED) — a distinct root cause from, and no reopening of, F-0059's own genuinely-fixed original wiring defect. A separate real information-leak audit then found the real repair prompt sent corpus-authored evaluator-only metadata (title/rationale/defect_class/injection_target) to the repair model for all 8 corpus entries; fixed and closed the same session as **F-0064** (`GOLDEN_REPAIR_EVALUATOR_INFORMATION_LEAK`, HIGH, CLOSED) — `golden-work-129-repair-1/2/3`'s own historical results are unchanged but must now be read as repairs completed under the historical leaking harness, not leak-free proof. The separate current-unrepairable-instance corpus-design question, left unwritten pending this same review, is now recorded as **F-0065** (`CURRENT_UNREPAIRABLE_INSTANCE_INVALIDATED`, MEDIUM, **OPEN**, blocking `golden-work-129-repair-4` and any `ARK-REQ-0188` discharge on the current corpus instance, non-blocking otherwise) — F-0064's own fix does not resolve it, and its invalidation of `permission_check_removal`'s rationale is independent of the leak, not merely a consequence of it. A separate real ARKALI LIVE PRODUCT CHECKPOINT then found and closed, same session, **F-0066** (`GOLDEN_ACCEPTANCE_DECLARED_CAPABILITY_COVERAGE_GAP`, HIGH, CLOSED, `acceptance.engine`) — the acceptance journey's own related-resource-create silent-skip and never-verified edit-form prefill, root-cause-consolidated under one generic declared-capability-coverage invariant, fixed and proven across two synthetic domains; `golden-work-129`'s own historical `ACCEPTED`/`GOLDEN_ACCEPTANCE_PASS` state is unchanged, product-generation code is entirely untouched, and several further product-generation-family findings from that same checkpoint remain proposed, not yet recorded. The first of those, the generator-side root cause behind F-0066's own edit-prefill manifestation, is now fixed and closed the same session as **F-0067** (`GENERATED_FORM_INITIAL_STATE_WIRING_GAP`, HIGH, CLOSED, `engineering.factory`) — a new `frontend_forms`-scoped preflight catches a reused form component that never wires a real record's own data into its own field state, proven statically and via a real browser-driven React runtime test across two structurally unrelated domains; `golden-work-129` itself and F-0066's own acceptance-layer fix are both untouched, and no new candidate was generated this session. Family 1 (Task/Work Management) was then attempted fresh, real, from the canonical `GOAL` text alone: `golden-work-130` (real `qwen2.5-coder:14b`, real elapsed 1379.8s) reached a real, honest, terminal `STAGE_FAILED` at `product_ux_spec`, never a fabricated PASS. Root-caused and fixed the same session as **F-0068** (`PRODUCT_UX_MODULE_PARITY_NORMALIZATION_GAP`, HIGH, CLOSED, `engineering.factory`) — `_module_parity_findings` now reuses the sibling action/route check's own existing "model"-suffix normalization (`_resource_matching_name`) instead of a stricter, un-normalized comparison, resolving the identical real defect class independently reproduced across `golden-work-095`, `golden-work-098` and `golden-work-130`; `golden-work-130` itself is untouched, its real `STAGE_FAILED` state preserved as historical evidence, and no retry was started this session.

Three HIGH defects were found by independent review at HUMAN GATE 1 and are now closed: F-0005 (false closure of ARK-REQ-0090/0091), F-0006 (five Phase 0A deliverables absent), F-0007 (ARK-REQ-0012 misclassification). See `KNOWN_FAILURES.md`.

## External blockers

| ID | Blocker | Severity | Status | Owner | Resolution |
|---|---|---|---|---|---|
| EXT-001 | HUMAN GATE 1 — Phase 0A+0B acceptance not yet granted | BLOCKER | CLOSED | human acceptance authority | granted on candidate `007ebf6`, record `HGR-001` |
| EXT-002 | F-0015 — `engineering.import` module root is a Python reserved keyword | HIGH | CLOSED | human acceptance authority | governance erratum ERR-001 authorized and applied |

**No external blockers open.** Phase 7 is machine-accepted; Phase 8 is unlocked.

Next gate condition: **HUMAN GATE 2** at Phase 23 (Stable Core promotion), or an earlier trigger of GATE 4 (security boundary change), GATE 5 (applicability waiver), GATE 6 (migration APPLY to real/stable data) or GATE 8 (architecture budget exception).

## Deferred items (not blockers, tracked so they are not lost)

| ID | Item | Deferred to | Reason |
|---|---|---|---|
| DEF-001 | Architecture gate implementation + negative-control fixtures | Phase 2 | requires source code to analyse |
| DEF-002 | Phase Gate Checker implementation | Phase 2 | specification only in Phase 0 |
| ~~DEF-003~~ | ~~Isolation backend availability probe~~ | — | **CLOSED at Phase 4.** Seven declared backends probed read-only on the real host; four PASS, three NOT_CONFIGURED. No feature installed or enabled |
| DEF-004 | Golden Repair corpus **instantiation** (ARK-REQ-0187, 0188) | Phase 30 | Definition delivered at Phase 0B per BP §Phase 0B. Instantiation requires an accepted Golden Product as injection target — state is NOT_APPLICABLE at Phase 0B, not deferred-and-closed |
| DEF-005 | Canonical Windows clean-test baseline **image** | Phase 36 | definition exists; VM snapshot is an acceptance-environment asset |
| DEF-006 | `ARKALI_RELEASE_QUALITY_REPORT.md` content contract | Phase 26 | LOW; undefined in the canonical set, carried forward honestly |
| ~~DEF-007~~ | ~~Contract inventory~~ | — | **WITHDRAWN.** Deferring it was defect F-0006: Build Protocol §Phase 0A requires the inventory at Phase 0A. Delivered as `docs/canonical/CONTRACT_INVENTORY.md`. Phase 2 implements the schemas, not the inventory |
| DEF-008 | Contract **schema files** under `docs/contracts/` | Phase 2 | the inventory (0A) names 36 families and their planned locations; the schemas themselves are implementation |
| DEF-009 | **REAL PRODUCT READINESS GAP — no live orchestrator chains GOAL → BLUEPRINT → ROUTING → MODEL → CANDIDATE → PRODUCT.** Mechanically confirmed by ARKALI REAL PRODUCT CHECKPOINT 2 (live OpenAPI schema of `scripts/run_command_center.py`'s served app carries no route for a goal, blueprint, candidate, local-AI, or product-output; `grep` across `surfaces/`, `scripts/`, `frontend/`, `src-tauri/` finds zero callers of `derive_blueprint`, `select_execution_tier`, `generate_product`, or any `engineering.localai` adapter outside their own package and `backend/tests/`). Three distinct sub-gaps, not one: **(a)** a live worker/scheduler execution loop — `execution.scheduler` (Phase 8, C-21) delivered the worker *contract* only, honestly recorded at its own acceptance ("Production admission returns `CAPABILITY_NOT_CONFIGURED`... that is designed behaviour"); no later phase title in the matrix claims building the loop itself — **owner not canonically assigned, unresolved**. **(b)** a live orchestrating surface wiring the existing pipeline together — Phase 16's own acceptance record states plainly "no execution engine built"; MS's frontend-delivery rule (§ "Capability phases deliver their own frontend increment... Phase 27 consolidates... it is not the first integration") assigns Phase 27 only navigation/cross-area UI consolidation, not backend orchestration — **owner not canonically assigned, unresolved**. **(c)** real AI-provider/local-model-driven multi-file product generation beyond the accepted Tier-1 deterministic, single-file `product_generation.py` ceiling — **Phase 30** (Golden Product + Golden Repair Verification), confirmed three ways: `product_generation.py`'s own docstring ("Golden Factory Acceptance requires... Phase 30 — not claimed here"), the dependency matrix (Phase 30 prerequisites include Phase 16, 14 **and 24**), and Phase 16's own acceptance row ("no Golden Factory Acceptance claimed, Phase 30's own obligation") | Phase 30 for (c); (a) and (b) unresolved — re-measure at each Real Product Checkpoint | Not a regression in any accepted phase: Phases 8, 16, 22, 22B and 23 each honestly recorded this absence as designed behaviour at their own acceptance. Explicitly **out of Phase 24's canonical scope** — Phase 24 (`ARK-REQ-0131`–`0133`, `0358`, C-36) builds child-product evolution/SDK machinery for products that already exist; it does not build initial generation. Tracked here per repository convention (mirrors DEF-004's shape exactly) rather than inventing a new tracking system. Re-measured at Real Product Checkpoints 3 (first live orchestration/scheduler capability), 4 (first ARKALI-produced running application) and 5 (v1→v2 upgrade/rollback cycle) |

**Phase 30 ownership resolution (D-027).** The historical DEF-009 row above is
retained verbatim as the discovery evidence that caused the stop. Human ruling
D-027 now assigns only the top-level Phase 30 production goal→release
composition to `engineering.factory`; the live loop's admission/resource work
remains `execution.scheduler`, durable state remains `execution.durable`, and
workflow execution remains `execution.workflow`. This resolves the ownership
ambiguity, not the missing behaviour: every DEF-009 functional sub-gap remains
OPEN until shipping implementation and real-product evidence close it.

## Residual MEDIUM/LOW findings carried from the canonical repair

These were reported at the canonical repair and remain open by design; none blocks Phase 0.

Opened at Phase 4, **all closed within the phase**: F-0022 (structure check 11 flagged a mention as a mutation path) · F-0023 (LOW — secret-shaped test fixtures tripped the secret scanner).

Opened at Phase 3, **all now closed**: F-0018 closed by erratum ERR-002 (Plugin `REMOVED` is terminal) · F-0019 closed — all 9 numeric budgets are now evaluated by the gate · F-0020 closed by erratum ERR-003 (ratified budget measurement contract; three exposed violations repaired by decomposition) · F-0021 opened and closed within Phase 3 (expiring negative controls). No Phase 3 finding remains open.

MEDIUM: installer code-signing vs SmartScreen on a clean baseline · "Temporal-grade" unmeasurable except behaviorally · mutation-testing budget at system scale · fine-tuning acceptance path · air-gapped signing key management · Git branching/remote model unspecified · knowledge-lifecycle transition rules undefined · Phase 0 deliverable breadth · workflow derived-cache invalidation complexity · Import/Rescue phase-level budget.

LOW: `ARKALI_RELEASE_QUALITY_REPORT.md` contents · `ADR_INDEX` format conventions · PostgreSQL path unverified · "Transaction/Transactions" plural mismatch in the canonical set · `docs/canonical` vs `docs/contracts` scope overlap · MCP adapters unverified · skill-mode verification implied via Phase 27 rather than an explicit VDC clause.
